| Day | File | Primary Topic | Angle | Covered Scope | Extension Ideas |
|---:|---|---|---|---|---|
| 01 | `content/資訊安全/Day01_SQL_Injection.md` | SQL Injection | 入門 | SQLi 基本原理、登入繞過、Prepared Statement、密碼雜湊、縱深防禦 | ORM 查詢建構器誤用、Second-order SQLi、Stored procedure 風險 |
| 02 | `content/資訊安全/Day02_XSS.md` | XSS | 入門 | Stored/Reflected/DOM XSS、留言板案例、輸出編碼、CSP、自我檢查 | Trusted Types、HTML sanitizer 比較、CSP rollout 與 report-only |
| 03 | `content/資訊安全/Day03_CSRF.md` | CSRF | 入門 | CSRF 條件、轉帳案例、SameSite、CSRF token、JWT/Bearer token 情境 | Login CSRF、OAuth state 綁定、SameSite 相容性與例外 |
| 04 | `content/資訊安全/Day04_Password_Hashing.md` | Password Hashing | 入門 | 密碼不可明文/快速 hash、salt、慢雜湊、Java/Go 範例、timing attack 提醒 | Argon2 參數調校、密碼重雜湊 migration、credential stuffing 防禦 |
| 05 | `content/資訊安全/Day05_JWT_vs_Session.md` | Session vs JWT | 入門 | Session/JWT 差異、JWT 常見漏洞、適用情境、檢查表 | Token revocation、refresh token rotation、session store 高可用 |
| 06 | `content/資訊安全/Day06_Brute_Force_Protection.md` | Brute Force Protection | 入門 | Rate limiting、漸進延遲、CAPTCHA、帳號凍結、異常通知、完整流程 | Credential stuffing 偵測、風險式驗證、裝置指紋與隱私 |
| 07 | `content/資訊安全/Day07_Broken_Access_Control_IDOR.md` | Broken Access Control / IDOR | 入門 | IDOR、UUID 不是解法、垂直越權、default deny、分層防禦 | ABAC/ReBAC 設計、租戶隔離測試、policy engine 導入 |
| 08 | `content/資訊安全/Day08_Input_Validation_Mass_Assignment.md` | Input Validation / Mass Assignment | 混合入門 | Mass Assignment、DTO、allowlist、型別/格式/商業邏輯/跨欄位驗證、validation 攻擊面 | 純 Input Validation 延伸、schema validation、錯誤訊息洩漏、驗證規則測試 |
| 09 | `content/資訊安全/Day09_Security_Headers_CORS.md` | Security Headers / CORS | 入門 | HSTS、CSP、X-Frame-Options、nosniff、Referrer-Policy、Permissions-Policy、CORS 設定 | CSP nonce/hash 實作、CORS preflight cache、credentialed CORS 審查 |
| 10 | `content/資訊安全/Day10_SSRF_Server_Side_Request_Forgery.md` | SSRF | 入門 | 使用者控制 URL、metadata service、內網 pivot、port scan、blind SSRF、黑名單問題、DNS rebinding、redirect、Java/Go 防禦 | DNS pinning、egress proxy、cloud metadata hardening、SSRF 偵測 |
| 11 | `content/資訊安全/Day11_Path_Traversal_File_Upload.md` | Path Traversal / File Upload | 入門 | 檔名路徑穿越、黑名單繞過、canonical path、安全上傳、常見陷阱 | Symlink race、object storage upload policy、檔案內容掃描 |
| 12 | `content/資訊安全/Day12_Command_Injection.md` | Command Injection | 入門 | Shell 拼接風險、命令注入能力、避免 shell、參數化呼叫、變形與陷阱 | Argument injection、ImageMagick/ffmpeg 安全包裝、sandboxing |
| 13 | `content/資訊安全/Day13_XXE_XML_External_Entity.md` | XXE | 入門 | XML Entity、讀檔 payload、SSRF、Blind XXE、Billion Laughs、Java parser 設定、Go 注意事項 | SAML/SOAP XXE、parser-specific hardening、legacy XML migration |
| 14 | `content/資訊安全/Day14_Insecure_Deserialization.md` | Insecure Deserialization | 入門 | Java ObjectInputStream、Jackson default typing、FastJSON、SnakeYAML、Go 型別混淆、防禦原則 | Gadget chain 案例、ObjectInputFilter、套件 CVE 分析 |
| 15 | `content/資訊安全/Day15_Secrets_Management.md` | Secrets Management | 入門 | Secret 類型、錯誤做法、分層防禦、log 不洩漏、檢查清單 | Secret rotation、KMS/Vault 整合、CI secret scanning |
| 16 | `content/資訊安全/Day16_Security_Logging_Monitoring.md` | Security Logging / Monitoring | 入門 | 安全日誌欄位、不可記錄資料、SLF4J/MDC、Go slog、trace ID、警報 | SIEM detection rules、audit log tamper resistance、PII redaction |
| 17 | `content/資訊安全/Day17_Rate_Limiting.md` | Rate Limiting / API Throttling | 入門 | 演算法、Bucket4j、Redis Lua、Go rate limiter、部署層級與坑 | Distributed quotas、fairness、per-user/per-IP 混合策略 |
| 18 | `content/資訊安全/Day18_Supply_Chain_Vulnerable_Dependencies.md` | Supply Chain / Dependencies / SBOM | 入門 | 供應鏈攻擊類型、Dependency-Check、govulncheck、Trivy、SBOM | SLSA、dependency confusion、lockfile review、provenance |
| 19 | `content/資訊安全/Day19_TLS_Cryptographic_Failures.md` | TLS / Cryptographic Failures | 入門 | TLS 基礎、常見加密失誤、敏感資料外洩、安全 API 呼叫 | mTLS rollout、certificate pinning 取捨、cipher policy |
| 20 | `content/資訊安全/Day20_Open_Redirect.md` | Open Redirect | 入門 | 釣魚風險、危險 redirect 寫法、allowlist、防禦、OAuth redirect_uri 注意 | OAuth mix-up、deep link redirect、signed return URL |
| 21 | `content/資訊安全/Day21_SSTI_Server_Side_Template_Injection.md` | SSTI | 入門 | SSTI 原理、與 XSS 差異、危險後端寫法、防禦、使用者可編輯模板 | Template sandbox bypass、Jinja/Freemarker/Thymeleaf 差異 |
| 22 | `content/資訊安全/Day22_Race_Condition_TOCTOU.md` | Race Condition / TOCTOU | 入門 | 競爭條件、銀行轉帳案例、併發攻擊、DB transaction、悲觀鎖、條件式 update、樂觀鎖、分散式鎖、idempotency | DB isolation 深入、併發測試、分散式一致性與補償 |
| 23 | `content/資訊安全/Day23_HTTP_Request_Smuggling.md` | HTTP Request Smuggling | 入門 | CL/TE 不一致、CL.TE、Go/Java 安全性、HTTP/2 downgrade、防禦 | Proxy chain 測試、H2/H3 邊界、WAF/CDN 行為差異 |
| 24 | `content/資訊安全/Day24_OAuth2_OIDC_Pitfalls.md` | OAuth2 / OIDC Pitfalls | 入門 | Authorization Code Flow、redirect_uri、PKCE、state、ID Token 驗證、Grant Type | Token substitution、nonce、multi-tenant issuer validation |
| 25 | `content/資訊安全/Day25_BOPLA_GraphQL_Excessive_Data_Exposure.md` | BOPLA / GraphQL Excessive Data Exposure | 入門 | BOLA vs BOPLA、讀取面暴露、寫入面 Mass Assignment、角色欄位、GraphQL、N+1 | Field-level auth framework、schema lint、GraphQL auth tests |
| 26 | `content/資訊安全/Day26_Webhook_Security.md` | Webhook Security | 入門 | HMAC 簽章、重放攻擊、冪等性、出站 Webhook SSRF、檢查表 | Webhook retry policy、secret rotation、tenant isolation |
| 27 | `content/資訊安全/Day27_MFA_TOTP.md` | MFA / TOTP | 入門 | MFA 要素、RFC 6238、後端流程、Java/Go、重放、防備援碼 | Recovery flow abuse、step-up auth、TOTP drift 與鎖定策略 |
| 28 | `content/資訊安全/Day28_FIDO2_WebAuthn_Passkey.md` | FIDO2 / WebAuthn / Passkey | 入門 | WebAuthn/Passkey 關係、防釣魚、密碼學、Java/Go、地雷、傳統 FIDO2 vs Passkey | Account recovery、resident key、enterprise attestation |
| 29 | `content/資訊安全/Day29_NoSQL_Injection.md` | NoSQL Injection | 入門 | MongoDB 登入繞過、型別混淆、Java/Go 範例、Aggregation Pipeline、防禦清單 | Elasticsearch injection、query DSL allowlist、operator injection tests |
| 30 | `content/資訊安全/Day30_Web_Cache_Poisoning.md` | Web Cache Poisoning | 入門 | Cache key vs unkeyed input、X-Forwarded-Host、污染手法、Java/Go、CDN 設定 | Cache deception、Vary header strategy、CDN-specific hardening |
| 31 | `content/資訊安全/Day31_ReDoS_Regex_Denial_of_Service.md` | ReDoS | 入門 | 指數回溯、災情、Java email validator、Go regexp 安全性、防禦清單 | Regex linting、timeout strategy、RE2 migration |
| 32 | `content/資訊安全/Day32_Timing_Attack_Constant_Time_Compare.md` | Timing Attack / Constant-Time Compare | 入門 | 字串比對時序洩漏、API token 驗證、Go 標準庫、其他時序洩漏點 | Remote timing feasibility、padding oracle、constant-time review |
| 33 | `content/資訊安全/Day33_Session_Fixation.md` | Session Fixation | 入門 | Session fixation 原理、與 hijacking 差異、登入後換 session ID、防禦、測試 | Session rotation edge cases、single sign-out、device binding |
| 34 | `content/資訊安全/Day34_CRLF_Injection_HTTP_Header_Injection.md` | CRLF / HTTP Header Injection | 入門 | CRLF、response splitting、常見變體、防禦層次、驗證範例 | Log injection、email header injection、proxy header normalization |
| 35 | `content/資訊安全/Day35_Subdomain_Takeover.md` | Subdomain Takeover | 入門 | Dangling DNS、常見服務、接管影響、偵測腳本、流程防禦 | Cloud inventory automation、CI/CD DNS lifecycle、brand monitoring |
| 36 | `content/資訊安全/Day36_Clickjacking_UI_Redressing.md` | Clickjacking / UI Redressing | 入門 | iframe 點擊劫持、變體、後端責任、X-Frame-Options/CSP、防禦範例 | CSP frame-ancestors migration、OAuth consent clickjacking |
| 37 | `content/資訊安全/Day37_JWT_Algorithm_Confusion.md` | JWT Algorithm Confusion | 入門 | JWT 結構、alg none、RS256 to HS256、kid 注入、Java/Go、防禦與 CVE | JWKS cache poisoning、key rotation、multi-tenant issuer isolation |
| 38 | `content/資訊安全/Day38_X_Forwarded_For_Spoofing.md` | X-Forwarded-For / Proxy Header Trust | 入門 | 錯信 header、XFF 正確讀法、災難情境、Spring/Go、Cloudflare/CDN | Multi-proxy chain policy、rate limit identity、trusted proxy config tests |
| 39 | `content/資訊安全/Day39_DNS_Rebinding.md` | DNS Rebinding | 入門 | 瀏覽器變內網跳板、攻擊流程、與 SSRF 差異、後端四道防線、Actuator 陷阱 | DNS pinning implementation、admin panel hardening、browser private network access |
| 40 | `content/資訊安全/Day40_HTTP_Parameter_Pollution.md` | HTTP Parameter Pollution | 入門 | 重複參數、框架差異、攻擊情境、漏洞程式碼、防禦與測試 | API gateway normalization、OAuth parameter pollution、contract tests |
| 41 | `content/資訊安全/Day41_LDAP_Injection.md` | LDAP Injection | 入門 | LDAP filter、登入驗證漏洞、防禦、程式碼審查清單 | Active Directory edge cases、escaping libraries、SSO directory integration |
| 42 | `content/資訊安全/Day42_Cookie_Security_Attributes.md` | Cookie Security Attributes | 入門 | HttpOnly、Secure、SameSite、`__Host-`、實作、檢查表、迷思 | Cookie partitioning、CHIPS、跨站登入兼容性 |
| 43 | `content/資訊安全/Day43_Prototype_Pollution_Object_Injection.md` | Prototype Pollution / Object Injection | 入門 | JS prototype pollution、Jackson readerForUpdating、Go mergo/map 合併、防禦與測試 | Lodash CVE、schema hardening、recursive merge audit |
| 44 | `content/資訊安全/Day44_ZIP_Slip_Archive_Extraction.md` | ZIP Slip / Archive Extraction | 入門 | 壓縮檔路徑穿越、Java/Go 解壓縮、惡意 zip 測試、zip bomb、防禦規則 | Tar symlink attacks、nested archive limits、antivirus pipeline |
| 45 | `content/資訊安全/Day45_WebSocket_Security.md` | WebSocket Security | 入門 | CSWSH、Java/Go 正確寫法、長連線授權、其他地雷、檢查清單 | Token refresh over WebSocket、message-level auth、backpressure DoS |
| 46 | `content/資訊安全/Day46_Host_Header_Injection.md` | Host Header Injection | 入門 | Host header 原理、後端踩雷、攻擊手法、漏洞範例、防禦 | Password reset poisoning、reverse proxy allowed hosts、absolute URL generation |
| 47 | `content/資訊安全/Day47_Insecure_Randomness.md` | Insecure Randomness | 入門 | PRNG vs CSPRNG、常見災難、Java/Go 範例、SecureRandom 地雷 | Token entropy testing、UUID misuse、randomness in distributed systems |
| 48 | `content/資訊安全/Day48_HMAC_API_Request_Signing.md` | HMAC / API Request Signing | 入門 | Request signing、canonical string、接收端地雷、密鑰管理、檢查表 | Canonicalization pitfalls、clock skew/replay design、key rotation |
| 49 | `content/資訊安全/Day49_BFLA_Broken_Function_Level_Authorization.md` | BFLA | 入門 | 功能層級授權、漏洞樣貌、deny by default、細節、API 測試、檢查表 | Route inventory、policy-as-code、admin endpoint testing |
| 50 | `content/資訊安全/Day50_GraphQL_Security.md` | GraphQL Security | 入門 | Introspection、深度/複雜度攻擊、Alias/Batching 繞 rate limit、細節、檢查表 | Persisted queries、operation cost model、GraphQL auth middleware |
| 51 | `content/資訊安全/Day51_gRPC_Protobuf_Security.md` | gRPC / Protobuf Security | 入門 | gRPC 攻擊面、Protobuf 反序列化 DoS、MaxRecvMsgSize、TLS/mTLS、檢查表 | Service mesh mTLS、protobuf schema evolution、gRPC auth interceptor |
| 52 | `content/資訊安全/Day52_Insecure_Deserialization.md` | Insecure Deserialization | 重寫/補強 | Java 原生序列化 RCE、ObjectInputFilter、Jackson polymorphism、SnakeYAML、Go 注意事項 | Gadget chain 深入、JEP 415、Runtime detection、CVE case study |
| 53 | `content/資訊安全/Day53_SSRF_Server_Side_Request_Forgery.md` | SSRF | 重寫/補強 | SSRF 本質、metadata endpoint、縮圖服務案例、黑名單 IP 失效、Java/Go safe HTTP client、檢查表 | Egress proxy、redirect/DNS pinning、cloud metadata hardening、blind SSRF detection |
| 54 | `content/資訊安全/Day54_XXE_XML_External_Entity_Injection.md` | XXE | 重寫/補強 | DTD/外部實體、預設設定風險、讀檔與打內網、Java 禁用外部實體、Go encoding/xml 注意 | SAML/SOAP 實戰、parser regression tests、legacy XML parser migration |
| 55 | `content/資訊安全/Day55_Mass_Assignment_Auto_Binding.md` | Mass Assignment / Auto-Binding | 重寫/補強 | Auto-binding 本質、Spring 反例與 DTO/JsonIgnore/setAllowedFields、Go json.Unmarshal model 陷阱、檢查表 | Framework-specific binding rules、OpenAPI schema tests、BOPLA write-side auth |
| 56 | `content/資訊安全/Day56_Race_Condition_TOCTOU.md` | Race Condition / TOCTOU | 重寫/補強 | Check/use 非原子、Java synchronized/樂觀鎖/悲觀鎖、Go mutex/atomic DB update、檢查表 | Isolation levels、idempotency key design、concurrency fuzz tests、distributed lock failure modes |
| 57 | `content/資訊安全/Day57_JWT_Common_Pitfalls.md` | JWT Common Pitfalls | 延伸 | JWT 安全依賴簽章、alg none、decode without verify、演算法混淆、弱密鑰、Java/Go 驗證、檢查表 | JWKS/kid injection、key rotation、refresh token rotation、multi-tenant issuer/audience validation |
| 58 | `content/資訊安全/Day58_CORS_Misconfiguration.md` | Security Headers / CORS | 延伸（承 Day09） | CORS 白名單比對 bug（startsWith/endsWith/contains/未錨定 regex）、反射 Origin、null origin（sandboxed iframe）、Vary: Origin 快取污染、Spring setAllowedOrigins/OriginPatterns、Go map 精確比對、code review/測試清單 | 子網域接管與 CORS 信任鏈、CDN/proxy cache key 與 Origin、CORS 與 CSRF 區隔誤區 |
| 59 | `content/資訊安全/Day59_CSV_Formula_Injection.md` | CSV / Formula Injection | 入門 | 公式注入本質（試算表把 `=+-@\t\r` 開頭當公式）、HYPERLINK/WEBSERVICE/IMPORTXML/DDE 危害、輸入驗證 vs 輸出逸出分離、`'` 中和 + CSV 結構逸出、Java 手寫 CSV 與 Apache POI setCellValue vs setCellFormula、Go encoding/csv 不做公式逸出、誤區與測試清單 | XLSX/匯入端解析硬化（zip bomb/XXE）、其他匯出格式注入、Google Sheets 特有函數外洩 |
| 60 | `content/資訊安全/Day60_XLSX_Parsing_Import_Hardening.md` | XLSX 匯入端解析硬化 | 延伸（承 Day44 × Day13/Day54） | 匯出逸出 vs 匯入解析兩道防線對照、XLSX=ZIP+XML 結構拆解、解壓放大/zip bomb（POI ZipSecureFile setMinInflateRatio/setMaxEntrySize/setMaxTextSize、IOUtils.setByteArrayMaxOverride；Go excelize UnzipSizeLimit/UnzipXMLSizeLimit 下修預設）、XLSX 內藏 XXE（POI 預設關閉、自拆 XML 才需套禁用 DTD、Go encoding/xml 不吃外部實體）、外部參照/公式回連 SSRF、streaming 解析與縱深設計、檢查表與測試 | 其他 Office 格式（docx/pptx）解析硬化、polyglot/magic bytes 檔案真身驗證、formula evaluator 沙箱化 |
| 61 | `content/資訊安全/Day61_SSTI_Template_Engine_Hardening.md` | SSTI | 延伸（承 Day21） | 模板來源可控才是 SSTI 本質（compile 階段 vs render 階段）、Thymeleaf 動態 view name/fragment 表達式、FreeMarker new Template(StringReader)/?new/?eval 與 setNewBuiltinClassResolver(ALLOWS_NOTHING)/setAPIBuiltinEnabled/ObjectWrapper 設定、Velocity evaluate vs getTemplate 常數路徑、Go text/template vs html/template（防 XSS 非防 SSTI）/Parse 使用者字串/FuncMap 與 {{.}} 資訊洩漏、sandbox 繞過本質（可達物件圖+黑名單）、Code Review/grep checklist 與回歸測試 | 各引擎 sandbox bypass CVE 案例、Pebble/Groovy/Jinja 對照、使用者自訂模板的受限 DSL 設計、隔離程序+egress 控制實作 |
| 62 | `content/資訊安全/Day62_Mass_Assignment_Nested_Binding.md` | Mass Assignment / Auto-Binding | 延伸（承 Day08 / Day55） | 巢狀／集合綁定才是 DTO 之後仍中招的點（白名單必須跟物件圖往下鑽）、Spring nested path binding（role.name/items[0].price/account.permissions[0]）、@InitBinder setAllowedFields/setDisallowedFields 巢狀萬用字元陷阱（items*.quantity/shipping.address.*、disallow 漏 *）、@InitBinder 只管表單而 @RequestBody 走 Jackson（巢狀貧血 DTO/@JsonProperty READ_ONLY）、readerForUpdating 部分更新的巢狀提權、Go 巢狀 struct json.Unmarshal/DisallowUnknownFields 遞迴、PATCH map[string]any deepMerge 用巢狀完整路徑 allowlist 過濾、OpenAPI additionalProperties:false + CI 越權回歸測試 | 多型/@JsonTypeInfo 綁定越權、集合元素層級授權、GraphQL input object 巢狀綁定、ORM cascade 連動寫入風險 |
| 63 | `content/資訊安全/Day63_ESI_Injection.md` | ESI Injection（Edge Side Includes） | 入門 | 漏洞在 CDN/反向代理而非應用碼、ESI 標籤（esi:include/esi:vars）邊緣節點組裝頁面、Surrogate-Control content="ESI/1.0" 觸發 ESI、偵測 payload、危害四類（SSRF 從邊緣發出、繞 HttpOnly 讀 cookie、cache poisoning、邊緣 SSTI/XSLT RCE）、HTML 逸出≠ESI 逸出、Java Spring 反例與 Go html/template、Varnish do_esi 白名單與 origin-only header、include 來源白名單、egress 控制、code review/偵測測試清單 | 各 ESI 實作差異（Akamai XSLT/Fastly/Varnish）、surrogate header 全鏈路盤點自動化、與 Day30 cache poisoning 的交叉、邊緣端 RCE gadget |
| 64 | `content/資訊安全/Day64_Web_Cache_Deception.md` | Web Cache Deception（網頁快取欺騙） | 入門 | 與 Day30 Cache Poisoning 方向相反（讀出私密 vs 寫入污染）、攻擊鏈五步（誘導已登入受害者點 /account/profile/x.css → CDN 看副檔名快取且忽略 Cookie → origin 寬鬆比對回 200 私密頁 → 快取存個資 → 攻擊者未登入 cache hit 讀取）、根因為 origin 與 CDN 兩套世界觀不一致（CDN 用副檔名/前綴/Content-Type 猜靜態並忽略 cookie；origin 路徑正規化/後綴比對/catch-all 把假副檔名 URL 接到私密 handler）、防禦 1 動態回應一律 Cache-Control: no-store（no-store vs private vs no-cache 區別、default-deny 快取白名單 opt-in、Spring ResourceHandler+OncePerRequestFilter、Go middleware staticPrefixes）、防禦 2 嚴格路徑比對讓 /account/profile/x.css 直接 404（Spring useSuffixPatternMatch/AntPathMatcher 反例、PathPatternParser、Go path.Clean 後精確比對）、防禦 3 CDN cache key/靜態動態網域隔離/normalization 對齊、code review 與偵測回歸測試清單 | CDN 廠商特定快取規則（Cloudflare/Akamai/Fastly）、path normalization 差異模糊測試自動化、與 Day30/Day63 邊緣快取家族交叉、靜態網域隔離架構落地 |
| 65 | `content/資訊安全/Day65_Email_Header_Injection.md` | Email Header Injection（郵件標頭注入 / SMTP 注入） | 入門 | 與 Day34 同源 CRLF 但 sink 換成 email/MIME（非 HTTP 回應）、RFC 5322 標頭區/空行分界原理、輸入含 \r\n 偷插 Bcc/Cc/From 或整段 MIME body（垃圾信/釣魚信跳板，過你 SPF/DKIM）、最常中招欄位（收件人/Cc/Bcc、Reply-To/From 顯示名稱、Subject、自訂 header、範本插值點）、Java 反例 addHeader/setHeader 手拼 vs 正解 InternetAddress(strict=true)+setReplyTo/setRecipients/setSubject(RFC 2047 encoding)、Go net/smtp.SendMail 收原始 []byte 自拼 header 風險 vs 正解 net/mail.ParseAddress 驗證+sanitizeHeaderValue reject CR/LF+mime.QEncoding 主旨+mail.Address.String()、防禦三原則（用函式庫型別把關/標頭值 reject CR/LF/收件人由系統決定 envelope 一致）、code review grep 重點與 CRLF payload 偵測回歸測試 | DMARC/SPF/DKIM 寄件網域驗證、寄信服務（SES/SendGrid）設定誤區、HTML email 內容類注入、寄信端點 rate limit 濫用防護 |
| 66 | `content/資訊安全/Day66_DMARC_SPF_DKIM_Email_Authentication.md` | SPF / DKIM / DMARC 寄件網域驗證 | 延伸（承 Day65） | 威脅換層：Day65 是「後端被注入」、本篇是「網域被冒名」（程式碼乾淨攻擊者仍可從自己主機寄 From: 你的網域）、SMTP 雙寄件者（信封寄件者 MAIL FROM/Return-Path vs 標頭 From: 各自獨立不驗證）、三層分工（SPF 授權寄件 IP 驗信封寄件者 TXT v=spf1 include -all、DKIM 私鑰簽章 d=/s=selector/h= 保內容完整與授權、DMARC 把前兩者對齊到使用者看到的 From: _dmarc TXT p=none/quarantine/reject + rua + adkim/aspf）、缺一不可原因（SPF/DKIM 都不直接管 From: 網域，DMARC 補對齊縫）、後端殺手 alignment（SPF alignment：服務商共享 Return-Path amazonses.com 不對齊→設自訂 MAIL FROM 子網域；DKIM alignment：共享 d= 不對齊→啟用自有網域 DKIM CNAME 讓 d=your-domain）、Java SES SDK v2 fromEmailAddress+feedbackForwardingEmailAddress 寫死已驗證 From、Go sesv2 同概念 From 用常數不讓使用者控制網域、SES/SendGrid 六大誤區（只驗單一信箱/共享 Return-Path 與 DKIM/直接 p=reject 沒先 p=none/~all+all/SPF 超過 10 次 lookup PermError/多寄件來源只設一個 DKIM）、驗證測試（raw header Authentication-Results spf/dkim header.d 對齊/dmarc=pass、CI dig TXT/CNAME 斷言、rua 報告盤點冒名與漏設來源） | BIMI 與品牌驗證、DKIM key rotation/selector 輪替、SES SNS bounce/complaint 事件處理、DMARC 報告解析自動化、子網域寄件策略與信任隔離 |
| 67 | `content/資訊安全/Day67_Open_Redirect_Parser_Differential_Signed_URL.md` | Open Redirect | 延伸（承 Day20） | 不重講 Day20 入門（定義/釣魚/基本白名單/常見繞過 payload/跳轉代碼映射），聚焦「白名單寫對仍被繞過」、核心問題 parser differential（驗證用的 parser 與跳轉用的 client 對同一畸形 URL 解出不同 host，RFC 3986 vs WHATWG 容錯差異）、畸形 payload（yourapp.com\@evil.com、evil.com\.yourapp.com、yourapp.com:@evil.com、#@evil.com）、Java 三 parser 不一致（java.net.URI 嚴格可能 host=null、java.net.URL 較寬鬆、Spring UriComponentsBuilder 又一套）、穩健三原則（正規化 \→/、拒絕 userinfo @、跳轉用「重建後字串」uc.toUriString() 而非原始 next，讓驗到的 host==跳出的 host）、Go net/url 陷阱（不自動正規化 \、// protocol-relative 解出 Host 無 Scheme、用 u.Hostname() 去 port、拒 u.User!=nil、回 u.String()）、簽章式 return URL 解動態外站（白名單列不完時改 HMAC 簽 to+sig，接 Day48；Java MessageDigest.isEqual／Go hmac.Equal constant-time 比較接 Day32；密鑰接 Day15；進階加過期與綁 session 防重放）、進階串接 Open Redirect→OAuth token 竊取（即使 provider redirect_uri exact match，應用內 Open Redirect 當合法網域跳板把 token 從 callback 二次跳轉帶到 evil.com，交叉 Day24）、偵測與 Code Review checklist（驗證/跳轉同一份解析、host 用 equals/map 非 endsWith/startsWith、callback 二次跳轉納白名單、白名單拒絕告警接 Day16、繞過 payload 當 CI 回歸測資、簽章驗證失敗率監控） | CRLF/Location header 注入交叉（與 Day34）、deep link/custom scheme 跳轉劫持、URL parser 模糊測試自動化、SSO 跨網域 return URL 信任邊界、meta refresh/JS 導頁的 javascript: 升 XSS |
| 68 | `content/資訊安全/Day68_Open_Redirect_CRLF_Location_Header_Injection.md` | Open Redirect × CRLF / Location Header Injection | 延伸（承 Day67，交叉 Day20 × Day34） | 不重講 Open Redirect 白名單入門（Day20/67）也不重講 CRLF response splitting 入門（Day34），聚焦交會點：跳轉值過了「網域白名單」但仍含 CR/LF 塞進 Location 標頭 → 注入新標頭/分裂回應（白名單管 host、控制字元把關管字串，兩道正交防線）、反例 host 驗過卻送原始 next（next=https://yourapp.com/welcome%0d%0aSet-Cookie:...）、現代框架防護與盲點（Tomcat setHeader/addHeader/sendRedirect 拒絕含控制字元標頭；Go net/http 用 replacer 把 \r\n 換空白而非拒絕；兩者盲點都在「工程師自己先解碼/手拼標頭/走非標準輸出」）、三種破口（先 URLDecoder/QueryUnescape 解碼再驗證再手拼 setHeader、把跳轉值再塞進自訂 X-* 標頭、meta refresh/JS location 跳出標頭消毒變成 XSS+javascript: scheme）、正解三道正交防線（A 網域白名單用重建字串承 Day67、B 解碼後 reject 控制字元 [\x00-\x1f\x7f] 不依賴框架、C sink 對號入座：流到標頭交框架寫入器不手拼／流到 HTML 用輸出編碼+scheme 白名單拒 javascript:/data:）、Java safeRedirectTarget（解碼一次+CTRL pattern+UriComponentsBuilder host/userInfo/重建）、Go safeRedirectTarget（query 已解碼一次不再多解防 double-encoding %250d%250a、ctrlChars regex、u.User/u.Hostname/u.String）、CRLF 家族 sink 地圖（Location=本篇×Day34、Host=Day46、Email/MIME=Day65、Log=Day16、HTML=Day02 XSS）、Code Review/CI 回歸測試 checklist（驗證後是否多解碼、解碼後 reject 控制字元、手拼標頭盤點、CRLF payload 斷言無分裂/無多標頭、控制字元 reject 告警接 Day16） | Reflected File Download（Content-Disposition sink，Day69）、double/multi encoding 正規化管線設計、response splitting 在 HTTP/2 的差異、proxy/gateway 層標頭正規化不一致、自訂標頭全端點盤點自動化 |
| 69 | `content/資訊安全/Day69_Reflected_File_Download.md` | Reflected File Download（RFD） | 入門（新主題，Content-Disposition sink，承 Day68 預告） | RFD 本質（攻擊者讓瀏覽器把「你的 API 回應」存成可執行檔且內容變成可執行指令，2014 Oren Hafif）、三個 AND 成立條件（1 反射 Reflection：回應含使用者可控字串，最常見 JSONP callback 或原封反射 query/path；2 檔名 Filename：無 Content-Disposition 時瀏覽器拿 URL 路徑末段當檔名，用 matrix 參數 ;/setup.bat 控制副檔名；3 執行 Execution：callback=cmd|'/c calc'|| 讓 body 變合法 batch 指令）、經典攻擊 URL /api/search;/setup.bat?q=x&callback=cmd|'/c calc'||、為何比 XSS 難防且好騙（網址列是可信網域、HTTPS 合法、繞過 SmartScreen/Gatekeeper 信任、檔案是你伺服器親手簽發、把使用者對網域的信任武器化，根因在後端）、Java 反例（Spring @GetMapping produces=application/javascript 原封反射 callback 無 Content-Disposition，路由層移除 ;matrix 仍打到 handler 但瀏覽器看到 setup.bat 的認知不一致）、Go 反例（net/http 反射 callback 無 Content-Disposition/nosniff）、三道防禦各打掉一條件（A 強制 Content-Disposition: attachment; filename="固定安全檔名" 打掉可控檔名，檔名含使用者輸入須白名單 [A-Za-z0-9._-] 去 ;/"/控制字元且副檔名自己決定；B X-Content-Type-Options: nosniff + 正確 Content-Type application/json 非 javascript/html 打掉型別誤判；C 下架/白名單化 JSONP 反射：SAFE_CALLBACK ^[A-Za-z0-9_.]{1,64}$ 打掉可執行內容，能改 CORS 就下架 callback）、輔助 Spring setRemoveSemicolonContent(true) 讓路由一致但無法改瀏覽器檔名（主力仍是防禦一）、Code Review/CI checklist（盤點 JSONP callback 端點、callback 白名單、可下載端點固定 attachment+安全檔名、檔名不吃使用者副檔名、全站 nosniff、Content-Type 正確、路由拒 ;或可執行副檔名結尾、CDN/gateway 與後端 path 解析一致、探測 URL 斷言帶 Content-Disposition+nosniff、可疑檔名 reject 告警接 Day16） | Content-Disposition filename/filename*（RFC 5987/6266）非 ASCII 檔名安全處理（Day70）、inline vs attachment 與預覽取捨、Content-Type↔副檔名白名單映射、http.ServeContent/ResponseEntity<Resource> 大檔串流下載、mime.FormatMediaType 標頭注入防護 |
| 70 | `content/資訊安全/Day70_Content_Disposition_Download_Hardening.md` | Content-Disposition 下載端點進階防禦 | 延伸（承 Day69） | 不重講 RFD 三條件與 Day69 入門防禦，聚焦「本來就要提供下載」的合法端點怎麼做對、合法下載端點三破口（標頭注入 filename 引號逸出/CRLF、非 ASCII 檔名亂拼、Content-Type 誤判 sniff 提權）、RFC 6266/5987 標頭語法（filename= 只能 Latin-1 quoted-string；filename*=UTF-8''percent-encoded 給非 ASCII；相容策略兩者並給現代瀏覽器優先 filename*）、Java Spring ContentDisposition.attachment().filename(name, UTF_8).build() 自動處理 filename* 與逸出（builder 只保證格式正確不保證語意安全）、Go mime.FormatMediaType 自動走 RFC 2231/5987 且不合法回空字串要判斷降級、Content-Type↔副檔名伺服器白名單映射（不依使用者副檔名/client header/不 sniff，白名單外降 application/octet-stream + nosniff、SVG/HTML 禁 inline）、inline vs attachment（預設 attachment、disposition-type 伺服器常數、可預覽型別隔離無 cookie 網域承 Day64）、sanitizeFilename 語意清洗（reject 控制字元承 Day68、去路徑成分承 Day11、去 "/;、限長、副檔名伺服器決定承 Day69）、大檔串流 Go http.ServeContent 自動處理 Range/Content-Length/If-Range 與 Java ResponseEntity<Resource>+FileSystemResource+contentLength 實際大小（不讀成 byte[]、Content-Length 一致避免邊界錯位呼應 Day23、授權在取檔前承 Day07）、Code Review/CI checklist 與惡意/非 ASCII 檔名+Range 回歸測試 | HTTP Range 請求 DoS/range amplification 與段數上限（Day71）、使用者內容網域隔離架構落地、magic bytes 決定可信型別（承 Day60）、簽章式下載連結與過期、CDN 下載快取與 Vary |
| 71 | `content/資訊安全/Day71_HTTP_Range_Request_DoS.md` | HTTP Range 請求安全與 DoS | 入門（新主題，承 Day70 串流下載） | Range 本身是攻擊面（承 Day70「把 Range 交給標準庫」）、Range 語意複習（bytes=、206 Partial Content、Content-Range、Accept-Ranges、多段 multipart/byteranges 每段 boundary+Content-Type+Content-Range 標頭 overhead）、range amplification 兩種放大分開看：資料放大（重疊 range 同段複製多次 sum>檔案大小，經典 CVE-2011-3192 Apache Killer bytes=0-,0-1,0-2... 把 100KB 放大成近 100MB 吃記憶體/CPU/頻寬）vs overhead/CPU 放大（大量微小不重疊 range bytes=0-0,2-2,4-4... 總和小躲過檢查但每段 MIME 標頭累積+上萬次字串組裝）、標準庫只擋一半：Go http.ServeContent 內部 sumRangesSize(ranges)>size 就 ranges=nil 回 200 整檔擋掉資料放大但不限段數、Tomcat/Spring ResourceHttpRequestHandler/ResourceRegion 依版本自行加固不可假設、防禦主線前置中介層/Filter 三規則（段數上限 ≤16 超標拒絕 416 或降級丟棄 Range 回 200、總量上限 sum>檔案大小丟棄、畸形 range 負值/start>end/超界回 416+Content-Range: bytes */size）、Go LimitRanges middleware 只數逗號提早短路+r.Header.Del("Range")降級+http.Server MaxHeaderBytes 16KB+ReadHeaderTimeout 擋巨型 Range 標頭、Java RangeLimitFilter OncePerRequestFilter countSegments+sendError 416 或 NoRangeRequestWrapper 隱藏 Range+server.max-http-request-header-size 16KB+connection-timeout、If-Range 一致性（ETag 相符 206 不符回 200 整新檔避免拼接損毀）與坑（ETag 不穩定/弱 ETag 讓 If-Range 頻繁失效把 206 退化成 200 整檔放大頻寬）、CDN/反向代理 Range 納 cache key/Vary 承 Day30/Day64、授權在取 range 前承 Day07、Code Review/CI checklist 與段數/重疊/巨型標頭/畸形/If-Range 回歸測試 | Slowloris/慢速 HTTP DoS 連線佔用型（Day72）、range 與 CDN cache key/Vary 深入、HTTP/2 flow control 與 range 交互、影片串流分塊授權、byte-serving 與簽章式下載連結交叉 |
| 72 | `content/資訊安全/Day72_Slowloris_Slow_HTTP_DoS.md` | Slowloris / 慢速 HTTP DoS（連線佔用型） | 入門（新主題，承 Day71 DoS 家族） | 與 Day71「放大型」DoS 相反的「連線佔用型」（少流量小封包，用慢速把有限連線槽/執行緒卡死，CPU 低流量小傳統告警抓不到）、請求生命週期拆解對應三種攻擊（收標頭階段=Slowloris 永不送標頭區結束空行改隔幾十秒補無關標頭續命；收 body 階段=Slow POST/R.U.D.Y. Content-Length 宣告很大 body 每幾秒送 1 byte；送回應階段=Slow Read 極小 TCP 接收視窗慢讀塞住 server 送出緩衝；keep-alive 閒置也佔槽）、thread-per-connection（Tomcat BIO）為何脆弱（一條慢連線佔一整個 worker thread，maxThreads 預設 200，幾百條 Slowloris 就耗盡執行緒池）vs 非阻塞式（Go net/http goroutine 輕量、Netty/Tomcat NIO event loop 少數 I/O 執行緒照顧上萬連線，對執行緒耗盡免疫但瓶頸換成連線數/fd/記憶體仍非免疫）、防禦主線給連線生命週期每段設 timeout+連線數上限、Go http.Server 四 timeout 欄位預設 0=永不逾時最危險必顯式設（ReadHeaderTimeout 短擋 Slowloris 為主力/ReadTimeout 涵蓋 body 擋 Slow POST 但誤傷慢上傳/WriteTimeout 擋 Slow Read/IdleTimeout 回收 keep-alive/MaxHeaderBytes 承 Day71）、Go per-handler http.TimeoutHandler 管 handler 執行+寫回應非讀 body、http.MaxBytesReader 限 body 大小擋 Content-Length 說謊、netutil.LimitListener 限連線總數（官方 x/net 擴充只限總數不分 IP）、Java 防線在連接器層非 filter 層（慢速攻擊在 servlet 前發生）、Tomcat NIO 讓 maxConnections≫maxThreads 較耐打、connection-timeout 主力踢慢連線/keep-alive-timeout/max-keep-alive-requests/max-connections/accept-count/maxSwallowSize 限中止請求吞 body 成本（-1 危險）、connectionTimeout 別為容忍慢網路調太長、應用層 timeout 不夠前面要 buffering 反向代理（Nginx client_header/body_timeout+send_timeout+limit_conn 每 IP+先 buffer 完整請求天生免疫慢速把攻擊擋在便宜前層打不到 origin）、監控承 Day16 盯連線數逼近上限/connectionTimeout 中止數/單 IP 連線數（CPU/流量告警抓不到）、slowhttptest -H/-B/-X 三模式測試+斷言慢連線被主動關閉+慢上傳不誤殺、Code Review/CI checklist | HTTP/2 特有 DoS（Rapid Reset CVE-2023-44487/HPACK bomb/SETTINGS·PING·CONTINUATION flood，Day73）、非同步 servlet 與 long polling 慢速防護、mTLS/TLS handshake DoS、L4/L7 連線速率限制與 SYN flood、每 IP 連線數精細控制與 fail2ban |
| 73 | `content/資訊安全/Day73_HTTP2_Protocol_DoS.md` | HTTP/2 協定層 DoS | 延伸（承 Day71/72 DoS 家族，交叉 Day60/Day16/Day18） | 不重講 DoS 基本觀念（放大型/連線佔用型/為何設 timeout 看 Day71/72），聚焦 HTTP/2 協定機制被武器化、心智模型（多工 multiplexing/stream 狀態機/HPACK 壓縮+flow control 兩級視窗，資源計量單位從「連線」變「stream/frame/解壓後標頭」故舊防禦全要補新層：限連線數擋不住單連線內 stream 濫用）、Rapid Reset（CVE-2023-44487，Go 專屬 CVE-2023-39325）本篇主角（開 HEADERS 立刻 RST_STREAM 取消高速循環，被 reset 的 stream 不計入當前併發數故繞過 MAX_CONCURRENT_STREAMS，很多實作收 HEADERS 就 dispatch 給 worker，RST 取消的是回應不是工作→成本不對稱放大，與 Slowloris「慢」恰好相反是「快」）、Go 緩解（升級 runtime 為第一優先 Go≥1.21.3/1.20.10 內建限制「同時處理中 stream 數」非只限協定層併發、http2.ConfigureServer 顯式設 MaxConcurrentStreams 非越大越好/MaxReadFrameSize/IdleTimeout/MaxUploadBufferPerConnection·PerStream、HTTP/1.1 四 timeout 承 Day72 照設）、Tomcat 緩解 overhead 記帳（overheadCountFactor 別關=0，浪費型 frame 增 count 有進展 frame 降 count 超門檻斷線，maxConcurrentStreams vs maxConcurrentStreamExecution 後者限真正吃 worker 數承 Day72 執行緒池、maxHeaderSize/maxHeaderCount、Spring Boot 走 TomcatConnectorCustomizer 非 properties key、屬性隨版本演進最重要仍是升級）、HPACK bomb（呼應 Day60 zip bomb/Day13 Billion Laughs 只要有解壓就有放大，動態表塞長值短參照重複引用解壓放大幾十 MB，防禦 MaxDecoderHeaderTableSize/MaxHeaderListSize/maxHeaderSize 解壓過程即累計超標中止，與 Day71 MaxHeaderBytes 同哲學但管「解壓後」大小）、control-frame flood（SETTINGS/PING 逼回 ACK、WINDOW_UPDATE/空 frame flood、CONTINUATION flood 2024 標頭組裝完成前不設上限持續配置記憶體是 HPACK bomb 近親，防禦靠升級+Tomcat overhead*Threshold+HTTP/2 感知代理）、實務決策清單（升級升級升級投報率最高、顯式設上限別吃預設別設無限、HTTP/1.1 timeout 疊加非取代、前擺 HTTP/2 感知 buffering 代理/托管 LB、監控盯 RST_STREAM 速率/單連線 stream 建立取消次數/已 reset 仍執行 stream 數而非 CPU 流量承 Day16）、Code Review/維運 checklist、h2load/h2spec 測試斷言 overhead 門檻斷線+解壓過程即拒 | mTLS/TLS handshake DoS 運算不對稱（Day74）、HTTP/3 QUIC 特有攻擊面、gRPC over HTTP/2 stream 濫用交叉 Day51、CDN/邊緣層 HTTP/2 緩解落地、每 IP stream 速率限制精細控制 |
| 74 | `content/資訊安全/Day74_TLS_Handshake_mTLS_DoS.md` | mTLS / TLS 握手 DoS | 延伸（承 Day19 TLS × Day72/73 DoS 家族，交叉 Day51/Day16/Day15/Day10/Day39） | 不重講 Day19 TLS 基礎（TLS 是什麼/憑證怎麼驗/cipher 選擇/敏感資料保護），聚焦握手階段「運算不對稱」型 DoS（client 發起廉價 vs server 私鑰非對稱運算 RSA 解密/ECDHE 簽章 + mTLS 憑證鏈驗證昂貴，與 Day73 Rapid Reset「處理成本不對稱」是表兄弟，流量/CPU 未必爆在習慣告警點承 Day16）、心智模型（full handshake 一定要一次私鑰運算/resumption 走對稱式省掉非對稱運算/mTLS 多背 pre-auth 憑證鏈驗證成本；運算不對稱對照表）、重協商 DoS（THC-SSL-DOS/CVE-2011-1473，一條連線逼無限重跑握手，緩解優先序：①上 TLS 1.3 從協定移除 renegotiation 根除、②Go crypto/tls server 端天生不支援 renegotiation 免疫 Renegotiation 欄位只 client 端有意義、③Java JSSE 用 -Djdk.tls.rejectClientInitiatedRenegotiation=true 須 JSSE 初始化前生效）、握手洪水（大量新連線各走 full handshake，靠 resumption 提升省 non-asymmetric 比例但 session ticket 金鑰要輪替傷 forward secrecy 承 Day15、每 IP 新連線/握手速率限制放 L4/前置代理非應用層 Day17、握手 timeout 別讓握手無限拖=TLS 版 Slowloris）、mTLS 憑證鏈驗證放大本篇主角（放大點一長/深鏈逐張驗簽→限深度、放大點二 pre-auth 未授權者也逼你付驗證成本、放大點三握手路徑同步查 OCSP/CRL=每次握手一個對外呼叫交叉 Day10/Day39 SSRF→改 OCSP stapling/短效憑證；心態：mTLS 是授權手段非握手防 DoS 護身符反讓每次握手更貴，終結放前置閘道/mesh sidecar 承 Day51）、Go 控制（tls.Config MinVersion TLS1.3/ClientAuth RequireAndVerifyClientCert/ClientCAs/VerifyPeerCertificate 限鏈長但跑在標準驗證之後是縱深非省成本/SetSessionTicketKeys 輪替；http.Server 無獨立握手 timeout 欄位靠 ReadHeaderTimeout/ReadTimeout/WriteTimeout 最小正值當握手期限承 Day72，raw tls.Conn 用 SetDeadline；netutil.LimitListener 限總連線數只限總數不分 IP）、Java 控制（-Djdk.tls.rejectClientInitiatedRenegotiation=true；Tomcat SSLHostConfig protocols=TLSv1.3/certificateVerification=required/certificateVerificationDepth 壓低鏈深/sessionCacheSize·sessionTimeout resumption/connectionTimeout 涵蓋握手階段踢慢握手承 Day72/maxConnections≫maxThreads NIO；OCSP stapling 屬性依賴 OpenSSL tc-native；raw SSLEngine 無內建握手 timeout 要 Netty SslHandler.setHandshakeTimeoutMillis）、實務決策清單（收斂 TLS 1.3、握手 timeout、resumption+輪替金鑰、mTLS 限深+避免同步撤銷、握手洪水擋便宜前層+TLS 卸載給硬體加速 LB、監控握手層指標新握手速率/full vs resumption 比例/握手失敗率/TLS CPU 佔比/renegotiation 嘗試承 Day16）、Code Review/維運 checklist、testssl.sh/thc-ssl-dos/慢握手/長鏈/resumption 回歸測試 | TLS 憑證驗證失誤 client 端 MITM InsecureSkipVerify/TrustManager（Day75）、HTTP/3 QUIC 握手與 UDP 放大/0-RTT replay、session ticket 金鑰輪替與 forward secrecy 落地、憑證釘選 pinning 取捨、post-quantum TLS 握手成本 |
| 75 | `content/資訊安全/Day75_TLS_Certificate_Verification_MITM.md` | TLS 憑證驗證失誤與 MITM（後端作為 TLS client） | 延伸（承 Day19 TLS × Day74 mTLS，交叉 Day10/Day39/Day26/Day16/Day15） | 不重講 Day19 TLS 基礎與 Day74 server 端握手，聚焦後端「主動對外發 TLS 請求」時 client 端驗證破口、核心心智模型：TLS client 驗證＝①鏈驗證 AND ②主機名驗證兩件獨立事少一件身分保證歸零（只做①不做②→攻擊者用自己合法網域憑證冒充目標；①②都不做→任何自簽憑證即可 MITM）、「本機測得過」陷阱動機學（自簽/內部 CA 憑證→握手噴 unable to find valid certification path / certificate signed by unknown authority→時間壓力下關驗證→關掉的正是會噴錯那道防線→安靜地永遠成功含跟攻擊者通話）、Go 反例 InsecureSkipVerify=true（①②一起關的總開關）vs 正解 x509.SystemCertPool 疊加 AppendCertsFromPEM 內部 CA 設 RootCAs 只多信任不關驗證/ServerName 留空由 http.Transport 依 URL host 自動帶入做②別手動寫死/用專屬 http.Client 非全域改 DefaultTransport、進階 VerifyConnection 需自己把①鏈驗證 x509.VerifyOptions{Roots,Intermediates,DNSName:cs.ServerName}＋②主機名都做回來、Java 反例全信任 X509TrustManager（checkServerTrusted 空實作關①）＋HostnameVerifier return true/setDefaultHostnameVerifier（關②）vs 正解 KeyStore 載內部 CA truststore＋TrustManagerFactory.getDefaultAlgorithm 走標準 PKIX＋SSLContext.init(null,tmf,null)＋Java 11+ HttpClient 預設做 HTTPS 端點識別別關、raw SSLSocket/SSLEngine 預設不做主機名驗證要顯式 SSLParameters.setEndpointIdentificationAlgorithm("HTTPS")經典沉默陷阱、pinning 取捨（釘 SPKI 公鑰雜湊非整張憑證讓換發沿用同金鑰 pin 不失效、必有 backup pin 否則輪替把自己鎖死＝HPKP 廢棄主因、backend-to-backend 比瀏覽器可行因掌握兩端與輪替節奏、疊加在①②之上非取代不可 InsecureSkipVerify＋只 pin）、交叉 Day10 SSRF/Day39 DNS rebinding（關驗證放大 SSRF：TLS 本是最後一道「連到誰」身分裁決；rebind＋關驗證絕配，正確驗憑證是免費一道縱深）、常見誤區表（加密≠驗身分/內網也要驗/pin 不能取代①②/只關②也致命/測試環境關驗證會被複製到生產/raw SSLSocket 預設不驗主機名/別全域塞 DefaultTransport）、Code Review grep（InsecureSkipVerify:true、空 checkServerTrusted、HostnameVerifier return true、setEndpointIdentificationAlgorithm(null)）與測試（自簽 server 斷言握手失敗＝守門員、主機名不符斷言失敗、內部 CA 正例斷言成功、CI grep gate 承 Day18、驗證失敗大聲失敗＋告警承 Day16） | 憑證釘選 pinning 落地實作（Go VerifyConnection 比對 RawSubjectPublicKeyInfo SHA-256、Java 委派式 TrustManager PKIX 後加 pin、backup pin 與輪替流程、把自己鎖死營運風險）Day76、憑證/私鑰生命週期與到期停機/ACME 自動換發、Certificate Transparency 憑證誤發偵測、複合/委派 TrustManager 同時信任公開＋內部 CA、mTLS client 端金鑰保管 |
| 76 | `content/資訊安全/Day76_Certificate_Pinning_Implementation.md` | 憑證釘選（Certificate Pinning）落地實作 | 延伸（承 Day75，交叉 Day19/Day15/Day16/Day32/Day18） | 不重講 Day75 憑證驗證基礎（①鏈驗證 AND ②主機名驗證/InsecureSkipVerify 災難/內部 CA truststore）也不重講 Day75 第五節 pinning 取捨論述（為何釘 SPKI/為何要 backup pin/HPKP 廢棄/backend-to-backend 可行），聚焦實作與營運、pin 定義（pin＝SHA-256(SubjectPublicKeyInfo DER)再 base64＝RFC 7469 pin-sha256 格式；SPKI 結構 algorithm+subjectPublicKey 不含序號/效期/簽發者/簽章故換發沿用同金鑰 pin 不變）、openssl 三種算法（A s_client -connect+-servername SNI 帶對否則抓到別的 vhost｜B x509 -pubkey -noout 從憑證檔算供 CI｜C pkey -in backup.key -pubout 從「還沒簽成憑證的私鑰」算＝backup pin 可行的支點，pin 整張憑證做不到）、三個實作前決策（pin leaf 最窄適合自家掌握 vs pin intermediate 對端 leaf 換發不影響適合第三方 vs pin root≈自訂 truststore 用 Day75 做法即可別拿 pinning 做；pin set 至少 2 個；匹配規則鏈上任一張命中＝HPKP 語意但信任面等於 pin set 裡最寬那個）、鐵律「疊加不是取代」判準只問「內建驗證還在跑嗎」（只 pin 不驗鏈＝過期/被撤銷憑證照收，pin 回答「是不是這把金鑰」①②回答「憑證是否有效/是否簽給這 host」是三個不同問題）、Go 正解不碰 InsecureSkipVerify 只掛 VerifyPeerCertificate（標準驗證之後才呼叫、verifiedChains 為空＝InsecureSkipVerify 被打開必須拒絕當防呆、比對 verifiedChains 非 PeerCertificates 因後者是「對方說的」、x509.Certificate.RawSubjectPublicKeyInfo 直接給 DER 不用 MarshalPKIXPublicKey 繞、VerifyConnection 亦可但要對 VerifiedChains 比、pin 是公開資訊不需 constant-time 比較與 Day32 區隔、專屬 client 非 DefaultTransport）＋printPins 觀測小工具（tls.Dial 帶 ServerName 印鏈上每張 pin 與 NotAfter）、Java 正解委派式 X509ExtendedTrustManager（checkServerTrusted 第一行 delegate.checkServerTrusted 再 checkPins 讓「被註解掉」最顯眼、六個多載全委派、getPublicKey().getEncoded() 對 X.509 就是 SPKI DER、建構子對 pins.size()<2 防呆丟例外、MessageDigest 非 thread-safe、TrustManagerFactory.init((KeyStore)null) 取 JDK 預設 delegate 再包）、坑：實作陽春 X509TrustManager 而非 X509ExtendedTrustManager 會讓②端點識別落入 JSSE wrapper 包裝細節模糊地帶（JDK 會包 AbstractTrustManagerWrapper 補做但屬實作細節隨版本演進不該賭）、Java 1.8 無 java.net.http.HttpClient 改 HttpsURLConnection.setSSLSocketFactory 且 raw SSLSocket 仍要 setEndpointIdentificationAlgorithm("HTTPS")（pinning 不補②）、真正難點輪替 SOP 順序性（pin set 先擴張{A,B}→{A,B,C}→等所有 client 部署完→server 才換憑證 A→B→最後才收縮{B,C}；反過來做則 server「一秒生效」vs client「一週部署」時間差＝停機時間；加 pin 可慢慢來超集合不壞事、移除 pin 與換憑證必須等 client 就緒）、SOP 落地三要求（pin set 放組態可不重新編譯就更新因緊急換金鑰沒有幾天但寫入權限＝解除 pinning 權限要 audit＋告警承 Day15/16、空 pin set 是「設定壞了」非「不檢查」、report-only 模式導入與每次變更先觀察不命中率但 verifiedChains 為空的檢查永不受 report-only 影響、kill switch 兩難：只降級 pinning 一層①②永遠強制最壞退回一般 HTTPS 非 InsecureSkipVerify＋雙人審核）、實務衝突 ACME 自動換發預設換金鑰＝pin 每 60 天自動失效（三條路 A key reuse 如 certbot --reuse-key 但副作用是私鑰永不換要自排輪替、B 改 pin intermediate、C pin 更新自動化＝SOP 自動化版；CDN/托管 LB 後面的第三方對端憑證非其自有別 pin leaf 或根本別 pin）、常見誤區表（pin 了就可 InsecureSkipVerify/只放一個 pin/先換憑證再更新 pin/硬編 pin 讓自己救不了/pin set 當普通設定/空 pin set 當未啟用/以為要 constant-time/ACME 一勞永逸/pin 第三方 leaf/pin 不命中≠一定 MITM 更常是對端換金鑰要能區分營運事故與資安事件/Java 用陽春 X509TrustManager/pin root≈truststore）、Code Review checklist 分四區（程式碼疊加性/pin set 不鎖死/輪替 SOP 順序/監控）與測試（pin 命中正例、pin 不命中反例＝合法 CA 簽+主機名正確但別把金鑰須失敗＝模擬 CA 誤發＝pinning 存在證明、backup pin 測試證明救得了自己、疊加性測試 pin 命中但憑證過期/主機名不符/不受信任 CA 須失敗＝專抓「寫成取代」、CI pin 一致性檢查對測試環境跑 openssl 管線斷言在 pin set 裡、憑證 NotAfter 到期預警） | Certificate Transparency CT log 與憑證誤發偵測／CAA record 事前預防（Day77）、憑證/私鑰生命週期與 ACME 自動換發架構、複合/委派 TrustManager 同時信任公開＋內部 CA、mTLS server 端 pin client 憑證、pin set 組態發佈管線與雙人審核落地、行動端 pinning 與後端策略差異 |
| 77 | `content/資訊安全/Day77_Certificate_Transparency_CAA.md` | Certificate Transparency（CT）與憑證誤發偵測／CAA | 入門（新主題，承 Day76 預告，交叉 Day75/Day76/Day16/Day18/Day15/Day10/Day72/Day19） | 不重講 Day75 憑證驗證（①②）與 Day76 pinning 實作，視角從「TLS client 要不要信對方」換成「網域擁有者怎麼知道有誰在冒用我」、問題根源 CA 是 PKI 單點失效（信任幾十上百家 CA，任一家都能簽出你網域的合法憑證且 Day75 ①②完整通過；DigiNotar 2011 簽出 500+ 張假 *.google.com 用於伊朗 MITM 後被移除信任並破產、Symantec 2015-2017 誤發最終被 Chrome/Firefox 分階段撤銷根信任而推動力正是 CT 讓大家看得到）、pinning vs CT 五維對照表（誰做：client vs 網域擁有者｜時機：握手當下 vs 事後分鐘小時級｜效果：阻擋 vs 知道｜代價：綁死輪替節奏 vs 幾乎為零｜涵蓋：只有你控制的 client vs 全世界所有人看到的憑證＝關鍵差異因攻擊者拿假憑證 MITM 你的「使用者」時 pinning 完全幫不上）、CT 機制 RFC 6962 核心「不阻止誤發只讓誤發藏不住」、三方角色（Log＝append-only Merkle Tree 靠密碼學非承諾任何人可用 Merkle proof 驗證歷史沒被改／Monitor＝你要扮演的角色定期拉找自己網域／Auditor＝驗證 log 沒食言由瀏覽器與研究者做後端一般不需自己扮演）、SCT＝log 給 CA 的收據（承諾 MMD 24h 內公開、由 log 私鑰簽名，執行力來源不是規定 CA 要 log 而是「沒 SCT 的憑證 Chrome/Apple 不收」）、SCT 三種送達方式（A 嵌在憑證裡 X.509 extension OID 1.3.6.1.4.1.11129.2.4.2 最常見 server 零設定｜B TLS 握手擴充｜C OCSP stapling 夾帶）、precertificate 解雞蛋問題（poison extension OID 1.3.6.1.4.1.11129.2.4.3 critical 且無 client 認得故必被 reject＝進得了 log 卻永遠不能當憑證用；副作用同一張憑證在 log 裡兩筆 precert+正式憑證 → monitor 必須用 serial+issuer 去重否則雜訊乘二）、後端結論 SCT 是 CA 與瀏覽器之間的事你什麼都不用做只要當 monitor、核心對照 CT 事後偵測 vs CAA 事前預防（CAA RFC 8659 DNS 記錄 flag 0/128 critical、issue 授權一般憑證、issuewild 只要存在就完全接管萬用字元授權讓 issue 對萬用字元失效、iodef 違規通報管道、值 ";" 代表不授權任何 CA；執行力來自 CA/B Forum 2017-09 起強制所有公開信任 CA 簽發前檢查 CAA 否則違反 BR 有被踢出根計畫風險；dig CAA example.com +short；樹狀爬升 api.staging.example.com→staging.example.com→example.com 取第一個找得到的層級就停 → 只要 apex 設一組整棵子網域樹受保護，但子網域自己設會蓋掉 apex＝最常見自我 DoS 讓該子網域另一家 CA 自動換發全簽不出來）、兩者界線（路徑 A 向合法 CA 騙簽→CAA 擋得住；路徑 B CA 被入侵/內部作惡 DigiNotar 模式→CAA 完全無效因被入侵的 CA 不會為難自己，但憑證仍必須進 CT log 否則瀏覽器不收→CT 是唯一防線；CAA 靠「CA 願意遵守規則」防君子、CT 靠「憑證想被接受就必須公開」防小人；CAA 是門鎖 CT 是監視器沒人裝了鎖就拆監視器）、資料來源選擇（自建 get-entries monitor 每天數百萬筆是大工程絕大多數團隊不該做；crt.sh JSON 非官方介面快速起步但免費公共服務無 SLA 會限速格式會變；Cert Spotter API 有正式文件免費額度+付費；商用 CT 監控/CA 附帶）、Go 實作 fetchCrtSh（?q=%25.<domain>&output=json 用 url.Values 編碼不手拼承 Day67/68、User-Agent 表明身分、io.LimitReader 32MB 上限承 Day71、http.Client Timeout 承 Day72、非 200 與 JSON 解析失敗都往上丟當事件不能靜默＝monitor 瞎了、struct 只映射用得到欄位因非官方介面多映射一欄多一個會壞的地方）、Audit 三步核心（1 serial+issuer 去重 2 第一關便宜 issuer 片段包含比對 99% 自家 ACME 換發在此過濾 3 第一關沒過才付貴的成本抓 PEM ?d=<id> 算 SPKI）、issuer 用片段而非完整字串比對（CA 會換中繼 R3→R10→R11 綁死完整字串＝每次換中繼一輪假警報兩次後大家開始無視）、SPKI 分流把營運事故與資安事件分開（SPKI 已知＝換 CA/CA 換中繼沒更新組態＝營運事故；未知 issuer+未知金鑰＝疑似誤發＝資安事件立即升級；抓不到 PEM 不能當沒事要當「查不出來」報出去）、spkiPinFromCert 用與 Day76 完全相同的 RawSubjectPublicKeyInfo 計算故 pin set 組態可兩套機制共用、Java 實作 CtMonitor（java.net.http.HttpClient connectTimeout 承 Day72、followRedirects NEVER 承 Day10、非 200 丟例外絕不 catch 掉當今天沒事、CertificateFactory 從 PEM 算 getPublicKey().getEncoded() SPKI 承 Day76、ScheduledExecutorService/Spring @Scheduled 每小時、成功也要記 ct_monitor_success 指標）、Java 1.8 提醒（無 java.net.http.HttpClient 改 HttpsURLConnection 但別關憑證驗證承 Day75 否則查誤發卻自己 InsecureSkipVerify 太諷刺、record 是 16+）、CI CAA 斷言 script（dig +short CAA、雙向檢查：預期的 CA 都在＋沒有預期外的 CA 被授權因只檢查前者則別人偷加一行 issue 你不會知道而那正是把鎖打開的方法、去掉 RFC 8657 參數 accounturi/validationmethods、iodef 存在檢查、承 Day18 讓它在 CI 紅不要在生產紅、DNS 記錄很容易在某次調整 DNS 時被誤刪）、雜訊治理是成敗關鍵（naive 有新憑證就告警第一週就會被關掉）、五個雜訊來源與處理（自家 ACME 換發量最大→issuer 白名單+SPKI｜precert+正式憑證雙筆→serial+issuer 去重否則乘二｜子網域萬用憑證→白名單以 issuer+SPKI 為主別用網域字串精確比對因萬用憑證 CN 跟子網域清單本來就對不起來｜CDN/托管服務代簽用自己帳號幫你簽 issuer 他們選節奏你管不到→把服務商的 CA 加白名單，正是 Day76「CDN 後面別 pin」的同一現象｜內部 CA 不在 CT 裡→這是盲區不是雜訊）、最重要盲區 CT 看不到內部 CA（CT 執行力來自瀏覽器不收沒 SCT 的憑證而內部 CA 本就不在根計畫裡此壓力不存在→內部 PKI 完全沒有 CT 保護、內部 CA 被入侵沒有公開帳本記錄、對應解法是 Day76 pinning＋內部 CA 簽發稽核日誌承 Day16、別把「CT 沒告警」讀成「沒人冒充我」）、CT×pinning 分工矩陣（對外服務公開 CA：事前 CAA／事後 CT／阻擋 pinning 若可行；內部服務內部 CA：事前簽發流程管控／事後 CT 看不到＝盲區／阻擋 pinning 是主場）、偵測型控制通病「monitor 掛了與沒有誤發在儀表板上長得一模一樣」（ct_monitor_error 要指標告警、ct_monitor_success 也要指標並設「N 小時內沒成功執行」告警因「沒有成功」跟「有失敗」是兩件事 process 沒被排到連 error 都不會有、定期陽性測試對已知會命中目標跑斷言真的會叫＝monitor 的存在證明等同 Day76「pin 不命中反例是 pinning 存在證明」）、常見誤區表（CT 不阻止任何事是監視器不是門鎖/CAA 只約束守規矩 CA 被入侵時無效/兩者非二選一擋不同路徑/pinning 做了仍需 CT 因保護不到用瀏覽器的使用者/CT 沒告警≠沒人冒充因內部 CA 盲區/有新憑證就告警會被關掉雜訊治理是主線/crt.sh 是別人免費服務要 timeout+限速+失敗告警/issuer 完整字串比對每次換中繼假警報/查詢失敗 skip 掉等於瞎了/CT log 一筆一張憑證是錯的 precert 各一筆/SCT 不用 server 端設定/每個子網域都設 CAA 會蓋掉 apex 自我 DoS/發現誤發撤銷掉就沒事＝Day78 主題）、Code Review checklist 四區（CAA 事前預防/CT monitor 事後偵測/告警品質承 Day16/範圍認知）與測試（陽性測試＝monitor 存在證明、去重測試同 serial precert+正式憑證只產生一筆、issuer 片段比對測試 R3→R11 不告警、SPKI 分流測試未知 issuer+已知 SPKI 歸類營運事故、失敗路徑測試 429/非 JSON 要丟錯告警而非回空清單假裝沒事、CI CAA 斷言排程） | 憑證撤銷 CRL/OCSP soft-fail/OCSP stapling/短效憑證（Day78）、自建 CT monitor 對接 RFC 6962 get-entries、CT v2（RFC 9162）與 SCT 驗證細節、內部 PKI 簽發稽核與私有 CA 治理、CAA accounturi/validationmethods（RFC 8657）綁定 ACME 帳號、iodef 報告處理流程 |
| 78 | `content/資訊安全/Day78_Certificate_Revocation_OCSP_Soft_Fail.md` | 憑證撤銷（Revocation）：CRL / OCSP / soft-fail / 短效憑證 | 入門（新主題，承 Day77 預告，交叉 Day75/Day76/Day74/Day10/Day16/Day15/Day18/Day19） | 不重講 Day75 ①②驗證、Day76 pinning 實作、Day77 CT/CAA，聚焦「憑證本來有效但我後悔了」這第三個獨立問題、根因（憑證＝CA 簽的一句離線就能驗的話，離線可驗是 PKI 撐起網際網路的優點但代價是無法收回已說出口的話只能旁邊貼公告然後祈禱有人看；撤銷的核心問題只有一個＝怎麼讓離線驗證流程額外查一個線上狀態、而且查不到怎麼辦）、第一代 CRL（RFC 5280、CRLDistributionPoints、openssl x509 -text 看發佈點；死法一體積全量與待驗憑證無關為 20 bytes 答案下載 5MB 數十萬筆序號 Heartbleed 2014 讓 CRL 一夜暴漲反促使跳過、死法二 nextUpdate 更新週期數小時到七天＝撤銷生效可能等一週但撤銷場景恰好分秒必爭、死法三 抓 CRL 本身是對外呼叫會失敗於是又 soft-fail；內部 PKI 規模小仍可行）、第二代 OCSP（RFC 6960、AIA 欄位、openssl x509 -ocsp_uri / openssl ocsp -issuer -cert -url、good/revoked/unknown 三態、體積 MB→數百 bytes；問題一每次握手一次對外呼叫＝延遲疊加＋可用性反轉 CA responder 掛你就連不上對方 2016 GlobalSign 誤設造成全球假撤銷＝撤銷機制自己成故障來源、問題二 Day74 握手 DoS 放大點每握手一個對外呼叫＝你變成別人 DoS 上游 CA 的放大器然後被限速然後握手全掛、問題三 Day10 SSRF 教科書結構＝收對方遞來的憑證→讀出憑證裡的 URL→主動連過去（實務被 AIA 查詢時機與標準庫限制收住但心智模型要正確：開撤銷檢查＝驗證路徑多一個連憑證裡寫的 URL 的動作）、問題四隱私每查一次等於告訴 CA 你在連誰＝Let's Encrypt 關掉 OCSP 主因之一）、soft-fail 是壓垮一切的核心（hard-fail＝responder 抖動/咖啡廳 Wi-Fi/公司 proxy 擋 OCSP 就全站打不開使用者怪你不怪 CA；soft-fail＝一切正常沒人抱怨；所有主流實作都選 soft-fail；殺手級推論＝你要防的是攻擊者拿被撤銷憑證 MITM，但能 MITM 的攻擊者本來就在網路路徑上，在路徑上就能把 OCSP 查詢丟掉（不回應/TCP reset/DNS 給錯答案）→查不到→soft-fail→放行 ⇒ soft-fail 的撤銷檢查恰好對唯一會用到它的那種攻擊者完全無效＝Adam Langley「只在你不需要時才有用的安全帶」；不是實作 bug 是邏輯死結；也不能改 hard-fail 因為那把 CA 每次抖動變成你的停機 ⇒ 卡十幾年沒解直到大家決定繞過）、OCSP stapling（RFC 6066 Certificate Status Request、由 server 自己去問把 CA 簽名回應夾在握手裡；一次解掉延遲/CA 負載每 server 每幾小時/隱私；回應是 CA 私鑰簽的 server 無法偽造、thisUpdate/nextUpdate 讓 server 無法重播舊 good；Day77 SCT 送達方式 C 的細節在此收；但沒解 soft-fail＝stapling 是 server 自願夾帶的，攻擊者只要單純不夾 client 就退回無撤銷資訊→soft-fail→放行＝攻擊者什麼都不用做；補洞只有 Must-Staple RFC 7633 TLS Feature 擴充把 soft-fail 對這張憑證變 hard-fail，技術上正確但把「stapling 設定壞掉」升級成「全站掛掉」故幾乎沒人用，Let's Encrypt 2025 已停止支援）、Go 實作（crypto/tls 預設完全不做撤銷檢查一行都沒有＝設計選擇非 bug 因 soft-fail 沒安全價值就不假裝有；x509.Certificate.Verify 只做 Day75 ①②無撤銷語意；stapled 回應放在 tls.ConnectionState.OCSPResponse 理不理是你的事；Day76 讀者必踩坑＝VerifyPeerCertificate 簽名只有 rawCerts/verifiedChains 拿不到 OCSPResponse 必須改用 VerifyConnection 收整個 ConnectionState；checkStapledOCSP 五要點＝VerifiedChains 為空當 InsecureSkipVerify 防呆拒絕承 Day76／沒夾時放行是刻意選擇要寫註解／鏈長<2 無 issuer 無法驗簽／ocsp.ParseResponseForCert 而非 ParseResponse 因前者驗簽＋確認回應講的是這張憑證防止拿別張 good 搪塞／自己比 ThisUpdate·NextUpdate 因 x/crypto/ocsp 不判時效否則三年前 good 可無限重播；Revoked 要告警承 Day16；疊在 Day76 checkPins 旁邊、專屬 client 不動 DefaultTransport、Timeout 承 Day72；主動查 OCSP 用 ocsp.CreateRequest 可行但答案幾乎都是別做）、Java 實作（三開關：-Dcom.sun.net.ssl.checkRevocation 預設 false＋-Djdk.tls.client.enableStatusRequestExtension JDK9 起預設 true ⇒ 標準情境是你已經在要 stapled 回應然後把它丟掉；兩者要一起才有意義前者要後者檢查；PKIXRevocationChecker Java 8 起提供才是精細控制的正解＝CertPathValidator.getInstance("PKIX").getRevocationChecker()＋setOptions＋PKIXBuilderParameters.setRevocationEnabled(true)（沒這行 addCertPathChecker 不生效）＋addCertPathChecker＋TrustManagerFactory.getInstance("PKIX").init(new CertPathTrustManagerParameters(params))；四 Option 逐一＝SOFT_FAIL 不加是 hard-fail 加了查不到就放行例外被吞進 getSoftFailExceptions()／NO_FALLBACK 建議加否則 OCSP 失敗退回抓 CRL＝一次握手兩種對外呼叫延遲與失敗面加倍／ONLY_END_ENTITY 建議加否則鏈多深就多幾個呼叫承 Day74／PREFER_CRLS 一般不加內部 PKI 可考慮；SOFT_FAIL 是 Java PKI API 最誠實也最陷阱的設計＝誠實在強迫你自己選且名字就叫軟性失敗，陷阱在不加＝CA 抖動害你停機壓測沒問題上線第三週凌晨全掛／加了＝回到死結還多一個架構圖上寫「已啟用撤銷檢查」的假安心比不做更糟；真加了至少把 getSoftFailExceptions() 撈出來 log.warn＋metrics 計數承 Day16 否則連「撤銷檢查從來沒成功過」都不知道＝Day77 偵測器自己壞掉；Java 1.8 有 PKIXRevocationChecker 但無 java.net.http.HttpClient 要走 HttpsURLConnection.setSSLSocketFactory 且 raw SSLSocket 仍要 setEndpointIdentificationAlgorithm("HTTPS") 因撤銷檢查補不了②）、瀏覽器早已放棄 OCSP 改推播（Chrome CRLSets＝Google 彙整「重要的」撤銷經瀏覽器更新機制推送但非全量一般網站憑證撤銷不在裡面／Firefox CRLite＝Bloom filter 串把全量壓成幾百 KB 週期推送技術上漂亮涵蓋完整；解掉死結因握手時零對外呼叫＝不能被擋無延遲無隱私問題答案已在本機；但後端抄不了需要彙整全球 CT/CRL 並推給幾億 client 的基礎設施，沒有人幫你的 Java 服務維護 CRLite；認知校正＝「瀏覽器有在檢查撤銷」與「你的後端有在檢查撤銷」是完全不同兩件事且前者方法你抄不了）、產業答案＝不修撤銷改讓憑證活得夠短（邏輯：撤銷是問題因為剩餘效期長，只剩 3 天則撤銷與等它過期差別就 3 天；而「過期」不需線上查詢不會被擋不會 soft-fail＝寫在憑證裡是 Day75 ①的一部分離線就驗得掉＝繞回第一節「離線可驗」的優點當解法；已發生的事實：Let's Encrypt 2025-08-06 OCSP 服務正式終止非降級非棄用警告是關掉且 2025-05 起新簽憑證不再含 OCSP URL／6 天短效憑證 shortlived profile 首張 2025-02 簽出 2026-01-15 一般可用實務建議每 2.5 天換發／CA/B Forum Ballot SC-081v3 2025-04-11 通過排定 2026-03-15 起最長 200 天（今天已生效）→2027-03-15 100 天→2029-03-15 47 天，網域驗證資料重用期同步收到 2029 剩 10 天；對後端真正意義＝責任轉移非輕鬆＝撤銷從「你依賴 CA 提供但其實不管用的機制」變成「你自己的換發管線是否可靠」＝不用煩惱 OCSP 但 ACME 管線從此是生產基礎設施掛三天你就掛；2026-05-08 Let's Encrypt 簽發中斷是預演＝效期越短對管線中斷容忍窗口越小 6 天憑證每 2.5 天換只剩幾小時緩衝）、CT 叫了的 runbook（先做 Day77 分流＝SPKI 已知＝營運事故九成告警到此結束／SPKI 未知＋issuer 未知＝疑似誤發往下；申請撤銷要做但定位是（a）進入 CRLSets/CRLite 推播保護瀏覽器使用者（b）啟動 CA 的 BR 時限稽核程序留下紀錄＝Symantec 被摘根信任的路徑承 Day77＝你是在推動生態不是在關掉那張憑證；絕不能當止血手段因提報到失效中間有推播週期＋soft-fail＋一堆根本不查撤銷的 client 包括你自己家所有 Go 服務；真正止血在別處＝確認私鑰沒外洩並輪替承 Day15／更新 CAA 事前阻斷承 Day77／自家 client 上 pinning 承 Day76＝唯一真的會擋下來且不看撤銷／升級告警等級保留證據究責承 Day16／縮短自己憑證效期；重新解讀 Day76「只 pin 不驗鏈連被撤銷憑證都照收」其實是廢話＝Go 標準庫本來就不查撤銷「被撤銷憑證照收」是預設行為跟有沒有 pin 無關 ⇒ pinning 的價值從來不在撤銷而在它問的是一個不需任何線上查詢的問題「是不是這把金鑰」＝攻擊者擋不掉一個不存在的查詢）、常見誤區表（發現誤發撤銷掉就沒事/我的 Go 服務會擋被撤銷憑證/Java 預設有做撤銷檢查/開 SOFT_FAIL 比較安全至少有檢查/那就用 hard-fail/stapling 解決了 soft-fail/OCSP 是現代做法 CRL 是老東西＝兩個都在退場/瀏覽器有查 OCSP 所以機制還行/開撤銷檢查只是多一點延遲/鏈上每張都查比較嚴謹/短效憑證只是換發頻繁一點/憑證效期照習慣簽一年＝已違反 BR/撤銷檢查可以補主機名驗證＝三個不同問題）、Code Review checklist 五區（認知/Go stapled 加固/Java 撤銷開關/短效憑證與換發＝真正主線/內部 PKI）與測試（撤銷檢查存在證明＝連 revoked.badssl.com 斷言連不上，測不過就是裝飾品，多數人第一次跑會驚訝 Go 預設 client 連得上／soft-fail 行為測試＝擋掉 responder 網路觀察放行或拒絕＝這才是你真實的安全姿態不是設定檔寫的那個／stapling 缺席測試／時效重播測試 NextUpdate 過期須拒絕／hard-fail 爆炸半徑測試量有多少功能會掛／CI 憑證效期斷言 openssl x509 -noout -enddate 超門檻 CI 紅承 Day18／換發演練因「從來沒失敗過」與「從來沒被驗證過」長得一樣承 Day77） | ACME 自動換發管線與四階段/HTTP-01·DNS-01·TLS-ALPN-01 challenge 安全取捨/RFC 8657 accounturi·validationmethods 收緊 CAA/autocert 的坑/單一 CA 依賴與 backup CA（Day79）、內部 PKI 撤銷與私有 CA CRL 發佈、CRLite/Bloom filter 資料結構深入、mTLS server 端 client 憑證撤銷（短效 workload 憑證/SPIFFE）、憑證 reload 不重啟的實作、Must-Staple 導入的停機風險評估 |
| 79 | `content/資訊安全/Day79_ACME_Automated_Certificate_Renewal.md` | ACME 自動換發管線（RFC 8555） | 入門（新主題，承 Day78 預告，交叉 Day77/Day76/Day75/Day74/Day18/Day16/Day15/Day10/Day07） | 不重講 Day78 撤銷機制與 Day76/77 pinning/CT，聚焦「短效憑證把換發變成生產基礎設施」、ACME 解的問題＝讓機器自動證明控制網域然後自動拿憑證（取代人工填表/驗證信，短效憑證每 2.5 天換 146 次/年不可能派人）、四階段心智模型（帳號 Account＝帳號金鑰≠憑證金鑰兩者分開／order 下單列出要完成哪些 authorization／challenge 網域驗證是核心／finalize+download 送 CSR 帶憑證金鑰公鑰取憑證；換發無「續約」動作＝把後三階段重跑每次都要重新證明控制權）、三種 challenge 安全取捨（HTTP-01 在 web root 放 /.well-known/acme-challenge/token 檔 CA 走明文 80 埠來抓且會跟跳轉＝Day10/20/67 心智模型／需 80 埠對公網開／不能簽 wildcard／權限窄爆炸半徑小｜TLS-ALPN-01 用 acme-tls/1 ALPN 在 443 握手回自簽臨時憑證帶 extension／不需開 80／一樣不能 wildcard／需能在握手層動態回應 LB 終結 TLS 時插不上手／權限窄｜DNS-01 在 _acme-challenge 放 TXT／是 wildcard 唯一選項也是內網/防火牆後服務唯一能用的／但要整個 zone 寫入權而 DNS 正是 Day77 CAA 那把鎖所在地誰能寫 DNS 誰能拆 CAA 叫任何 CA 簽你＝爆炸半徑最大最危險）、選擇原則能 HTTP-01/TLS-ALPN-01 就別 DNS-01、DNS-01 權限解法 CNAME 委派把 _acme-challenge CNAME 指到權限隔離的小 zone 換發程式只拿小 zone 寫入權（承 Day07 最小權限收爆炸半徑）、RFC 8657 把 CAA 從 Day77「哪家 CA」收緊到「哪個帳號哪種方法」（accounturi＝只有特定 ACME 帳號能簽擋攻擊者用同一家 CA 開別帳號、validationmethods＝只允許指定 challenge 方法擋攻擊者切換到你沒在用的方法；幾乎零成本應該設；CI 斷言 dig CAA grep accounturi/validationmethods 承 Day18 DNS 記錄易被誤刪）、Go autocert 三個坑（正解掛 m.TLSConfig() 的 GetCertificate＋HTTPHandler；坑一 HostPolicy 不設＝任意 SNI 都幫申請到被 Let's Encrypt 限速自我 DoS＝必填非選配用 HostWhitelist；坑二 預設 Cache 不落磁碟或本機 DirCache 在多副本下每台各簽一份撞每週限制＝多副本必須共享 Cache 自己實作 Cache 介面接 Redis/DB/S3/Secret；坑三不適合放 LB 後面因 CA 驗證流量要打到握著 challenge 狀態那台但 LB 分流到任意後端且通常自己終結 TLS 握手到不了 Go 服務＝autocert 假設服務自己直接面對公網終結 TLS）、Java 沒有標準庫等價物且非缺陷（JDK 無 ACME client；路徑 A acme4j 在 JVM 內跑但要自理帳號金鑰/challenge 落地/儲存 reload＝等於重寫 certbot 骨架；路徑 B 最常見把換發移出 JVM＝cert-manager sidecar 塞 Secret 服務只讀 Secret／前置 LB/ingress 終結 TLS 並換發 Java 跑純 HTTP；路徑 C certbot+deploy hook reload；架構判斷 TLS 在哪終結換發就在哪做，Java 常躲 LB 後面 TLS 不在 JVM 終結所以不需 autocert 這不是語言優劣是部署形態差異）、維運主線（觸發時機不是快到期才換而是一拿到就開始換把緩衝拉到最大＝緩衝＝能容忍連續換發失敗多久 6 天憑證設剩 2 天換是自殺 certbot 三分之二預設是為 90 天設計短效要更激進；換發失敗告警要分兩種缺一不可＝換發失敗有 error＋太久沒成功換發沒 success 因排程沒跑連 error 都不會有＝Day77 偵測器自壞老病 renewal_success 當指標設 N 小時沒成功告警；到期預警門檻用剩餘效期比例非固定天數 6 天憑證設剩 7 天告警永不觸發；憑證 reload 不重啟 Go 靠每次握手都呼叫的 GetCertificate 用 mutex 保護可替換 tls.Certificate reload 失敗保留舊憑證別裸奔／Java JSSE KeyManager 初始化抓死憑證要自訂可換 X509KeyManager 包裝或重建 SSLContext 或交給 LB／reload 失敗保留舊憑證別握手全掛；單一 CA 依賴 2026-05-08 Let's Encrypt 簽發中斷是短效憑證預演效期越短對 CA 可用性依賴越硬 backup CA 值不值得看可用性要求＝支持論據消單點 反對論據多一套帳號金鑰承 Day15+多一組 CAA 授權放寬 Day77 鎖+多一條沒測過的換發路徑＝Day77 從來沒驗證過；務實建議先把換發緩衝拉大+失敗告警+reload 做好能扛多數 CA 抖動沒演練過的 backup 跟沒有一樣甚至更糟給假安心）、常見誤區表（ACME 有續約動作＝沒有換發重跑後三階段／三種 challenge 差不多隨便選／DNS-01 方便就都用＝權限最大／CAA 寫 CA 名就安全＝攻擊者能開別帳號要 RFC 8657／autocert 掛上就好＝三個坑／Java 沒 autocert 是缺陷＝不是常躲 LB 後面／快到期才換省事＝短效時代自殺／換發沒告警＝正常＝沒在跑跟沒失敗長一樣／到期預警設固定天數／換憑證重啟一下就好／用一家 CA 就夠）、Code Review/維運 checklist 六區（challenge 選擇與權限/CAA 承 Day77/autocert Go/觸發時機與緩衝/告警承 Day16·77/reload 與 CA 依賴）與測試（換發演練最重要主動逼近到期驗證真的會換會 reload＝從來沒失敗跟從來沒被驗證長一樣承 Day77/78／challenge 失敗演練 DNS API 回錯或擋 80 埠斷言失敗且告警非靜默卡住／太久沒換告警測試停掉 cron 斷言告警會叫＝換發版偵測器存在證明／reload 不重啟測試斷言 PID 不變換上新憑證再放壞檔斷言保留舊憑證／CAA CI 斷言 accounturi+validationmethods／限速防呆測試 autocert 假 SNI 斷言 HostPolicy 擋掉） | 內部服務身分與短效憑證 SPIFFE/SPIRE workload identity（Day80）、acme4j 在 JVM 內實作四階段細節、cert-manager Kubernetes 憑證管理落地、DNS-01 各 DNS 供應商 API 串接與 CNAME 委派自動化、憑證 reload 的 Java KeyManager 熱替換實作、backup CA 切換演練與多 CA 換發路徑、ACME External Account Binding（EAB）與商用 CA 綁定 |
