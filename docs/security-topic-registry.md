| Day | File | Primary Topic | Angle | Covered Scope | Extension Ideas |
|---:|---|---|---|---|---|
| 01 | `content/資訊安全/Day01_SQL_Injection.md` | SQL Injection | 入門 | SQLi 基本原理、登入繞過、Prepared Statement、密碼雜湊、縱深防禦 | ORM 查詢建構器誤用、Second-order SQLi、Stored procedure 風險 |
| 02 | `content/資訊安全/Day02_XSS.md` | XSS | 入門 | Stored/Reflected/DOM XSS、留言板案例、輸出編碼、CSP、自我檢查 | Trusted Types、HTML sanitizer 比較、CSP rollout 與 report-only |
| 03 | `content/資訊安全/Day03_CSRF.md` | CSRF | 入門 | CSRF 條件、轉帳案例、SameSite、CSRF token、JWT/Bearer token 情境 | Login CSRF、OAuth state 綁定、SameSite 相容性與例外 |
| 04 | `content/資訊安全/Day04_Password_Hashing.md` | Password Hashing | 入門 | 密碼不可明文/快速 hash、salt、慢雜湊、Java/Go 範例、timing attack 提醒 | Argon2 參數調校、密碼重雜湊 migration、credential stuffing 防禦 |
| 05 | `content/資訊安全/Day05_JWT_vs_Session.md` | Session vs JWT | 入門 | Session/JWT 差異、JWT 常見漏洞、適用情境、檢查表 | Token revocation、refresh token rotation、session store 高可用 |
| 06 | `content/資訊安全/Day06_Brute_Force_Protection.md` | Brute Force Protection | 入門 | Rate limiting、漸進延遲、CAPTCHA、帳號凍結、異常通知、完整流程 | Credential stuffing 偵測、風險式驗證、裝置指紋與隱私 |
| 07 | `content/資訊安全/Day07_Broken_Access_Control_IDOR.md` | Broken Access Control / IDOR | 入門 | IDOR、UUID 不是解法、垂直越權、default deny、分層防禦 | ABAC/ReBAC 設計、租戶隔離測試、policy engine 導入 |
| 08 | `content/資訊安全/Day08_Input_Validation_Mass_Assignment.md` | Input Validation / Mass Assignment | 混合入門 | Mass Assignment、DTO、allowlist、型別/格式/商業邏輯/跨欄位驗證、validation 攻擊面 | 純 Input Validation 延伸、schema validation、錯誤訊息洩漏、驗證規則測試 |
| 09 | `content/資訊安全/Day09_Security_Headers_CORS.md` | Security Headers / CORS | 入門 | HSTS、CSP、X-Frame-Options、nosniff、Referrer-Policy、Permissions-Policy、CORS 設定 | CSP nonce/hash 實作、CORS preflight cache、credentialed CORS 審查 |
| 10 | `content/資訊安全/Day10_SSRF_Server_Side_Request_Forgery.md` | SSRF | 入門 | 使用者控制 URL、metadata service、內網 pivot、port scan、blind SSRF、黑名單問題、DNS rebinding、redirect、Java/Go 防禦 | DNS pinning、egress proxy、cloud metadata hardening、SSRF 偵測 |
| 11 | `content/資訊安全/Day11_Path_Traversal_File_Upload.md` | Path Traversal / File Upload | 入門 | 檔名路徑穿越、黑名單繞過、canonical path、安全上傳、常見陷阱 | Symlink race、object storage upload policy、檔案內容掃描 |
| 12 | `content/資訊安全/Day12_Command_Injection.md` | Command Injection | 入門 | Shell 拼接風險、命令注入能力、避免 shell、參數化呼叫、變形與陷阱 | Argument injection、ImageMagick/ffmpeg 安全包裝、sandboxing |
| 13 | `content/資訊安全/Day13_XXE_XML_External_Entity.md` | XXE | 入門 | XML Entity、讀檔 payload、SSRF、Blind XXE、Billion Laughs、Java parser 設定、Go 注意事項 | SAML/SOAP XXE、parser-specific hardening、legacy XML migration |
| 14 | `content/資訊安全/Day14_Insecure_Deserialization.md` | Insecure Deserialization | 入門 | Java ObjectInputStream、Jackson default typing、FastJSON、SnakeYAML、Go 型別混淆、防禦原則 | Gadget chain 案例、ObjectInputFilter、套件 CVE 分析 |
| 15 | `content/資訊安全/Day15_Secrets_Management.md` | Secrets Management | 入門 | Secret 類型、錯誤做法、分層防禦、log 不洩漏、檢查清單 | Secret rotation、KMS/Vault 整合、CI secret scanning |
| 16 | `content/資訊安全/Day16_Security_Logging_Monitoring.md` | Security Logging / Monitoring | 入門 | 安全日誌欄位、不可記錄資料、SLF4J/MDC、Go slog、trace ID、警報 | SIEM detection rules、audit log tamper resistance、PII redaction |
| 17 | `content/資訊安全/Day17_Rate_Limiting.md` | Rate Limiting / API Throttling | 入門 | 演算法、Bucket4j、Redis Lua、Go rate limiter、部署層級與坑 | Distributed quotas、fairness、per-user/per-IP 混合策略 |
| 18 | `content/資訊安全/Day18_Supply_Chain_Vulnerable_Dependencies.md` | Supply Chain / Dependencies / SBOM | 入門 | 供應鏈攻擊類型、Dependency-Check、govulncheck、Trivy、SBOM | SLSA、dependency confusion、lockfile review、provenance |
| 19 | `content/資訊安全/Day19_TLS_Cryptographic_Failures.md` | TLS / Cryptographic Failures | 入門 | TLS 基礎、常見加密失誤、敏感資料外洩、安全 API 呼叫 | mTLS rollout、certificate pinning 取捨、cipher policy |
| 20 | `content/資訊安全/Day20_Open_Redirect.md` | Open Redirect | 入門 | 釣魚風險、危險 redirect 寫法、allowlist、防禦、OAuth redirect_uri 注意 | OAuth mix-up、deep link redirect、signed return URL |
| 21 | `content/資訊安全/Day21_SSTI_Server_Side_Template_Injection.md` | SSTI | 入門 | SSTI 原理、與 XSS 差異、危險後端寫法、防禦、使用者可編輯模板 | Template sandbox bypass、Jinja/Freemarker/Thymeleaf 差異 |
| 22 | `content/資訊安全/Day22_Race_Condition_TOCTOU.md` | Race Condition / TOCTOU | 入門 | 競爭條件、銀行轉帳案例、併發攻擊、DB transaction、悲觀鎖、條件式 update、樂觀鎖、分散式鎖、idempotency | DB isolation 深入、併發測試、分散式一致性與補償 |
| 23 | `content/資訊安全/Day23_HTTP_Request_Smuggling.md` | HTTP Request Smuggling | 入門 | CL/TE 不一致、CL.TE、Go/Java 安全性、HTTP/2 downgrade、防禦 | Proxy chain 測試、H2/H3 邊界、WAF/CDN 行為差異 |
| 24 | `content/資訊安全/Day24_OAuth2_OIDC_Pitfalls.md` | OAuth2 / OIDC Pitfalls | 入門 | Authorization Code Flow、redirect_uri、PKCE、state、ID Token 驗證、Grant Type | Token substitution、nonce、multi-tenant issuer validation |
| 25 | `content/資訊安全/Day25_BOPLA_GraphQL_Excessive_Data_Exposure.md` | BOPLA / GraphQL Excessive Data Exposure | 入門 | BOLA vs BOPLA、讀取面暴露、寫入面 Mass Assignment、角色欄位、GraphQL、N+1 | Field-level auth framework、schema lint、GraphQL auth tests |
| 26 | `content/資訊安全/Day26_Webhook_Security.md` | Webhook Security | 入門 | HMAC 簽章、重放攻擊、冪等性、出站 Webhook SSRF、檢查表 | Webhook retry policy、secret rotation、tenant isolation |
| 27 | `content/資訊安全/Day27_MFA_TOTP.md` | MFA / TOTP | 入門 | MFA 要素、RFC 6238、後端流程、Java/Go、重放、防備援碼 | Recovery flow abuse、step-up auth、TOTP drift 與鎖定策略 |
| 28 | `content/資訊安全/Day28_FIDO2_WebAuthn_Passkey.md` | FIDO2 / WebAuthn / Passkey | 入門 | WebAuthn/Passkey 關係、防釣魚、密碼學、Java/Go、地雷、傳統 FIDO2 vs Passkey | Account recovery、resident key、enterprise attestation |
| 29 | `content/資訊安全/Day29_NoSQL_Injection.md` | NoSQL Injection | 入門 | MongoDB 登入繞過、型別混淆、Java/Go 範例、Aggregation Pipeline、防禦清單 | Elasticsearch injection、query DSL allowlist、operator injection tests |
| 30 | `content/資訊安全/Day30_Web_Cache_Poisoning.md` | Web Cache Poisoning | 入門 | Cache key vs unkeyed input、X-Forwarded-Host、污染手法、Java/Go、CDN 設定 | Cache deception、Vary header strategy、CDN-specific hardening |
| 31 | `content/資訊安全/Day31_ReDoS_Regex_Denial_of_Service.md` | ReDoS | 入門 | 指數回溯、災情、Java email validator、Go regexp 安全性、防禦清單 | Regex linting、timeout strategy、RE2 migration |
| 32 | `content/資訊安全/Day32_Timing_Attack_Constant_Time_Compare.md` | Timing Attack / Constant-Time Compare | 入門 | 字串比對時序洩漏、API token 驗證、Go 標準庫、其他時序洩漏點 | Remote timing feasibility、padding oracle、constant-time review |
| 33 | `content/資訊安全/Day33_Session_Fixation.md` | Session Fixation | 入門 | Session fixation 原理、與 hijacking 差異、登入後換 session ID、防禦、測試 | Session rotation edge cases、single sign-out、device binding |
| 34 | `content/資訊安全/Day34_CRLF_Injection_HTTP_Header_Injection.md` | CRLF / HTTP Header Injection | 入門 | CRLF、response splitting、常見變體、防禦層次、驗證範例 | Log injection、email header injection、proxy header normalization |
| 35 | `content/資訊安全/Day35_Subdomain_Takeover.md` | Subdomain Takeover | 入門 | Dangling DNS、常見服務、接管影響、偵測腳本、流程防禦 | Cloud inventory automation、CI/CD DNS lifecycle、brand monitoring |
| 36 | `content/資訊安全/Day36_Clickjacking_UI_Redressing.md` | Clickjacking / UI Redressing | 入門 | iframe 點擊劫持、變體、後端責任、X-Frame-Options/CSP、防禦範例 | CSP frame-ancestors migration、OAuth consent clickjacking |
| 37 | `content/資訊安全/Day37_JWT_Algorithm_Confusion.md` | JWT Algorithm Confusion | 入門 | JWT 結構、alg none、RS256 to HS256、kid 注入、Java/Go、防禦與 CVE | JWKS cache poisoning、key rotation、multi-tenant issuer isolation |
| 38 | `content/資訊安全/Day38_X_Forwarded_For_Spoofing.md` | X-Forwarded-For / Proxy Header Trust | 入門 | 錯信 header、XFF 正確讀法、災難情境、Spring/Go、Cloudflare/CDN | Multi-proxy chain policy、rate limit identity、trusted proxy config tests |
| 39 | `content/資訊安全/Day39_DNS_Rebinding.md` | DNS Rebinding | 入門 | 瀏覽器變內網跳板、攻擊流程、與 SSRF 差異、後端四道防線、Actuator 陷阱 | DNS pinning implementation、admin panel hardening、browser private network access |
| 40 | `content/資訊安全/Day40_HTTP_Parameter_Pollution.md` | HTTP Parameter Pollution | 入門 | 重複參數、框架差異、攻擊情境、漏洞程式碼、防禦與測試 | API gateway normalization、OAuth parameter pollution、contract tests |
| 41 | `content/資訊安全/Day41_LDAP_Injection.md` | LDAP Injection | 入門 | LDAP filter、登入驗證漏洞、防禦、程式碼審查清單 | Active Directory edge cases、escaping libraries、SSO directory integration |
| 42 | `content/資訊安全/Day42_Cookie_Security_Attributes.md` | Cookie Security Attributes | 入門 | HttpOnly、Secure、SameSite、`__Host-`、實作、檢查表、迷思 | Cookie partitioning、CHIPS、跨站登入兼容性 |
| 43 | `content/資訊安全/Day43_Prototype_Pollution_Object_Injection.md` | Prototype Pollution / Object Injection | 入門 | JS prototype pollution、Jackson readerForUpdating、Go mergo/map 合併、防禦與測試 | Lodash CVE、schema hardening、recursive merge audit |
| 44 | `content/資訊安全/Day44_ZIP_Slip_Archive_Extraction.md` | ZIP Slip / Archive Extraction | 入門 | 壓縮檔路徑穿越、Java/Go 解壓縮、惡意 zip 測試、zip bomb、防禦規則 | Tar symlink attacks、nested archive limits、antivirus pipeline |
| 45 | `content/資訊安全/Day45_WebSocket_Security.md` | WebSocket Security | 入門 | CSWSH、Java/Go 正確寫法、長連線授權、其他地雷、檢查清單 | Token refresh over WebSocket、message-level auth、backpressure DoS |
| 46 | `content/資訊安全/Day46_Host_Header_Injection.md` | Host Header Injection | 入門 | Host header 原理、後端踩雷、攻擊手法、漏洞範例、防禦 | Password reset poisoning、reverse proxy allowed hosts、absolute URL generation |
| 47 | `content/資訊安全/Day47_Insecure_Randomness.md` | Insecure Randomness | 入門 | PRNG vs CSPRNG、常見災難、Java/Go 範例、SecureRandom 地雷 | Token entropy testing、UUID misuse、randomness in distributed systems |
| 48 | `content/資訊安全/Day48_HMAC_API_Request_Signing.md` | HMAC / API Request Signing | 入門 | Request signing、canonical string、接收端地雷、密鑰管理、檢查表 | Canonicalization pitfalls、clock skew/replay design、key rotation |
| 49 | `content/資訊安全/Day49_BFLA_Broken_Function_Level_Authorization.md` | BFLA | 入門 | 功能層級授權、漏洞樣貌、deny by default、細節、API 測試、檢查表 | Route inventory、policy-as-code、admin endpoint testing |
| 50 | `content/資訊安全/Day50_GraphQL_Security.md` | GraphQL Security | 入門 | Introspection、深度/複雜度攻擊、Alias/Batching 繞 rate limit、細節、檢查表 | Persisted queries、operation cost model、GraphQL auth middleware |
| 51 | `content/資訊安全/Day51_gRPC_Protobuf_Security.md` | gRPC / Protobuf Security | 入門 | gRPC 攻擊面、Protobuf 反序列化 DoS、MaxRecvMsgSize、TLS/mTLS、檢查表 | Service mesh mTLS、protobuf schema evolution、gRPC auth interceptor |
| 52 | `content/資訊安全/Day52_Insecure_Deserialization.md` | Insecure Deserialization | 重寫/補強 | Java 原生序列化 RCE、ObjectInputFilter、Jackson polymorphism、SnakeYAML、Go 注意事項 | Gadget chain 深入、JEP 415、Runtime detection、CVE case study |
| 53 | `content/資訊安全/Day53_SSRF_Server_Side_Request_Forgery.md` | SSRF | 重寫/補強 | SSRF 本質、metadata endpoint、縮圖服務案例、黑名單 IP 失效、Java/Go safe HTTP client、檢查表 | Egress proxy、redirect/DNS pinning、cloud metadata hardening、blind SSRF detection |
| 54 | `content/資訊安全/Day54_XXE_XML_External_Entity_Injection.md` | XXE | 重寫/補強 | DTD/外部實體、預設設定風險、讀檔與打內網、Java 禁用外部實體、Go encoding/xml 注意 | SAML/SOAP 實戰、parser regression tests、legacy XML parser migration |
| 55 | `content/資訊安全/Day55_Mass_Assignment_Auto_Binding.md` | Mass Assignment / Auto-Binding | 重寫/補強 | Auto-binding 本質、Spring 反例與 DTO/JsonIgnore/setAllowedFields、Go json.Unmarshal model 陷阱、檢查表 | Framework-specific binding rules、OpenAPI schema tests、BOPLA write-side auth |
| 56 | `content/資訊安全/Day56_Race_Condition_TOCTOU.md` | Race Condition / TOCTOU | 重寫/補強 | Check/use 非原子、Java synchronized/樂觀鎖/悲觀鎖、Go mutex/atomic DB update、檢查表 | Isolation levels、idempotency key design、concurrency fuzz tests、distributed lock failure modes |
| 57 | `content/資訊安全/Day57_JWT_Common_Pitfalls.md` | JWT Common Pitfalls | 延伸 | JWT 安全依賴簽章、alg none、decode without verify、演算法混淆、弱密鑰、Java/Go 驗證、檢查表 | JWKS/kid injection、key rotation、refresh token rotation、multi-tenant issuer/audience validation |
| 58 | `content/資訊安全/Day58_CORS_Misconfiguration.md` | Security Headers / CORS | 延伸（承 Day09） | CORS 白名單比對 bug（startsWith/endsWith/contains/未錨定 regex）、反射 Origin、null origin（sandboxed iframe）、Vary: Origin 快取污染、Spring setAllowedOrigins/OriginPatterns、Go map 精確比對、code review/測試清單 | 子網域接管與 CORS 信任鏈、CDN/proxy cache key 與 Origin、CORS 與 CSRF 區隔誤區 |
| 59 | `content/資訊安全/Day59_CSV_Formula_Injection.md` | CSV / Formula Injection | 入門 | 公式注入本質（試算表把 `=+-@\t\r` 開頭當公式）、HYPERLINK/WEBSERVICE/IMPORTXML/DDE 危害、輸入驗證 vs 輸出逸出分離、`'` 中和 + CSV 結構逸出、Java 手寫 CSV 與 Apache POI setCellValue vs setCellFormula、Go encoding/csv 不做公式逸出、誤區與測試清單 | XLSX/匯入端解析硬化（zip bomb/XXE）、其他匯出格式注入、Google Sheets 特有函數外洩 |
| 60 | `content/資訊安全/Day60_XLSX_Parsing_Import_Hardening.md` | XLSX 匯入端解析硬化 | 延伸（承 Day44 × Day13/Day54） | 匯出逸出 vs 匯入解析兩道防線對照、XLSX=ZIP+XML 結構拆解、解壓放大/zip bomb（POI ZipSecureFile setMinInflateRatio/setMaxEntrySize/setMaxTextSize、IOUtils.setByteArrayMaxOverride；Go excelize UnzipSizeLimit/UnzipXMLSizeLimit 下修預設）、XLSX 內藏 XXE（POI 預設關閉、自拆 XML 才需套禁用 DTD、Go encoding/xml 不吃外部實體）、外部參照/公式回連 SSRF、streaming 解析與縱深設計、檢查表與測試 | 其他 Office 格式（docx/pptx）解析硬化、polyglot/magic bytes 檔案真身驗證、formula evaluator 沙箱化 |
| 61 | `content/資訊安全/Day61_SSTI_Template_Engine_Hardening.md` | SSTI | 延伸（承 Day21） | 模板來源可控才是 SSTI 本質（compile 階段 vs render 階段）、Thymeleaf 動態 view name/fragment 表達式、FreeMarker new Template(StringReader)/?new/?eval 與 setNewBuiltinClassResolver(ALLOWS_NOTHING)/setAPIBuiltinEnabled/ObjectWrapper 設定、Velocity evaluate vs getTemplate 常數路徑、Go text/template vs html/template（防 XSS 非防 SSTI）/Parse 使用者字串/FuncMap 與 {{.}} 資訊洩漏、sandbox 繞過本質（可達物件圖+黑名單）、Code Review/grep checklist 與回歸測試 | 各引擎 sandbox bypass CVE 案例、Pebble/Groovy/Jinja 對照、使用者自訂模板的受限 DSL 設計、隔離程序+egress 控制實作 |
| 62 | `content/資訊安全/Day62_Mass_Assignment_Nested_Binding.md` | Mass Assignment / Auto-Binding | 延伸（承 Day08 / Day55） | 巢狀／集合綁定才是 DTO 之後仍中招的點（白名單必須跟物件圖往下鑽）、Spring nested path binding（role.name/items[0].price/account.permissions[0]）、@InitBinder setAllowedFields/setDisallowedFields 巢狀萬用字元陷阱（items*.quantity/shipping.address.*、disallow 漏 *）、@InitBinder 只管表單而 @RequestBody 走 Jackson（巢狀貧血 DTO/@JsonProperty READ_ONLY）、readerForUpdating 部分更新的巢狀提權、Go 巢狀 struct json.Unmarshal/DisallowUnknownFields 遞迴、PATCH map[string]any deepMerge 用巢狀完整路徑 allowlist 過濾、OpenAPI additionalProperties:false + CI 越權回歸測試 | 多型/@JsonTypeInfo 綁定越權、集合元素層級授權、GraphQL input object 巢狀綁定、ORM cascade 連動寫入風險 |
| 63 | `content/資訊安全/Day63_ESI_Injection.md` | ESI Injection（Edge Side Includes） | 入門 | 漏洞在 CDN/反向代理而非應用碼、ESI 標籤（esi:include/esi:vars）邊緣節點組裝頁面、Surrogate-Control content="ESI/1.0" 觸發 ESI、偵測 payload、危害四類（SSRF 從邊緣發出、繞 HttpOnly 讀 cookie、cache poisoning、邊緣 SSTI/XSLT RCE）、HTML 逸出≠ESI 逸出、Java Spring 反例與 Go html/template、Varnish do_esi 白名單與 origin-only header、include 來源白名單、egress 控制、code review/偵測測試清單 | 各 ESI 實作差異（Akamai XSLT/Fastly/Varnish）、surrogate header 全鏈路盤點自動化、與 Day30 cache poisoning 的交叉、邊緣端 RCE gadget |
| 64 | `content/資訊安全/Day64_Web_Cache_Deception.md` | Web Cache Deception（網頁快取欺騙） | 入門 | 與 Day30 Cache Poisoning 方向相反（讀出私密 vs 寫入污染）、攻擊鏈五步（誘導已登入受害者點 /account/profile/x.css → CDN 看副檔名快取且忽略 Cookie → origin 寬鬆比對回 200 私密頁 → 快取存個資 → 攻擊者未登入 cache hit 讀取）、根因為 origin 與 CDN 兩套世界觀不一致（CDN 用副檔名/前綴/Content-Type 猜靜態並忽略 cookie；origin 路徑正規化/後綴比對/catch-all 把假副檔名 URL 接到私密 handler）、防禦 1 動態回應一律 Cache-Control: no-store（no-store vs private vs no-cache 區別、default-deny 快取白名單 opt-in、Spring ResourceHandler+OncePerRequestFilter、Go middleware staticPrefixes）、防禦 2 嚴格路徑比對讓 /account/profile/x.css 直接 404（Spring useSuffixPatternMatch/AntPathMatcher 反例、PathPatternParser、Go path.Clean 後精確比對）、防禦 3 CDN cache key/靜態動態網域隔離/normalization 對齊、code review 與偵測回歸測試清單 | CDN 廠商特定快取規則（Cloudflare/Akamai/Fastly）、path normalization 差異模糊測試自動化、與 Day30/Day63 邊緣快取家族交叉、靜態網域隔離架構落地 |
| 65 | `content/資訊安全/Day65_Email_Header_Injection.md` | Email Header Injection（郵件標頭注入 / SMTP 注入） | 入門 | 與 Day34 同源 CRLF 但 sink 換成 email/MIME（非 HTTP 回應）、RFC 5322 標頭區/空行分界原理、輸入含 \r\n 偷插 Bcc/Cc/From 或整段 MIME body（垃圾信/釣魚信跳板，過你 SPF/DKIM）、最常中招欄位（收件人/Cc/Bcc、Reply-To/From 顯示名稱、Subject、自訂 header、範本插值點）、Java 反例 addHeader/setHeader 手拼 vs 正解 InternetAddress(strict=true)+setReplyTo/setRecipients/setSubject(RFC 2047 encoding)、Go net/smtp.SendMail 收原始 []byte 自拼 header 風險 vs 正解 net/mail.ParseAddress 驗證+sanitizeHeaderValue reject CR/LF+mime.QEncoding 主旨+mail.Address.String()、防禦三原則（用函式庫型別把關/標頭值 reject CR/LF/收件人由系統決定 envelope 一致）、code review grep 重點與 CRLF payload 偵測回歸測試 | DMARC/SPF/DKIM 寄件網域驗證、寄信服務（SES/SendGrid）設定誤區、HTML email 內容類注入、寄信端點 rate limit 濫用防護 |
| 66 | `content/資訊安全/Day66_DMARC_SPF_DKIM_Email_Authentication.md` | SPF / DKIM / DMARC 寄件網域驗證 | 延伸（承 Day65） | 威脅換層：Day65 是「後端被注入」、本篇是「網域被冒名」（程式碼乾淨攻擊者仍可從自己主機寄 From: 你的網域）、SMTP 雙寄件者（信封寄件者 MAIL FROM/Return-Path vs 標頭 From: 各自獨立不驗證）、三層分工（SPF 授權寄件 IP 驗信封寄件者 TXT v=spf1 include -all、DKIM 私鑰簽章 d=/s=selector/h= 保內容完整與授權、DMARC 把前兩者對齊到使用者看到的 From: _dmarc TXT p=none/quarantine/reject + rua + adkim/aspf）、缺一不可原因（SPF/DKIM 都不直接管 From: 網域，DMARC 補對齊縫）、後端殺手 alignment（SPF alignment：服務商共享 Return-Path amazonses.com 不對齊→設自訂 MAIL FROM 子網域；DKIM alignment：共享 d= 不對齊→啟用自有網域 DKIM CNAME 讓 d=your-domain）、Java SES SDK v2 fromEmailAddress+feedbackForwardingEmailAddress 寫死已驗證 From、Go sesv2 同概念 From 用常數不讓使用者控制網域、SES/SendGrid 六大誤區（只驗單一信箱/共享 Return-Path 與 DKIM/直接 p=reject 沒先 p=none/~all+all/SPF 超過 10 次 lookup PermError/多寄件來源只設一個 DKIM）、驗證測試（raw header Authentication-Results spf/dkim header.d 對齊/dmarc=pass、CI dig TXT/CNAME 斷言、rua 報告盤點冒名與漏設來源） | BIMI 與品牌驗證、DKIM key rotation/selector 輪替、SES SNS bounce/complaint 事件處理、DMARC 報告解析自動化、子網域寄件策略與信任隔離 |
| 67 | `content/資訊安全/Day67_Open_Redirect_Parser_Differential_Signed_URL.md` | Open Redirect | 延伸（承 Day20） | 不重講 Day20 入門（定義/釣魚/基本白名單/常見繞過 payload/跳轉代碼映射），聚焦「白名單寫對仍被繞過」、核心問題 parser differential（驗證用的 parser 與跳轉用的 client 對同一畸形 URL 解出不同 host，RFC 3986 vs WHATWG 容錯差異）、畸形 payload（yourapp.com\@evil.com、evil.com\.yourapp.com、yourapp.com:@evil.com、#@evil.com）、Java 三 parser 不一致（java.net.URI 嚴格可能 host=null、java.net.URL 較寬鬆、Spring UriComponentsBuilder 又一套）、穩健三原則（正規化 \→/、拒絕 userinfo @、跳轉用「重建後字串」uc.toUriString() 而非原始 next，讓驗到的 host==跳出的 host）、Go net/url 陷阱（不自動正規化 \、// protocol-relative 解出 Host 無 Scheme、用 u.Hostname() 去 port、拒 u.User!=nil、回 u.String()）、簽章式 return URL 解動態外站（白名單列不完時改 HMAC 簽 to+sig，接 Day48；Java MessageDigest.isEqual／Go hmac.Equal constant-time 比較接 Day32；密鑰接 Day15；進階加過期與綁 session 防重放）、進階串接 Open Redirect→OAuth token 竊取（即使 provider redirect_uri exact match，應用內 Open Redirect 當合法網域跳板把 token 從 callback 二次跳轉帶到 evil.com，交叉 Day24）、偵測與 Code Review checklist（驗證/跳轉同一份解析、host 用 equals/map 非 endsWith/startsWith、callback 二次跳轉納白名單、白名單拒絕告警接 Day16、繞過 payload 當 CI 回歸測資、簽章驗證失敗率監控） | CRLF/Location header 注入交叉（與 Day34）、deep link/custom scheme 跳轉劫持、URL parser 模糊測試自動化、SSO 跨網域 return URL 信任邊界、meta refresh/JS 導頁的 javascript: 升 XSS |
| 68 | `content/資訊安全/Day68_Open_Redirect_CRLF_Location_Header_Injection.md` | Open Redirect × CRLF / Location Header Injection | 延伸（承 Day67，交叉 Day20 × Day34） | 不重講 Open Redirect 白名單入門（Day20/67）也不重講 CRLF response splitting 入門（Day34），聚焦交會點：跳轉值過了「網域白名單」但仍含 CR/LF 塞進 Location 標頭 → 注入新標頭/分裂回應（白名單管 host、控制字元把關管字串，兩道正交防線）、反例 host 驗過卻送原始 next（next=https://yourapp.com/welcome%0d%0aSet-Cookie:...）、現代框架防護與盲點（Tomcat setHeader/addHeader/sendRedirect 拒絕含控制字元標頭；Go net/http 用 replacer 把 \r\n 換空白而非拒絕；兩者盲點都在「工程師自己先解碼/手拼標頭/走非標準輸出」）、三種破口（先 URLDecoder/QueryUnescape 解碼再驗證再手拼 setHeader、把跳轉值再塞進自訂 X-* 標頭、meta refresh/JS location 跳出標頭消毒變成 XSS+javascript: scheme）、正解三道正交防線（A 網域白名單用重建字串承 Day67、B 解碼後 reject 控制字元 [\x00-\x1f\x7f] 不依賴框架、C sink 對號入座：流到標頭交框架寫入器不手拼／流到 HTML 用輸出編碼+scheme 白名單拒 javascript:/data:）、Java safeRedirectTarget（解碼一次+CTRL pattern+UriComponentsBuilder host/userInfo/重建）、Go safeRedirectTarget（query 已解碼一次不再多解防 double-encoding %250d%250a、ctrlChars regex、u.User/u.Hostname/u.String）、CRLF 家族 sink 地圖（Location=本篇×Day34、Host=Day46、Email/MIME=Day65、Log=Day16、HTML=Day02 XSS）、Code Review/CI 回歸測試 checklist（驗證後是否多解碼、解碼後 reject 控制字元、手拼標頭盤點、CRLF payload 斷言無分裂/無多標頭、控制字元 reject 告警接 Day16） | Reflected File Download（Content-Disposition sink，Day69）、double/multi encoding 正規化管線設計、response splitting 在 HTTP/2 的差異、proxy/gateway 層標頭正規化不一致、自訂標頭全端點盤點自動化 |
| 69 | `content/資訊安全/Day69_Reflected_File_Download.md` | Reflected File Download（RFD） | 入門（新主題，Content-Disposition sink，承 Day68 預告） | RFD 本質（攻擊者讓瀏覽器把「你的 API 回應」存成可執行檔且內容變成可執行指令，2014 Oren Hafif）、三個 AND 成立條件（1 反射 Reflection：回應含使用者可控字串，最常見 JSONP callback 或原封反射 query/path；2 檔名 Filename：無 Content-Disposition 時瀏覽器拿 URL 路徑末段當檔名，用 matrix 參數 ;/setup.bat 控制副檔名；3 執行 Execution：callback=cmd|'/c calc'|| 讓 body 變合法 batch 指令）、經典攻擊 URL /api/search;/setup.bat?q=x&callback=cmd|'/c calc'||、為何比 XSS 難防且好騙（網址列是可信網域、HTTPS 合法、繞過 SmartScreen/Gatekeeper 信任、檔案是你伺服器親手簽發、把使用者對網域的信任武器化，根因在後端）、Java 反例（Spring @GetMapping produces=application/javascript 原封反射 callback 無 Content-Disposition，路由層移除 ;matrix 仍打到 handler 但瀏覽器看到 setup.bat 的認知不一致）、Go 反例（net/http 反射 callback 無 Content-Disposition/nosniff）、三道防禦各打掉一條件（A 強制 Content-Disposition: attachment; filename="固定安全檔名" 打掉可控檔名，檔名含使用者輸入須白名單 [A-Za-z0-9._-] 去 ;/"/控制字元且副檔名自己決定；B X-Content-Type-Options: nosniff + 正確 Content-Type application/json 非 javascript/html 打掉型別誤判；C 下架/白名單化 JSONP 反射：SAFE_CALLBACK ^[A-Za-z0-9_.]{1,64}$ 打掉可執行內容，能改 CORS 就下架 callback）、輔助 Spring setRemoveSemicolonContent(true) 讓路由一致但無法改瀏覽器檔名（主力仍是防禦一）、Code Review/CI checklist（盤點 JSONP callback 端點、callback 白名單、可下載端點固定 attachment+安全檔名、檔名不吃使用者副檔名、全站 nosniff、Content-Type 正確、路由拒 ;或可執行副檔名結尾、CDN/gateway 與後端 path 解析一致、探測 URL 斷言帶 Content-Disposition+nosniff、可疑檔名 reject 告警接 Day16） | Content-Disposition filename/filename*（RFC 5987/6266）非 ASCII 檔名安全處理（Day70）、inline vs attachment 與預覽取捨、Content-Type↔副檔名白名單映射、http.ServeContent/ResponseEntity<Resource> 大檔串流下載、mime.FormatMediaType 標頭注入防護 |
| 70 | `content/資訊安全/Day70_Content_Disposition_Download_Hardening.md` | Content-Disposition 下載端點進階防禦 | 延伸（承 Day69） | 不重講 RFD 三條件與 Day69 入門防禦，聚焦「本來就要提供下載」的合法端點怎麼做對、合法下載端點三破口（標頭注入 filename 引號逸出/CRLF、非 ASCII 檔名亂拼、Content-Type 誤判 sniff 提權）、RFC 6266/5987 標頭語法（filename= 只能 Latin-1 quoted-string；filename*=UTF-8''percent-encoded 給非 ASCII；相容策略兩者並給現代瀏覽器優先 filename*）、Java Spring ContentDisposition.attachment().filename(name, UTF_8).build() 自動處理 filename* 與逸出（builder 只保證格式正確不保證語意安全）、Go mime.FormatMediaType 自動走 RFC 2231/5987 且不合法回空字串要判斷降級、Content-Type↔副檔名伺服器白名單映射（不依使用者副檔名/client header/不 sniff，白名單外降 application/octet-stream + nosniff、SVG/HTML 禁 inline）、inline vs attachment（預設 attachment、disposition-type 伺服器常數、可預覽型別隔離無 cookie 網域承 Day64）、sanitizeFilename 語意清洗（reject 控制字元承 Day68、去路徑成分承 Day11、去 "/;、限長、副檔名伺服器決定承 Day69）、大檔串流 Go http.ServeContent 自動處理 Range/Content-Length/If-Range 與 Java ResponseEntity<Resource>+FileSystemResource+contentLength 實際大小（不讀成 byte[]、Content-Length 一致避免邊界錯位呼應 Day23、授權在取檔前承 Day07）、Code Review/CI checklist 與惡意/非 ASCII 檔名+Range 回歸測試 | HTTP Range 請求 DoS/range amplification 與段數上限（Day71）、使用者內容網域隔離架構落地、magic bytes 決定可信型別（承 Day60）、簽章式下載連結與過期、CDN 下載快取與 Vary |
| 71 | `content/資訊安全/Day71_HTTP_Range_Request_DoS.md` | HTTP Range 請求安全與 DoS | 入門（新主題，承 Day70 串流下載） | Range 本身是攻擊面（承 Day70「把 Range 交給標準庫」）、Range 語意複習（bytes=、206 Partial Content、Content-Range、Accept-Ranges、多段 multipart/byteranges 每段 boundary+Content-Type+Content-Range 標頭 overhead）、range amplification 兩種放大分開看：資料放大（重疊 range 同段複製多次 sum>檔案大小，經典 CVE-2011-3192 Apache Killer bytes=0-,0-1,0-2... 把 100KB 放大成近 100MB 吃記憶體/CPU/頻寬）vs overhead/CPU 放大（大量微小不重疊 range bytes=0-0,2-2,4-4... 總和小躲過檢查但每段 MIME 標頭累積+上萬次字串組裝）、標準庫只擋一半：Go http.ServeContent 內部 sumRangesSize(ranges)>size 就 ranges=nil 回 200 整檔擋掉資料放大但不限段數、Tomcat/Spring ResourceHttpRequestHandler/ResourceRegion 依版本自行加固不可假設、防禦主線前置中介層/Filter 三規則（段數上限 ≤16 超標拒絕 416 或降級丟棄 Range 回 200、總量上限 sum>檔案大小丟棄、畸形 range 負值/start>end/超界回 416+Content-Range: bytes */size）、Go LimitRanges middleware 只數逗號提早短路+r.Header.Del("Range")降級+http.Server MaxHeaderBytes 16KB+ReadHeaderTimeout 擋巨型 Range 標頭、Java RangeLimitFilter OncePerRequestFilter countSegments+sendError 416 或 NoRangeRequestWrapper 隱藏 Range+server.max-http-request-header-size 16KB+connection-timeout、If-Range 一致性（ETag 相符 206 不符回 200 整新檔避免拼接損毀）與坑（ETag 不穩定/弱 ETag 讓 If-Range 頻繁失效把 206 退化成 200 整檔放大頻寬）、CDN/反向代理 Range 納 cache key/Vary 承 Day30/Day64、授權在取 range 前承 Day07、Code Review/CI checklist 與段數/重疊/巨型標頭/畸形/If-Range 回歸測試 | Slowloris/慢速 HTTP DoS 連線佔用型（Day72）、range 與 CDN cache key/Vary 深入、HTTP/2 flow control 與 range 交互、影片串流分塊授權、byte-serving 與簽章式下載連結交叉 |
| 72 | `content/資訊安全/Day72_Slowloris_Slow_HTTP_DoS.md` | Slowloris / 慢速 HTTP DoS（連線佔用型） | 入門（新主題，承 Day71 DoS 家族） | 與 Day71「放大型」DoS 相反的「連線佔用型」（少流量小封包，用慢速把有限連線槽/執行緒卡死，CPU 低流量小傳統告警抓不到）、請求生命週期拆解對應三種攻擊（收標頭階段=Slowloris 永不送標頭區結束空行改隔幾十秒補無關標頭續命；收 body 階段=Slow POST/R.U.D.Y. Content-Length 宣告很大 body 每幾秒送 1 byte；送回應階段=Slow Read 極小 TCP 接收視窗慢讀塞住 server 送出緩衝；keep-alive 閒置也佔槽）、thread-per-connection（Tomcat BIO）為何脆弱（一條慢連線佔一整個 worker thread，maxThreads 預設 200，幾百條 Slowloris 就耗盡執行緒池）vs 非阻塞式（Go net/http goroutine 輕量、Netty/Tomcat NIO event loop 少數 I/O 執行緒照顧上萬連線，對執行緒耗盡免疫但瓶頸換成連線數/fd/記憶體仍非免疫）、防禦主線給連線生命週期每段設 timeout+連線數上限、Go http.Server 四 timeout 欄位預設 0=永不逾時最危險必顯式設（ReadHeaderTimeout 短擋 Slowloris 為主力/ReadTimeout 涵蓋 body 擋 Slow POST 但誤傷慢上傳/WriteTimeout 擋 Slow Read/IdleTimeout 回收 keep-alive/MaxHeaderBytes 承 Day71）、Go per-handler http.TimeoutHandler 管 handler 執行+寫回應非讀 body、http.MaxBytesReader 限 body 大小擋 Content-Length 說謊、netutil.LimitListener 限連線總數（官方 x/net 擴充只限總數不分 IP）、Java 防線在連接器層非 filter 層（慢速攻擊在 servlet 前發生）、Tomcat NIO 讓 maxConnections≫maxThreads 較耐打、connection-timeout 主力踢慢連線/keep-alive-timeout/max-keep-alive-requests/max-connections/accept-count/maxSwallowSize 限中止請求吞 body 成本（-1 危險）、connectionTimeout 別為容忍慢網路調太長、應用層 timeout 不夠前面要 buffering 反向代理（Nginx client_header/body_timeout+send_timeout+limit_conn 每 IP+先 buffer 完整請求天生免疫慢速把攻擊擋在便宜前層打不到 origin）、監控承 Day16 盯連線數逼近上限/connectionTimeout 中止數/單 IP 連線數（CPU/流量告警抓不到）、slowhttptest -H/-B/-X 三模式測試+斷言慢連線被主動關閉+慢上傳不誤殺、Code Review/CI checklist | HTTP/2 特有 DoS（Rapid Reset CVE-2023-44487/HPACK bomb/SETTINGS·PING·CONTINUATION flood，Day73）、非同步 servlet 與 long polling 慢速防護、mTLS/TLS handshake DoS、L4/L7 連線速率限制與 SYN flood、每 IP 連線數精細控制與 fail2ban |
| 73 | `content/資訊安全/Day73_HTTP2_Protocol_DoS.md` | HTTP/2 協定層 DoS | 延伸（承 Day71/72 DoS 家族，交叉 Day60/Day16/Day18） | 不重講 DoS 基本觀念（放大型/連線佔用型/為何設 timeout 看 Day71/72），聚焦 HTTP/2 協定機制被武器化、心智模型（多工 multiplexing/stream 狀態機/HPACK 壓縮+flow control 兩級視窗，資源計量單位從「連線」變「stream/frame/解壓後標頭」故舊防禦全要補新層：限連線數擋不住單連線內 stream 濫用）、Rapid Reset（CVE-2023-44487，Go 專屬 CVE-2023-39325）本篇主角（開 HEADERS 立刻 RST_STREAM 取消高速循環，被 reset 的 stream 不計入當前併發數故繞過 MAX_CONCURRENT_STREAMS，很多實作收 HEADERS 就 dispatch 給 worker，RST 取消的是回應不是工作→成本不對稱放大，與 Slowloris「慢」恰好相反是「快」）、Go 緩解（升級 runtime 為第一優先 Go≥1.21.3/1.20.10 內建限制「同時處理中 stream 數」非只限協定層併發、http2.ConfigureServer 顯式設 MaxConcurrentStreams 非越大越好/MaxReadFrameSize/IdleTimeout/MaxUploadBufferPerConnection·PerStream、HTTP/1.1 四 timeout 承 Day72 照設）、Tomcat 緩解 overhead 記帳（overheadCountFactor 別關=0，浪費型 frame 增 count 有進展 frame 降 count 超門檻斷線，maxConcurrentStreams vs maxConcurrentStreamExecution 後者限真正吃 worker 數承 Day72 執行緒池、maxHeaderSize/maxHeaderCount、Spring Boot 走 TomcatConnectorCustomizer 非 properties key、屬性隨版本演進最重要仍是升級）、HPACK bomb（呼應 Day60 zip bomb/Day13 Billion Laughs 只要有解壓就有放大，動態表塞長值短參照重複引用解壓放大幾十 MB，防禦 MaxDecoderHeaderTableSize/MaxHeaderListSize/maxHeaderSize 解壓過程即累計超標中止，與 Day71 MaxHeaderBytes 同哲學但管「解壓後」大小）、control-frame flood（SETTINGS/PING 逼回 ACK、WINDOW_UPDATE/空 frame flood、CONTINUATION flood 2024 標頭組裝完成前不設上限持續配置記憶體是 HPACK bomb 近親，防禦靠升級+Tomcat overhead*Threshold+HTTP/2 感知代理）、實務決策清單（升級升級升級投報率最高、顯式設上限別吃預設別設無限、HTTP/1.1 timeout 疊加非取代、前擺 HTTP/2 感知 buffering 代理/托管 LB、監控盯 RST_STREAM 速率/單連線 stream 建立取消次數/已 reset 仍執行 stream 數而非 CPU 流量承 Day16）、Code Review/維運 checklist、h2load/h2spec 測試斷言 overhead 門檻斷線+解壓過程即拒 | mTLS/TLS handshake DoS 運算不對稱（Day74）、HTTP/3 QUIC 特有攻擊面、gRPC over HTTP/2 stream 濫用交叉 Day51、CDN/邊緣層 HTTP/2 緩解落地、每 IP stream 速率限制精細控制 |
| 74 | `content/資訊安全/Day74_TLS_Handshake_mTLS_DoS.md` | mTLS / TLS 握手 DoS | 延伸（承 Day19 TLS × Day72/73 DoS 家族，交叉 Day51/Day16/Day15/Day10/Day39） | 不重講 Day19 TLS 基礎（TLS 是什麼/憑證怎麼驗/cipher 選擇/敏感資料保護），聚焦握手階段「運算不對稱」型 DoS（client 發起廉價 vs server 私鑰非對稱運算 RSA 解密/ECDHE 簽章 + mTLS 憑證鏈驗證昂貴，與 Day73 Rapid Reset「處理成本不對稱」是表兄弟，流量/CPU 未必爆在習慣告警點承 Day16）、心智模型（full handshake 一定要一次私鑰運算/resumption 走對稱式省掉非對稱運算/mTLS 多背 pre-auth 憑證鏈驗證成本；運算不對稱對照表）、重協商 DoS（THC-SSL-DOS/CVE-2011-1473，一條連線逼無限重跑握手，緩解優先序：①上 TLS 1.3 從協定移除 renegotiation 根除、②Go crypto/tls server 端天生不支援 renegotiation 免疫 Renegotiation 欄位只 client 端有意義、③Java JSSE 用 -Djdk.tls.rejectClientInitiatedRenegotiation=true 須 JSSE 初始化前生效）、握手洪水（大量新連線各走 full handshake，靠 resumption 提升省 non-asymmetric 比例但 session ticket 金鑰要輪替傷 forward secrecy 承 Day15、每 IP 新連線/握手速率限制放 L4/前置代理非應用層 Day17、握手 timeout 別讓握手無限拖=TLS 版 Slowloris）、mTLS 憑證鏈驗證放大本篇主角（放大點一長/深鏈逐張驗簽→限深度、放大點二 pre-auth 未授權者也逼你付驗證成本、放大點三握手路徑同步查 OCSP/CRL=每次握手一個對外呼叫交叉 Day10/Day39 SSRF→改 OCSP stapling/短效憑證；心態：mTLS 是授權手段非握手防 DoS 護身符反讓每次握手更貴，終結放前置閘道/mesh sidecar 承 Day51）、Go 控制（tls.Config MinVersion TLS1.3/ClientAuth RequireAndVerifyClientCert/ClientCAs/VerifyPeerCertificate 限鏈長但跑在標準驗證之後是縱深非省成本/SetSessionTicketKeys 輪替；http.Server 無獨立握手 timeout 欄位靠 ReadHeaderTimeout/ReadTimeout/WriteTimeout 最小正值當握手期限承 Day72，raw tls.Conn 用 SetDeadline；netutil.LimitListener 限總連線數只限總數不分 IP）、Java 控制（-Djdk.tls.rejectClientInitiatedRenegotiation=true；Tomcat SSLHostConfig protocols=TLSv1.3/certificateVerification=required/certificateVerificationDepth 壓低鏈深/sessionCacheSize·sessionTimeout resumption/connectionTimeout 涵蓋握手階段踢慢握手承 Day72/maxConnections≫maxThreads NIO；OCSP stapling 屬性依賴 OpenSSL tc-native；raw SSLEngine 無內建握手 timeout 要 Netty SslHandler.setHandshakeTimeoutMillis）、實務決策清單（收斂 TLS 1.3、握手 timeout、resumption+輪替金鑰、mTLS 限深+避免同步撤銷、握手洪水擋便宜前層+TLS 卸載給硬體加速 LB、監控握手層指標新握手速率/full vs resumption 比例/握手失敗率/TLS CPU 佔比/renegotiation 嘗試承 Day16）、Code Review/維運 checklist、testssl.sh/thc-ssl-dos/慢握手/長鏈/resumption 回歸測試 | TLS 憑證驗證失誤 client 端 MITM InsecureSkipVerify/TrustManager（Day75）、HTTP/3 QUIC 握手與 UDP 放大/0-RTT replay、session ticket 金鑰輪替與 forward secrecy 落地、憑證釘選 pinning 取捨、post-quantum TLS 握手成本 |
| 75 | `content/資訊安全/Day75_TLS_Certificate_Verification_MITM.md` | TLS 憑證驗證失誤與 MITM（後端作為 TLS client） | 延伸（承 Day19 TLS × Day74 mTLS，交叉 Day10/Day39/Day26/Day16/Day15） | 不重講 Day19 TLS 基礎與 Day74 server 端握手，聚焦後端「主動對外發 TLS 請求」時 client 端驗證破口、核心心智模型：TLS client 驗證＝①鏈驗證 AND ②主機名驗證兩件獨立事少一件身分保證歸零（只做①不做②→攻擊者用自己合法網域憑證冒充目標；①②都不做→任何自簽憑證即可 MITM）、「本機測得過」陷阱動機學（自簽/內部 CA 憑證→握手噴 unable to find valid certification path / certificate signed by unknown authority→時間壓力下關驗證→關掉的正是會噴錯那道防線→安靜地永遠成功含跟攻擊者通話）、Go 反例 InsecureSkipVerify=true（①②一起關的總開關）vs 正解 x509.SystemCertPool 疊加 AppendCertsFromPEM 內部 CA 設 RootCAs 只多信任不關驗證/ServerName 留空由 http.Transport 依 URL host 自動帶入做②別手動寫死/用專屬 http.Client 非全域改 DefaultTransport、進階 VerifyConnection 需自己把①鏈驗證 x509.VerifyOptions{Roots,Intermediates,DNSName:cs.ServerName}＋②主機名都做回來、Java 反例全信任 X509TrustManager（checkServerTrusted 空實作關①）＋HostnameVerifier return true/setDefaultHostnameVerifier（關②）vs 正解 KeyStore 載內部 CA truststore＋TrustManagerFactory.getDefaultAlgorithm 走標準 PKIX＋SSLContext.init(null,tmf,null)＋Java 11+ HttpClient 預設做 HTTPS 端點識別別關、raw SSLSocket/SSLEngine 預設不做主機名驗證要顯式 SSLParameters.setEndpointIdentificationAlgorithm("HTTPS")經典沉默陷阱、pinning 取捨（釘 SPKI 公鑰雜湊非整張憑證讓換發沿用同金鑰 pin 不失效、必有 backup pin 否則輪替把自己鎖死＝HPKP 廢棄主因、backend-to-backend 比瀏覽器可行因掌握兩端與輪替節奏、疊加在①②之上非取代不可 InsecureSkipVerify＋只 pin）、交叉 Day10 SSRF/Day39 DNS rebinding（關驗證放大 SSRF：TLS 本是最後一道「連到誰」身分裁決；rebind＋關驗證絕配，正確驗憑證是免費一道縱深）、常見誤區表（加密≠驗身分/內網也要驗/pin 不能取代①②/只關②也致命/測試環境關驗證會被複製到生產/raw SSLSocket 預設不驗主機名/別全域塞 DefaultTransport）、Code Review grep（InsecureSkipVerify:true、空 checkServerTrusted、HostnameVerifier return true、setEndpointIdentificationAlgorithm(null)）與測試（自簽 server 斷言握手失敗＝守門員、主機名不符斷言失敗、內部 CA 正例斷言成功、CI grep gate 承 Day18、驗證失敗大聲失敗＋告警承 Day16） | 憑證釘選 pinning 落地實作（Go VerifyConnection 比對 RawSubjectPublicKeyInfo SHA-256、Java 委派式 TrustManager PKIX 後加 pin、backup pin 與輪替流程、把自己鎖死營運風險）Day76、憑證/私鑰生命週期與到期停機/ACME 自動換發、Certificate Transparency 憑證誤發偵測、複合/委派 TrustManager 同時信任公開＋內部 CA、mTLS client 端金鑰保管 |
| 76 | `content/資訊安全/Day76_Certificate_Pinning_Implementation.md` | 憑證釘選（Certificate Pinning）落地實作 | 延伸（承 Day75，交叉 Day19/Day15/Day16/Day32/Day18） | 不重講 Day75 憑證驗證基礎（①鏈驗證 AND ②主機名驗證/InsecureSkipVerify 災難/內部 CA truststore）也不重講 Day75 第五節 pinning 取捨論述（為何釘 SPKI/為何要 backup pin/HPKP 廢棄/backend-to-backend 可行），聚焦實作與營運、pin 定義（pin＝SHA-256(SubjectPublicKeyInfo DER)再 base64＝RFC 7469 pin-sha256 格式；SPKI 結構 algorithm+subjectPublicKey 不含序號/效期/簽發者/簽章故換發沿用同金鑰 pin 不變）、openssl 三種算法（A s_client -connect+-servername SNI 帶對否則抓到別的 vhost｜B x509 -pubkey -noout 從憑證檔算供 CI｜C pkey -in backup.key -pubout 從「還沒簽成憑證的私鑰」算＝backup pin 可行的支點，pin 整張憑證做不到）、三個實作前決策（pin leaf 最窄適合自家掌握 vs pin intermediate 對端 leaf 換發不影響適合第三方 vs pin root≈自訂 truststore 用 Day75 做法即可別拿 pinning 做；pin set 至少 2 個；匹配規則鏈上任一張命中＝HPKP 語意但信任面等於 pin set 裡最寬那個）、鐵律「疊加不是取代」判準只問「內建驗證還在跑嗎」（只 pin 不驗鏈＝過期/被撤銷憑證照收，pin 回答「是不是這把金鑰」①②回答「憑證是否有效/是否簽給這 host」是三個不同問題）、Go 正解不碰 InsecureSkipVerify 只掛 VerifyPeerCertificate（標準驗證之後才呼叫、verifiedChains 為空＝InsecureSkipVerify 被打開必須拒絕當防呆、比對 verifiedChains 非 PeerCertificates 因後者是「對方說的」、x509.Certificate.RawSubjectPublicKeyInfo 直接給 DER 不用 MarshalPKIXPublicKey 繞、VerifyConnection 亦可但要對 VerifiedChains 比、pin 是公開資訊不需 constant-time 比較與 Day32 區隔、專屬 client 非 DefaultTransport）＋printPins 觀測小工具（tls.Dial 帶 ServerName 印鏈上每張 pin 與 NotAfter）、Java 正解委派式 X509ExtendedTrustManager（checkServerTrusted 第一行 delegate.checkServerTrusted 再 checkPins 讓「被註解掉」最顯眼、六個多載全委派、getPublicKey().getEncoded() 對 X.509 就是 SPKI DER、建構子對 pins.size()<2 防呆丟例外、MessageDigest 非 thread-safe、TrustManagerFactory.init((KeyStore)null) 取 JDK 預設 delegate 再包）、坑：實作陽春 X509TrustManager 而非 X509ExtendedTrustManager 會讓②端點識別落入 JSSE wrapper 包裝細節模糊地帶（JDK 會包 AbstractTrustManagerWrapper 補做但屬實作細節隨版本演進不該賭）、Java 1.8 無 java.net.http.HttpClient 改 HttpsURLConnection.setSSLSocketFactory 且 raw SSLSocket 仍要 setEndpointIdentificationAlgorithm("HTTPS")（pinning 不補②）、真正難點輪替 SOP 順序性（pin set 先擴張{A,B}→{A,B,C}→等所有 client 部署完→server 才換憑證 A→B→最後才收縮{B,C}；反過來做則 server「一秒生效」vs client「一週部署」時間差＝停機時間；加 pin 可慢慢來超集合不壞事、移除 pin 與換憑證必須等 client 就緒）、SOP 落地三要求（pin set 放組態可不重新編譯就更新因緊急換金鑰沒有幾天但寫入權限＝解除 pinning 權限要 audit＋告警承 Day15/16、空 pin set 是「設定壞了」非「不檢查」、report-only 模式導入與每次變更先觀察不命中率但 verifiedChains 為空的檢查永不受 report-only 影響、kill switch 兩難：只降級 pinning 一層①②永遠強制最壞退回一般 HTTPS 非 InsecureSkipVerify＋雙人審核）、實務衝突 ACME 自動換發預設換金鑰＝pin 每 60 天自動失效（三條路 A key reuse 如 certbot --reuse-key 但副作用是私鑰永不換要自排輪替、B 改 pin intermediate、C pin 更新自動化＝SOP 自動化版；CDN/托管 LB 後面的第三方對端憑證非其自有別 pin leaf 或根本別 pin）、常見誤區表（pin 了就可 InsecureSkipVerify/只放一個 pin/先換憑證再更新 pin/硬編 pin 讓自己救不了/pin set 當普通設定/空 pin set 當未啟用/以為要 constant-time/ACME 一勞永逸/pin 第三方 leaf/pin 不命中≠一定 MITM 更常是對端換金鑰要能區分營運事故與資安事件/Java 用陽春 X509TrustManager/pin root≈truststore）、Code Review checklist 分四區（程式碼疊加性/pin set 不鎖死/輪替 SOP 順序/監控）與測試（pin 命中正例、pin 不命中反例＝合法 CA 簽+主機名正確但別把金鑰須失敗＝模擬 CA 誤發＝pinning 存在證明、backup pin 測試證明救得了自己、疊加性測試 pin 命中但憑證過期/主機名不符/不受信任 CA 須失敗＝專抓「寫成取代」、CI pin 一致性檢查對測試環境跑 openssl 管線斷言在 pin set 裡、憑證 NotAfter 到期預警） | Certificate Transparency CT log 與憑證誤發偵測／CAA record 事前預防（Day77）、憑證/私鑰生命週期與 ACME 自動換發架構、複合/委派 TrustManager 同時信任公開＋內部 CA、mTLS server 端 pin client 憑證、pin set 組態發佈管線與雙人審核落地、行動端 pinning 與後端策略差異 |
| 77 | `content/資訊安全/Day77_Certificate_Transparency_CAA.md` | Certificate Transparency（CT）與憑證誤發偵測／CAA | 入門（新主題，承 Day76 預告，交叉 Day75/Day76/Day16/Day18/Day15/Day10/Day72/Day19） | 不重講 Day75 憑證驗證（①②）與 Day76 pinning 實作，視角從「TLS client 要不要信對方」換成「網域擁有者怎麼知道有誰在冒用我」、問題根源 CA 是 PKI 單點失效（信任幾十上百家 CA，任一家都能簽出你網域的合法憑證且 Day75 ①②完整通過；DigiNotar 2011 簽出 500+ 張假 *.google.com 用於伊朗 MITM 後被移除信任並破產、Symantec 2015-2017 誤發最終被 Chrome/Firefox 分階段撤銷根信任而推動力正是 CT 讓大家看得到）、pinning vs CT 五維對照表（誰做：client vs 網域擁有者｜時機：握手當下 vs 事後分鐘小時級｜效果：阻擋 vs 知道｜代價：綁死輪替節奏 vs 幾乎為零｜涵蓋：只有你控制的 client vs 全世界所有人看到的憑證＝關鍵差異因攻擊者拿假憑證 MITM 你的「使用者」時 pinning 完全幫不上）、CT 機制 RFC 6962 核心「不阻止誤發只讓誤發藏不住」、三方角色（Log＝append-only Merkle Tree 靠密碼學非承諾任何人可用 Merkle proof 驗證歷史沒被改／Monitor＝你要扮演的角色定期拉找自己網域／Auditor＝驗證 log 沒食言由瀏覽器與研究者做後端一般不需自己扮演）、SCT＝log 給 CA 的收據（承諾 MMD 24h 內公開、由 log 私鑰簽名，執行力來源不是規定 CA 要 log 而是「沒 SCT 的憑證 Chrome/Apple 不收」）、SCT 三種送達方式（A 嵌在憑證裡 X.509 extension OID 1.3.6.1.4.1.11129.2.4.2 最常見 server 零設定｜B TLS 握手擴充｜C OCSP stapling 夾帶）、precertificate 解雞蛋問題（poison extension OID 1.3.6.1.4.1.11129.2.4.3 critical 且無 client 認得故必被 reject＝進得了 log 卻永遠不能當憑證用；副作用同一張憑證在 log 裡兩筆 precert+正式憑證 → monitor 必須用 serial+issuer 去重否則雜訊乘二）、後端結論 SCT 是 CA 與瀏覽器之間的事你什麼都不用做只要當 monitor、核心對照 CT 事後偵測 vs CAA 事前預防（CAA RFC 8659 DNS 記錄 flag 0/128 critical、issue 授權一般憑證、issuewild 只要存在就完全接管萬用字元授權讓 issue 對萬用字元失效、iodef 違規通報管道、值 ";" 代表不授權任何 CA；執行力來自 CA/B Forum 2017-09 起強制所有公開信任 CA 簽發前檢查 CAA 否則違反 BR 有被踢出根計畫風險；dig CAA example.com +short；樹狀爬升 api.staging.example.com→staging.example.com→example.com 取第一個找得到的層級就停 → 只要 apex 設一組整棵子網域樹受保護，但子網域自己設會蓋掉 apex＝最常見自我 DoS 讓該子網域另一家 CA 自動換發全簽不出來）、兩者界線（路徑 A 向合法 CA 騙簽→CAA 擋得住；路徑 B CA 被入侵/內部作惡 DigiNotar 模式→CAA 完全無效因被入侵的 CA 不會為難自己，但憑證仍必須進 CT log 否則瀏覽器不收→CT 是唯一防線；CAA 靠「CA 願意遵守規則」防君子、CT 靠「憑證想被接受就必須公開」防小人；CAA 是門鎖 CT 是監視器沒人裝了鎖就拆監視器）、資料來源選擇（自建 get-entries monitor 每天數百萬筆是大工程絕大多數團隊不該做；crt.sh JSON 非官方介面快速起步但免費公共服務無 SLA 會限速格式會變；Cert Spotter API 有正式文件免費額度+付費；商用 CT 監控/CA 附帶）、Go 實作 fetchCrtSh（?q=%25.<domain>&output=json 用 url.Values 編碼不手拼承 Day67/68、User-Agent 表明身分、io.LimitReader 32MB 上限承 Day71、http.Client Timeout 承 Day72、非 200 與 JSON 解析失敗都往上丟當事件不能靜默＝monitor 瞎了、struct 只映射用得到欄位因非官方介面多映射一欄多一個會壞的地方）、Audit 三步核心（1 serial+issuer 去重 2 第一關便宜 issuer 片段包含比對 99% 自家 ACME 換發在此過濾 3 第一關沒過才付貴的成本抓 PEM ?d=<id> 算 SPKI）、issuer 用片段而非完整字串比對（CA 會換中繼 R3→R10→R11 綁死完整字串＝每次換中繼一輪假警報兩次後大家開始無視）、SPKI 分流把營運事故與資安事件分開（SPKI 已知＝換 CA/CA 換中繼沒更新組態＝營運事故；未知 issuer+未知金鑰＝疑似誤發＝資安事件立即升級；抓不到 PEM 不能當沒事要當「查不出來」報出去）、spkiPinFromCert 用與 Day76 完全相同的 RawSubjectPublicKeyInfo 計算故 pin set 組態可兩套機制共用、Java 實作 CtMonitor（java.net.http.HttpClient connectTimeout 承 Day72、followRedirects NEVER 承 Day10、非 200 丟例外絕不 catch 掉當今天沒事、CertificateFactory 從 PEM 算 getPublicKey().getEncoded() SPKI 承 Day76、ScheduledExecutorService/Spring @Scheduled 每小時、成功也要記 ct_monitor_success 指標）、Java 1.8 提醒（無 java.net.http.HttpClient 改 HttpsURLConnection 但別關憑證驗證承 Day75 否則查誤發卻自己 InsecureSkipVerify 太諷刺、record 是 16+）、CI CAA 斷言 script（dig +short CAA、雙向檢查：預期的 CA 都在＋沒有預期外的 CA 被授權因只檢查前者則別人偷加一行 issue 你不會知道而那正是把鎖打開的方法、去掉 RFC 8657 參數 accounturi/validationmethods、iodef 存在檢查、承 Day18 讓它在 CI 紅不要在生產紅、DNS 記錄很容易在某次調整 DNS 時被誤刪）、雜訊治理是成敗關鍵（naive 有新憑證就告警第一週就會被關掉）、五個雜訊來源與處理（自家 ACME 換發量最大→issuer 白名單+SPKI｜precert+正式憑證雙筆→serial+issuer 去重否則乘二｜子網域萬用憑證→白名單以 issuer+SPKI 為主別用網域字串精確比對因萬用憑證 CN 跟子網域清單本來就對不起來｜CDN/托管服務代簽用自己帳號幫你簽 issuer 他們選節奏你管不到→把服務商的 CA 加白名單，正是 Day76「CDN 後面別 pin」的同一現象｜內部 CA 不在 CT 裡→這是盲區不是雜訊）、最重要盲區 CT 看不到內部 CA（CT 執行力來自瀏覽器不收沒 SCT 的憑證而內部 CA 本就不在根計畫裡此壓力不存在→內部 PKI 完全沒有 CT 保護、內部 CA 被入侵沒有公開帳本記錄、對應解法是 Day76 pinning＋內部 CA 簽發稽核日誌承 Day16、別把「CT 沒告警」讀成「沒人冒充我」）、CT×pinning 分工矩陣（對外服務公開 CA：事前 CAA／事後 CT／阻擋 pinning 若可行；內部服務內部 CA：事前簽發流程管控／事後 CT 看不到＝盲區／阻擋 pinning 是主場）、偵測型控制通病「monitor 掛了與沒有誤發在儀表板上長得一模一樣」（ct_monitor_error 要指標告警、ct_monitor_success 也要指標並設「N 小時內沒成功執行」告警因「沒有成功」跟「有失敗」是兩件事 process 沒被排到連 error 都不會有、定期陽性測試對已知會命中目標跑斷言真的會叫＝monitor 的存在證明等同 Day76「pin 不命中反例是 pinning 存在證明」）、常見誤區表（CT 不阻止任何事是監視器不是門鎖/CAA 只約束守規矩 CA 被入侵時無效/兩者非二選一擋不同路徑/pinning 做了仍需 CT 因保護不到用瀏覽器的使用者/CT 沒告警≠沒人冒充因內部 CA 盲區/有新憑證就告警會被關掉雜訊治理是主線/crt.sh 是別人免費服務要 timeout+限速+失敗告警/issuer 完整字串比對每次換中繼假警報/查詢失敗 skip 掉等於瞎了/CT log 一筆一張憑證是錯的 precert 各一筆/SCT 不用 server 端設定/每個子網域都設 CAA 會蓋掉 apex 自我 DoS/發現誤發撤銷掉就沒事＝Day78 主題）、Code Review checklist 四區（CAA 事前預防/CT monitor 事後偵測/告警品質承 Day16/範圍認知）與測試（陽性測試＝monitor 存在證明、去重測試同 serial precert+正式憑證只產生一筆、issuer 片段比對測試 R3→R11 不告警、SPKI 分流測試未知 issuer+已知 SPKI 歸類營運事故、失敗路徑測試 429/非 JSON 要丟錯告警而非回空清單假裝沒事、CI CAA 斷言排程） | 憑證撤銷 CRL/OCSP soft-fail/OCSP stapling/短效憑證（Day78）、自建 CT monitor 對接 RFC 6962 get-entries、CT v2（RFC 9162）與 SCT 驗證細節、內部 PKI 簽發稽核與私有 CA 治理、CAA accounturi/validationmethods（RFC 8657）綁定 ACME 帳號、iodef 報告處理流程 |
| 78 | `content/資訊安全/Day78_Certificate_Revocation_OCSP_Soft_Fail.md` | 憑證撤銷（Revocation）：CRL / OCSP / soft-fail / 短效憑證 | 入門（新主題，承 Day77 預告，交叉 Day75/Day76/Day74/Day10/Day16/Day15/Day18/Day19） | 不重講 Day75 ①②驗證、Day76 pinning 實作、Day77 CT/CAA，聚焦「憑證本來有效但我後悔了」這第三個獨立問題、根因（憑證＝CA 簽的一句離線就能驗的話，離線可驗是 PKI 撐起網際網路的優點但代價是無法收回已說出口的話只能旁邊貼公告然後祈禱有人看；撤銷的核心問題只有一個＝怎麼讓離線驗證流程額外查一個線上狀態、而且查不到怎麼辦）、第一代 CRL（RFC 5280、CRLDistributionPoints、openssl x509 -text 看發佈點；死法一體積全量與待驗憑證無關為 20 bytes 答案下載 5MB 數十萬筆序號 Heartbleed 2014 讓 CRL 一夜暴漲反促使跳過、死法二 nextUpdate 更新週期數小時到七天＝撤銷生效可能等一週但撤銷場景恰好分秒必爭、死法三 抓 CRL 本身是對外呼叫會失敗於是又 soft-fail；內部 PKI 規模小仍可行）、第二代 OCSP（RFC 6960、AIA 欄位、openssl x509 -ocsp_uri / openssl ocsp -issuer -cert -url、good/revoked/unknown 三態、體積 MB→數百 bytes；問題一每次握手一次對外呼叫＝延遲疊加＋可用性反轉 CA responder 掛你就連不上對方 2016 GlobalSign 誤設造成全球假撤銷＝撤銷機制自己成故障來源、問題二 Day74 握手 DoS 放大點每握手一個對外呼叫＝你變成別人 DoS 上游 CA 的放大器然後被限速然後握手全掛、問題三 Day10 SSRF 教科書結構＝收對方遞來的憑證→讀出憑證裡的 URL→主動連過去（實務被 AIA 查詢時機與標準庫限制收住但心智模型要正確：開撤銷檢查＝驗證路徑多一個連憑證裡寫的 URL 的動作）、問題四隱私每查一次等於告訴 CA 你在連誰＝Let's Encrypt 關掉 OCSP 主因之一）、soft-fail 是壓垮一切的核心（hard-fail＝responder 抖動/咖啡廳 Wi-Fi/公司 proxy 擋 OCSP 就全站打不開使用者怪你不怪 CA；soft-fail＝一切正常沒人抱怨；所有主流實作都選 soft-fail；殺手級推論＝你要防的是攻擊者拿被撤銷憑證 MITM，但能 MITM 的攻擊者本來就在網路路徑上，在路徑上就能把 OCSP 查詢丟掉（不回應/TCP reset/DNS 給錯答案）→查不到→soft-fail→放行 ⇒ soft-fail 的撤銷檢查恰好對唯一會用到它的那種攻擊者完全無效＝Adam Langley「只在你不需要時才有用的安全帶」；不是實作 bug 是邏輯死結；也不能改 hard-fail 因為那把 CA 每次抖動變成你的停機 ⇒ 卡十幾年沒解直到大家決定繞過）、OCSP stapling（RFC 6066 Certificate Status Request、由 server 自己去問把 CA 簽名回應夾在握手裡；一次解掉延遲/CA 負載每 server 每幾小時/隱私；回應是 CA 私鑰簽的 server 無法偽造、thisUpdate/nextUpdate 讓 server 無法重播舊 good；Day77 SCT 送達方式 C 的細節在此收；但沒解 soft-fail＝stapling 是 server 自願夾帶的，攻擊者只要單純不夾 client 就退回無撤銷資訊→soft-fail→放行＝攻擊者什麼都不用做；補洞只有 Must-Staple RFC 7633 TLS Feature 擴充把 soft-fail 對這張憑證變 hard-fail，技術上正確但把「stapling 設定壞掉」升級成「全站掛掉」故幾乎沒人用，Let's Encrypt 2025 已停止支援）、Go 實作（crypto/tls 預設完全不做撤銷檢查一行都沒有＝設計選擇非 bug 因 soft-fail 沒安全價值就不假裝有；x509.Certificate.Verify 只做 Day75 ①②無撤銷語意；stapled 回應放在 tls.ConnectionState.OCSPResponse 理不理是你的事；Day76 讀者必踩坑＝VerifyPeerCertificate 簽名只有 rawCerts/verifiedChains 拿不到 OCSPResponse 必須改用 VerifyConnection 收整個 ConnectionState；checkStapledOCSP 五要點＝VerifiedChains 為空當 InsecureSkipVerify 防呆拒絕承 Day76／沒夾時放行是刻意選擇要寫註解／鏈長<2 無 issuer 無法驗簽／ocsp.ParseResponseForCert 而非 ParseResponse 因前者驗簽＋確認回應講的是這張憑證防止拿別張 good 搪塞／自己比 ThisUpdate·NextUpdate 因 x/crypto/ocsp 不判時效否則三年前 good 可無限重播；Revoked 要告警承 Day16；疊在 Day76 checkPins 旁邊、專屬 client 不動 DefaultTransport、Timeout 承 Day72；主動查 OCSP 用 ocsp.CreateRequest 可行但答案幾乎都是別做）、Java 實作（三開關：-Dcom.sun.net.ssl.checkRevocation 預設 false＋-Djdk.tls.client.enableStatusRequestExtension JDK9 起預設 true ⇒ 標準情境是你已經在要 stapled 回應然後把它丟掉；兩者要一起才有意義前者要後者檢查；PKIXRevocationChecker Java 8 起提供才是精細控制的正解＝CertPathValidator.getInstance("PKIX").getRevocationChecker()＋setOptions＋PKIXBuilderParameters.setRevocationEnabled(true)（沒這行 addCertPathChecker 不生效）＋addCertPathChecker＋TrustManagerFactory.getInstance("PKIX").init(new CertPathTrustManagerParameters(params))；四 Option 逐一＝SOFT_FAIL 不加是 hard-fail 加了查不到就放行例外被吞進 getSoftFailExceptions()／NO_FALLBACK 建議加否則 OCSP 失敗退回抓 CRL＝一次握手兩種對外呼叫延遲與失敗面加倍／ONLY_END_ENTITY 建議加否則鏈多深就多幾個呼叫承 Day74／PREFER_CRLS 一般不加內部 PKI 可考慮；SOFT_FAIL 是 Java PKI API 最誠實也最陷阱的設計＝誠實在強迫你自己選且名字就叫軟性失敗，陷阱在不加＝CA 抖動害你停機壓測沒問題上線第三週凌晨全掛／加了＝回到死結還多一個架構圖上寫「已啟用撤銷檢查」的假安心比不做更糟；真加了至少把 getSoftFailExceptions() 撈出來 log.warn＋metrics 計數承 Day16 否則連「撤銷檢查從來沒成功過」都不知道＝Day77 偵測器自己壞掉；Java 1.8 有 PKIXRevocationChecker 但無 java.net.http.HttpClient 要走 HttpsURLConnection.setSSLSocketFactory 且 raw SSLSocket 仍要 setEndpointIdentificationAlgorithm("HTTPS") 因撤銷檢查補不了②）、瀏覽器早已放棄 OCSP 改推播（Chrome CRLSets＝Google 彙整「重要的」撤銷經瀏覽器更新機制推送但非全量一般網站憑證撤銷不在裡面／Firefox CRLite＝Bloom filter 串把全量壓成幾百 KB 週期推送技術上漂亮涵蓋完整；解掉死結因握手時零對外呼叫＝不能被擋無延遲無隱私問題答案已在本機；但後端抄不了需要彙整全球 CT/CRL 並推給幾億 client 的基礎設施，沒有人幫你的 Java 服務維護 CRLite；認知校正＝「瀏覽器有在檢查撤銷」與「你的後端有在檢查撤銷」是完全不同兩件事且前者方法你抄不了）、產業答案＝不修撤銷改讓憑證活得夠短（邏輯：撤銷是問題因為剩餘效期長，只剩 3 天則撤銷與等它過期差別就 3 天；而「過期」不需線上查詢不會被擋不會 soft-fail＝寫在憑證裡是 Day75 ①的一部分離線就驗得掉＝繞回第一節「離線可驗」的優點當解法；已發生的事實：Let's Encrypt 2025-08-06 OCSP 服務正式終止非降級非棄用警告是關掉且 2025-05 起新簽憑證不再含 OCSP URL／6 天短效憑證 shortlived profile 首張 2025-02 簽出 2026-01-15 一般可用實務建議每 2.5 天換發／CA/B Forum Ballot SC-081v3 2025-04-11 通過排定 2026-03-15 起最長 200 天（今天已生效）→2027-03-15 100 天→2029-03-15 47 天，網域驗證資料重用期同步收到 2029 剩 10 天；對後端真正意義＝責任轉移非輕鬆＝撤銷從「你依賴 CA 提供但其實不管用的機制」變成「你自己的換發管線是否可靠」＝不用煩惱 OCSP 但 ACME 管線從此是生產基礎設施掛三天你就掛；2026-05-08 Let's Encrypt 簽發中斷是預演＝效期越短對管線中斷容忍窗口越小 6 天憑證每 2.5 天換只剩幾小時緩衝）、CT 叫了的 runbook（先做 Day77 分流＝SPKI 已知＝營運事故九成告警到此結束／SPKI 未知＋issuer 未知＝疑似誤發往下；申請撤銷要做但定位是（a）進入 CRLSets/CRLite 推播保護瀏覽器使用者（b）啟動 CA 的 BR 時限稽核程序留下紀錄＝Symantec 被摘根信任的路徑承 Day77＝你是在推動生態不是在關掉那張憑證；絕不能當止血手段因提報到失效中間有推播週期＋soft-fail＋一堆根本不查撤銷的 client 包括你自己家所有 Go 服務；真正止血在別處＝確認私鑰沒外洩並輪替承 Day15／更新 CAA 事前阻斷承 Day77／自家 client 上 pinning 承 Day76＝唯一真的會擋下來且不看撤銷／升級告警等級保留證據究責承 Day16／縮短自己憑證效期；重新解讀 Day76「只 pin 不驗鏈連被撤銷憑證都照收」其實是廢話＝Go 標準庫本來就不查撤銷「被撤銷憑證照收」是預設行為跟有沒有 pin 無關 ⇒ pinning 的價值從來不在撤銷而在它問的是一個不需任何線上查詢的問題「是不是這把金鑰」＝攻擊者擋不掉一個不存在的查詢）、常見誤區表（發現誤發撤銷掉就沒事/我的 Go 服務會擋被撤銷憑證/Java 預設有做撤銷檢查/開 SOFT_FAIL 比較安全至少有檢查/那就用 hard-fail/stapling 解決了 soft-fail/OCSP 是現代做法 CRL 是老東西＝兩個都在退場/瀏覽器有查 OCSP 所以機制還行/開撤銷檢查只是多一點延遲/鏈上每張都查比較嚴謹/短效憑證只是換發頻繁一點/憑證效期照習慣簽一年＝已違反 BR/撤銷檢查可以補主機名驗證＝三個不同問題）、Code Review checklist 五區（認知/Go stapled 加固/Java 撤銷開關/短效憑證與換發＝真正主線/內部 PKI）與測試（撤銷檢查存在證明＝連 revoked.badssl.com 斷言連不上，測不過就是裝飾品，多數人第一次跑會驚訝 Go 預設 client 連得上／soft-fail 行為測試＝擋掉 responder 網路觀察放行或拒絕＝這才是你真實的安全姿態不是設定檔寫的那個／stapling 缺席測試／時效重播測試 NextUpdate 過期須拒絕／hard-fail 爆炸半徑測試量有多少功能會掛／CI 憑證效期斷言 openssl x509 -noout -enddate 超門檻 CI 紅承 Day18／換發演練因「從來沒失敗過」與「從來沒被驗證過」長得一樣承 Day77） | ACME 自動換發管線與四階段/HTTP-01·DNS-01·TLS-ALPN-01 challenge 安全取捨/RFC 8657 accounturi·validationmethods 收緊 CAA/autocert 的坑/單一 CA 依賴與 backup CA（Day79）、內部 PKI 撤銷與私有 CA CRL 發佈、CRLite/Bloom filter 資料結構深入、mTLS server 端 client 憑證撤銷（短效 workload 憑證/SPIFFE）、憑證 reload 不重啟的實作、Must-Staple 導入的停機風險評估 |
| 79 | `content/資訊安全/Day79_ACME_Automated_Certificate_Renewal.md` | ACME 自動換發管線（RFC 8555） | 入門（新主題，承 Day78 預告，交叉 Day77/Day76/Day75/Day74/Day18/Day16/Day15/Day10/Day07） | 不重講 Day78 撤銷機制與 Day76/77 pinning/CT，聚焦「短效憑證把換發變成生產基礎設施」、ACME 解的問題＝讓機器自動證明控制網域然後自動拿憑證（取代人工填表/驗證信，短效憑證每 2.5 天換 146 次/年不可能派人）、四階段心智模型（帳號 Account＝帳號金鑰≠憑證金鑰兩者分開／order 下單列出要完成哪些 authorization／challenge 網域驗證是核心／finalize+download 送 CSR 帶憑證金鑰公鑰取憑證；換發無「續約」動作＝把後三階段重跑每次都要重新證明控制權）、三種 challenge 安全取捨（HTTP-01 在 web root 放 /.well-known/acme-challenge/token 檔 CA 走明文 80 埠來抓且會跟跳轉＝Day10/20/67 心智模型／需 80 埠對公網開／不能簽 wildcard／權限窄爆炸半徑小｜TLS-ALPN-01 用 acme-tls/1 ALPN 在 443 握手回自簽臨時憑證帶 extension／不需開 80／一樣不能 wildcard／需能在握手層動態回應 LB 終結 TLS 時插不上手／權限窄｜DNS-01 在 _acme-challenge 放 TXT／是 wildcard 唯一選項也是內網/防火牆後服務唯一能用的／但要整個 zone 寫入權而 DNS 正是 Day77 CAA 那把鎖所在地誰能寫 DNS 誰能拆 CAA 叫任何 CA 簽你＝爆炸半徑最大最危險）、選擇原則能 HTTP-01/TLS-ALPN-01 就別 DNS-01、DNS-01 權限解法 CNAME 委派把 _acme-challenge CNAME 指到權限隔離的小 zone 換發程式只拿小 zone 寫入權（承 Day07 最小權限收爆炸半徑）、RFC 8657 把 CAA 從 Day77「哪家 CA」收緊到「哪個帳號哪種方法」（accounturi＝只有特定 ACME 帳號能簽擋攻擊者用同一家 CA 開別帳號、validationmethods＝只允許指定 challenge 方法擋攻擊者切換到你沒在用的方法；幾乎零成本應該設；CI 斷言 dig CAA grep accounturi/validationmethods 承 Day18 DNS 記錄易被誤刪）、Go autocert 三個坑（正解掛 m.TLSConfig() 的 GetCertificate＋HTTPHandler；坑一 HostPolicy 不設＝任意 SNI 都幫申請到被 Let's Encrypt 限速自我 DoS＝必填非選配用 HostWhitelist；坑二 預設 Cache 不落磁碟或本機 DirCache 在多副本下每台各簽一份撞每週限制＝多副本必須共享 Cache 自己實作 Cache 介面接 Redis/DB/S3/Secret；坑三不適合放 LB 後面因 CA 驗證流量要打到握著 challenge 狀態那台但 LB 分流到任意後端且通常自己終結 TLS 握手到不了 Go 服務＝autocert 假設服務自己直接面對公網終結 TLS）、Java 沒有標準庫等價物且非缺陷（JDK 無 ACME client；路徑 A acme4j 在 JVM 內跑但要自理帳號金鑰/challenge 落地/儲存 reload＝等於重寫 certbot 骨架；路徑 B 最常見把換發移出 JVM＝cert-manager sidecar 塞 Secret 服務只讀 Secret／前置 LB/ingress 終結 TLS 並換發 Java 跑純 HTTP；路徑 C certbot+deploy hook reload；架構判斷 TLS 在哪終結換發就在哪做，Java 常躲 LB 後面 TLS 不在 JVM 終結所以不需 autocert 這不是語言優劣是部署形態差異）、維運主線（觸發時機不是快到期才換而是一拿到就開始換把緩衝拉到最大＝緩衝＝能容忍連續換發失敗多久 6 天憑證設剩 2 天換是自殺 certbot 三分之二預設是為 90 天設計短效要更激進；換發失敗告警要分兩種缺一不可＝換發失敗有 error＋太久沒成功換發沒 success 因排程沒跑連 error 都不會有＝Day77 偵測器自壞老病 renewal_success 當指標設 N 小時沒成功告警；到期預警門檻用剩餘效期比例非固定天數 6 天憑證設剩 7 天告警永不觸發；憑證 reload 不重啟 Go 靠每次握手都呼叫的 GetCertificate 用 mutex 保護可替換 tls.Certificate reload 失敗保留舊憑證別裸奔／Java JSSE KeyManager 初始化抓死憑證要自訂可換 X509KeyManager 包裝或重建 SSLContext 或交給 LB／reload 失敗保留舊憑證別握手全掛；單一 CA 依賴 2026-05-08 Let's Encrypt 簽發中斷是短效憑證預演效期越短對 CA 可用性依賴越硬 backup CA 值不值得看可用性要求＝支持論據消單點 反對論據多一套帳號金鑰承 Day15+多一組 CAA 授權放寬 Day77 鎖+多一條沒測過的換發路徑＝Day77 從來沒驗證過；務實建議先把換發緩衝拉大+失敗告警+reload 做好能扛多數 CA 抖動沒演練過的 backup 跟沒有一樣甚至更糟給假安心）、常見誤區表（ACME 有續約動作＝沒有換發重跑後三階段／三種 challenge 差不多隨便選／DNS-01 方便就都用＝權限最大／CAA 寫 CA 名就安全＝攻擊者能開別帳號要 RFC 8657／autocert 掛上就好＝三個坑／Java 沒 autocert 是缺陷＝不是常躲 LB 後面／快到期才換省事＝短效時代自殺／換發沒告警＝正常＝沒在跑跟沒失敗長一樣／到期預警設固定天數／換憑證重啟一下就好／用一家 CA 就夠）、Code Review/維運 checklist 六區（challenge 選擇與權限/CAA 承 Day77/autocert Go/觸發時機與緩衝/告警承 Day16·77/reload 與 CA 依賴）與測試（換發演練最重要主動逼近到期驗證真的會換會 reload＝從來沒失敗跟從來沒被驗證長一樣承 Day77/78／challenge 失敗演練 DNS API 回錯或擋 80 埠斷言失敗且告警非靜默卡住／太久沒換告警測試停掉 cron 斷言告警會叫＝換發版偵測器存在證明／reload 不重啟測試斷言 PID 不變換上新憑證再放壞檔斷言保留舊憑證／CAA CI 斷言 accounturi+validationmethods／限速防呆測試 autocert 假 SNI 斷言 HostPolicy 擋掉） | 內部服務身分與短效憑證 SPIFFE/SPIRE workload identity（Day80）、acme4j 在 JVM 內實作四階段細節、cert-manager Kubernetes 憑證管理落地、DNS-01 各 DNS 供應商 API 串接與 CNAME 委派自動化、憑證 reload 的 Java KeyManager 熱替換實作、backup CA 切換演練與多 CA 換發路徑、ACME External Account Binding（EAB）與商用 CA 綁定 |
| 80 | `content/資訊安全/Day80_SPIFFE_SPIRE_Workload_Identity.md` | 內部服務身分與短效憑證：SPIFFE / SPIRE、workload identity | 入門（新主題，承 Day79 預告，交叉 Day78/Day77/Day76/Day75/Day74/Day16/Day15/Day07） | 不重講 Day79 ACME/公開 CA 換發、也不重講 Day74 mTLS 握手 DoS，聚焦「內網服務之間的身分怎麼發/驗/自動輪替，且信任根從網域控制權換成 workload 證明」、SPIFFE 解的問題＝給每個 workload 一個不需自持祕密/可密碼學驗證/自動短效輪替的身分（ACME 問你控制哪個網域＝公網合理但內網 payment-service 沒對外網域且真正想確認的是「它真的是 payment-service 而非同機惡意程序」而非「它控制某網域」；不需自持祕密＝傳統塞 API key/憑證檔就是 Day15 難題 SPIFFE 讓身分由平台跑起來後證出來）、SPIFFE 是規格 SPIRE 是參考實作（同 ACME=RFC vs certbot/autocert=實作）、SPIFFE ID 格式 spiffe://trust-domain/workload-path（trust domain＝一整個信任邊界通常一組織/一叢集一個跨 domain 要 federation 不在範圍｜path＝命名規則常用 ns/sa 結構）、SPIFFE ID 塞在 X.509 憑證 SAN 的 URI 欄位（type=URI 非 DNS）＝直接決定驗證方式跟 Day75 不同（Day75 第②步比對 SAN DNS name vs host＝主機名驗證；SPIFFE 比對 SAN URI＝SPIFFE ID 完全不看主機名/IP）、裝著 SPIFFE ID 的短效憑證＝SVID（X509-SVID 主線、JWT-SVID 適合過不了 mTLS 的場景如 L7 gateway/雲端 API＝Day81）、openssl x509 grep SAN 看到 URI: 而非 DNS: 且 CN 通常空＝刻意逼你別用主機名判身分、身分根換掉是核心觀念（ACME 問「你是不是控制 example.com」你出示網域控制權；SPIRE 問「你是不是跑在我信任節點上/程序特徵符不符」＝attestation 分兩層缺一不可）、① node attestation 先確認跑 Agent 的機器可信（node attestor：AWS instance identity document/GCP·Azure metadata/K8s projected SA token/裸機 join token；Server 驗過發 Agent 一張代表節點的 SVID）② workload attestation 回答「來要憑證的這個程序憑什麼是它宣稱的服務而非同機惡意程序冒領」＝最精妙處：workload 呼叫 Workload API 不帶任何 token/祕密只是連上 Unix domain socket Agent 反過來觀察呼叫方核心層屬性（由 socket 對端拿 PID 再由 OS 查 UID/GID/K8s pod/SA/binary 路徑/selinux label＝workload attestor/selector）比對 registration entry 符合才發 SVID＝沒有祕密可偷冒充者 PID 特徵對不上就不發（正是 Day15 難題的解法，身分根不是「你持有什麼祕密」而是「你是什麼/跑在哪」偷不走）、Go 實作 go-spiffe v2（workloadapi.NewX509Source(ctx) 拿會背景自動更新的 X509Source＝socket 位址讀 SPIFFE_ENDPOINT_SOCKET 或 WithClientOptions(WithAddr("unix:///run/spire/agent.sock"))＝要當長生命週期物件重用內部維護到 Agent 的 stream SVID 快到期 Agent 主動推新的 source 自動換上＝短效卻不用寫換發排程換發被平台接管；source 同時是 KeyManager+TrustManager 故 client/server config 兩參數常傳同一 source；client 端 tlsconfig.MTLSClientConfig(source,source,tlsconfig.AuthorizeID(serverID))＝分水嶺在 authorizer：把標準主機名驗證關掉改成「鏈驗證 AND SVID 的 SPIFFE ID 符合 authorizer」host 只拿來建 TCP 身分裁決交給 SPIFFE ID＝對搬家/換 IP/mesh 導流免疫因身分綁 workload 不綁網路位置；authorizer 種類＝AuthorizeID 單一確切 ID 最窄/AuthorizeOneOf 清單任一/AuthorizeMemberOf 同 domain 任何 workload 很寬等於自己人就放行是 authN 非細粒度授權/AuthorizeAny 只驗鏈幾乎不該用；authorizer 只回答握手層要不要接受非業務授權 authZ 接 Day07；server 端 MTLSServerConfig + ListenAndServeTLS("","")＝憑證不從檔案從 source 動態且自動輪替用每次握手呼叫的 GetCertificate/GetConfigForClient＝Day79 reload 不重啟內建好了不用自寫 mutex 熱替換）、Java 實作 java-spiffe（io.spiffe:java-spiffe-core+java-spiffe-provider；DefaultX509Source.newSource() 長生命週期背景更新讀 SPIFFE_ENDPOINT_SOCKET；SpiffeSslContextFactory.getSslContext(SslContextOptions.builder().x509Source(source).acceptedSpiffeIdsSupplier(()->Set.of(SpiffeId.parse(...))).build()) 產 SSLContext 底層 TrustManager 換成驗 SPIFFE ID 版；跟 Day75 坑反過來＝千萬別再設 setEndpointIdentificationAlgorithm("HTTPS") 因 SVID SAN 是 URI 非 DNS 硬套會找不到 DNS name 握手失敗身分責任移交 SPIFFE TrustManager 的 acceptedSpiffeIds；心智轉換 Day75「主機名沒驗＝破口」SPIFFE「主機名根本不是身分 SPIFFE ID 才是」；守 Day75 鐵律別為跑起來自寫空 X509TrustManager 繞過驗證＝拆信任根比 InsecureSkipVerify 還糟；Java 1.8 無 java.net.http.HttpClient 走 HttpsURLConnection.setSSLSocketFactory 且 java-spiffe 對 JDK 版本有下限必要時走 sidecar）、三大安全主軸＝① SVID 短到幾分鐘~一小時讓 Day78 撤銷難題徹底消失非繞過是消失（活一小時憑證私鑰外洩可用窗口最多一小時然後自己過期「等它過期」跟「撤銷」差別縮到幾乎零而過期是離線可驗 Day75 第①步不需 OCSP 不會 soft-fail 不會被路徑攻擊者丟包→SPIFFE 世界根本沒有撤銷動作；代價轉嫁換發頻率比 Day79 更誇張每小時換管線斷幾分鐘就握手失敗但交給 SPIRE Agent+X509Source 自動更新；維運重點從 Day79「憑證會不會換」變「SPIRE 基礎設施在不在」＝把 Day79 單一 CA 依賴換成單一身分平台依賴）② 身分根從控制網域換成 workload 證明 ③ Day77 內部 CA 盲區的正解（CT 執行力來自瀏覽器不收沒 SCT 憑證而內部 CA 不在根計畫故無 CT 保護＝盲區；SPIFFE 比公開 CA ACME 更激進因你自己就是 CA 效期你說了算每次簽發經 attestation 簽了什麼/發給誰/什麼特徵通過都是你自己登記與日誌接 Day16 不需 CT 事後發現冒用因簽發決策全程在手可稽核；但誠實講不是萬靈丹＝CT 盲區只換地方內部 CA 不上 CT 沒變 SPIFFE 用「簽發決策可控可稽核」補償/SPIRE Server 簽發私鑰＝內部 CA key 是皇冠寶石被偷能簽任意 SPIFFE ID 整個 trust domain 淪陷要 HSM/upstream CA/短效中繼保護 Day15/對外服務還是得走公開 CA+CT+CAA SPIFFE 只管內部對內部邊界對公網 Day77·79 一個不能省）、Java 常見落地形態 sidecar/mesh 模式（SPIRE Agent+Envoy/mesh sidecar 在 pod 裡幫 Java 終結 mTLS Java 只講純 HTTP 給 localhost＝Day79「Java 常躲 LB 後面 TLS 不在 JVM 終結」的延伸把身分與 mTLS 從應用碼抽出交給平台應用甚至無感）、常見誤區表（SPIFFE=內部版 ACME＝問的問題不同/SPIFFE ID 拿主機名 DNS 比對＝在 URI 欄位驗 URI/workload 要自持 token 才能拿 SVID＝相反不帶祕密靠 attestation/node attestation 做了就夠＝擋不住同機冒領要 workload attestation/SVID 短效要搭撤銷＝短效正是取代撤銷/用 SPIFFE 就不用管輪替＝要管 SPIRE 基礎設施在不在/AuthorizeMemberOf 當授權＝太寬是 authN/為跑起來寫空 TrustManager＝拆信任根/SPIFFE mTLS 也設 HTTPS 端點識別＝URI 非 DNS 會失敗/內部用 SPIFFE 對外也不用 CT·CAA＝只管內部/SPIRE Server CA key 跟一般 secret 差不多＝皇冠寶石）、Code Review/維運 checklist 五區（身分與 SVID 使用＝source 單例·動態取憑證非讀死檔·server 別寫死憑證路徑/授權 authN vs authZ＝AuthorizeID vs MemberOf·認誰跟能不能做分開·AuthorizeAny 別出現/驗證正確性承 Day75＝別空 TrustManager 別 InsecureSkipVerify 別設 HTTPS 端點識別/基礎設施依賴承 Day79＝Agent·socket 掛掉失敗行為與告警·CA key 保管·registration entry 變更審核稽核 Day16/範圍認知＝對外別誤用 SPIFFE 取代 ACME·CT·CAA）與測試（身分比對測試最重要＝鏈過但 SPIFFE ID 不在允許清單斷言握手被拒＝守門員存在證明承 Day75·76 測不過 authorizer 是裝飾品/SVID 自動輪替測試效期設 1 分鐘跑超過一效期斷言連線仍正常且序號·NotBefore 換新/Agent 中斷演練 SVID 未過期停 Agent 斷言撐到過期且告警再讓過期斷言明確失敗非靜默降級＝Day79「太久沒成功換發」告警的 SPIFFE 版/attestation 冒領測試同節點但特徵對不上 registration entry 斷言 Agent 不發＝workload attestation 存在證明/主機名無關性測試用跟憑證 DNS SAN 不符甚至沒 DNS SAN 的 host 斷言只要 SPIFFE ID 對就能連） | JWT-SVID 與跨邊界 workload 身分/mTLS 過不去時（L7 gateway·雲端 API·只剩 header）/bearer token 與 aud audience 驗證承 Day31·33/confused deputy（Day81）、SPIFFE federation 跨 trust domain 信任、SPIRE Server upstream CA 與 CA key HSM 保管落地、workload attestor selector 各平台（K8s·AWS·裸機）細節、SVID 熱替換與 zero-downtime rollout、SPIFFE 與 service mesh（Istio/Envoy SDS）整合、node attestation 各 node attestor 深入 |
| 81 | `content/資訊安全/Day81_JWT_SVID_Bearer_Token_Audience.md` | JWT-SVID 與跨邊界 workload 身分 | 延伸（承 Day80 預告，交叉 Day57/Day37/Day33/Day49/Day07/Day38/Day67/Day68/Day16/Day10/Day72/Day75） | 不重講 Day80 的 SPIFFE ID 格式/attestation 兩層/X509-SVID 與 mTLS 基礎，聚焦「身分離開連線變成可攜帶資料」之後的性質改變、mTLS 插不進去的五類場景（L7 gateway/mesh ingress 終結重打、CDN/托管 LB、對雲端 API 與第三方 SaaS、訊息佇列與非同步任務身分要跟著訊息走、跨語言跨環境 hop 只剩 header）＝共同點是身分必須可被複製轉交、核心差異 proof-of-possession vs bearer token（X509-SVID 私鑰不出 workload 抄走憑證也簽不出握手＝拿到憑證≠能冒充；JWT-SVID 誰拿到字串誰就是你，驗證方只驗簽章不驗持有者 ⇒ Day80「沒有祕密可偷」性質部分失效因 token 本身是祕密且會主動塞進 header 送人）、claims 兩個位移（SPIFFE ID 從憑證 SAN URI 搬到 JWT sub；多出 X509-SVID 世界不存在且不能省的 aud＝補回離開連線所失去的綁定；公鑰來源是 Workload API 的 JWT bundle 不是 token header 的 jku/kid ⇒ Day37 kid 注入在正確用函式庫前提下無施力點）、aud 是生死線與 confused deputy（aud 開成 trust domain 或 "internal" ⇒ payment-service 收到 order-service 的 token 可原封不動轉發給 ledger-service 冒充上游＝Day33 token 重放的服務版；X509-SVID 無此問題因物理上重現不了握手，bearer 把物理屏障換成純約定；防禦兩端各半＝簽發端 audience 填目標服務確切 SPIFFE ID 一個下游一份 token、驗證端 expected audience 傳自己的 SPIFFE ID；最常見錯誤是驗證端照抄上游 ID＝方向反了完全沒驗）、Go 實作 go-spiffe v2（workloadapi.NewJWTSource 長生命週期單例有快取與自動更新；FetchJWTSVID(ctx, jwtsvid.Params{Audience: 目標ID}) Audience 必填＝規格層防呆；ExtraAudiences 能不用就不用每多一個就多一條重放路徑；svid.Marshal() 取字串；JWT-SVID 分鐘級短效是 bearer 唯一補救故別自己快取拉長；驗證端 jwtsvid.ParseAndValidate(raw, bundleSource, []string{myID}) 一次做完驗簽章/exp/aud；BundleSource 也是長生命週期單例；地雷 jwtsvid.ParseInsecure 不驗簽章只該用於 debug 出現在請求路徑＝Day57 decode without verify 攻擊者可任意捏 sub＝最該 grep 的字串）、Java 實作 java-spiffe（DefaultJwtSource.newSource() 讀 SPIFFE_ENDPOINT_SOCKET 長生命週期；fetchJwtSvid(SpiffeId 目標) 取 JwtSvid.getToken()；JwtSvid.parseAndValidate(token, JWT_SOURCE, Set.of(MY_ID)) JwtSource 同時是 JwtBundleSource；地雷 parseInsecure 同上；別繞過函式庫改用 jjwt/nimbus 自行驗簽＝公鑰來源與 kid 選擇責任回到自己身上 Day37 雷區；Java 1.8 無 java.net.http.HttpClient 走 HttpURLConnection/Apache 一樣設 timeout 且不跟跳轉）、該用不該用的決策（該用＝穿過控制不了的 L7 元件/身分跟著資料走的佇列與批次/對接不吃內部 CA 的雲端 OIDC federation；不該用＝兩端都控制且網路打得通時仍用 JWT-SVID＝主動把 PoP 降級成 bearer 沒好處、當成比較簡單的內部 auth＝危險在 mTLS 設錯握手當場失敗而 JWT-SVID 設錯一切照常運作直到有人重放；最重要 aud 不是授權 authN≠authZ 業務授權照 Day07/Day49 做）、常見誤區表（跟 X509-SVID 只是格式不同/有簽章就重放不了/audience 填 trust domain 好管/驗證端填上游 ID/不驗 aud 只驗簽章與 sub＝最致命單一疏漏/用 ParseInsecure 先跑起來/自己用 jjwt 驗比較好控制/token 快取久一點/有 JWT-SVID 就不用 mTLS/gateway 驗過後端不用驗＝繞過 gateway 直連內網全裸承 Day38/驗過 aud+sub 等於授權完成/client 跟著 3xx 跳轉沒關係＝把 Bearer token 送給攻擊者承 Day10/67/68）、Code Review checklist 四區（Source 生命週期＝單例·別自己快取拉長效期/簽發端＝audience 窄·ExtraAudiences 別偷懶·拿不到 SVID 大聲失敗·關 follow redirect·token 別進 log 承 Day16/驗證端＝expected audience 傳自己·grep ParseInsecure·別繞過函式庫·驗證失敗要告警·別假設 gateway 驗過/邊界與定位＝真的 mTLS 過不去嗎·別把 authN 當 authZ·token 會不會被下游轉發）與測試（audience 不符測試最重要＝簽章有效未過期但 aud 是別的服務斷言被拒＝audience 驗證存在證明承 Day75/76/80／重放 confused deputy 測試＝模擬下游原封不動轉發斷言第三方拒絕／簽章偽造測試＝捏高權限 sub 用別的金鑰或 alg none 斷言被拒＝偷用 ParseInsecure 的偵測器／過期測試與時鐘容忍／失敗行為測試＝Workload API socket 不可用時斷言明確失敗而非靜默降級成不帶 header 送出／CI grep 斷言 token 不進 log 承 Day18） | SPIFFE Federation 跨 trust domain bundle 交換與 bundle endpoint https_web·https_spiffe 取捨（Day82）、JWT-SVID 與雲端 OIDC federation（AWS IRSA/GCP Workload Identity）對接、Envoy/Istio 以 JWT-SVID 做 L7 authN 的落地、SPIRE JWT 簽發金鑰輪替與 JWKS 快取失效、token 在訊息佇列中的重放窗口與 nonce/jti 去重、gateway 終結身分後如何安全地把原始 caller 身分傳遞下去 |
| 82 | `content/資訊安全/Day82_SPIFFE_Federation_Trust_Domain.md` | SPIFFE Federation：跨 trust domain 的信任建立與收窄 | 入門（新主題，承 Day81 預告，交叉 Day80/Day81/Day75/Day77/Day79/Day10/Day16/Day07/Day49/Day71/Day72） | 不重講 Day80/81 的 SVID 基礎（SPIFFE ID 格式、attestation、X509-SVID/mTLS 怎麼跑），聚焦「信任邊界跨出自己 trust domain」的機制與收窄。federation 解的問題＝兩個獨立 trust domain（spiffe://example.org vs spiffe://partner.com）各持自己的 bundle，對方 SVID 由別的 CA 簽故驗不過（正確行為非 bug）；機制核心＝bundle 交換：不靠共同上級 CA 而是雙邊各自拿到對方 bundle（公鑰集合），點對點/雙邊/明確列舉的信任，與公網 PKI「大家信同一批根 CA」階層式共用天花板是完全不同的信任模型；粒度在自己手上、只影響自己。bundle 不是手動 copy 一次＝每個 domain 開 Bundle Endpoint 定期供最新 bundle（JWKS）對方主動來抓；端點驗證兩種 profile＝https_spiffe（端點出示 SVID 用已 bootstrap 的初始 bundle 驗、要帶外先換初始 bundle、更封閉可控、驗 SPIFFE ID 不看主機名）vs https_web（端點用公開 CA 一般 TLS 憑證、走標準 Web PKI 於是 Day75 主機名驗證+Day77 CT/CAA 整包風險背回來、省事但把地基外包給公網 CA 生態）；registration entry 的 federatesWith 才是實際開關＝沒寫的 workload 其 X509Source 不含 partner bundle＝預設拒絕＝可收窄到「只有 payment-service 認得 partner」。Go 實作＝workloadapi.NewX509Source 拿到的 bundle 只要 entry 有 federatesWith 就自動含 partner federated bundle（不用自己抓/存/輪替公鑰），差異只在 authorizer 換成 tlsconfig.AuthorizeID(spiffe://partner.com/確切服務)；authorizer 光譜在 federation 更要命＝AuthorizeID 逐一（該有的預設）/AuthorizeOneOf 少數明確/AuthorizeMemberOf(partner.com) 是災難＝把對方整個 trust domain 拉進信任邊界/AuthorizeAny 跨 domain 等於自殺。Java 實作＝DefaultX509Source + acceptedSpiffeIdsSupplier 放對方確切 SPIFFE ID，守 Day75/80 鐵律別設 setEndpointIdentificationAlgorithm(HTTPS)（SAN 是 URI 非 DNS）別自寫空 X509TrustManager 拆信任根；Java 1.8 無 HttpClient 走 HttpsURLConnection.setSSLSocketFactory 或 sidecar/mesh。三大安全主軸＝① federation 是「我認得你的 CA」不是「我信任你的所有 workload」授權必須逐一收窄否則把對方整個 trust domain 拉進來（AuthorizeMemberOf 是最常見開太寬錯誤）② bundle endpoint 是新攻擊面＝你會週期性主動去連「對方給的 URL」抓公鑰＝Day10 SSRF 教科書觸發條件＝URL 要受審核不吃動態輸入/出站走 egress allowlist/回應驗端點身分/設 size 逾時上限 ③ bundle refresh 失敗＝安靜的全面斷線＝對方輪替 CA 金鑰而你沒更新到→partner 每個 SVID 都驗不過→跨 domain 連線全部握手失敗且報一般 TLS handshake failure 非「bundle 過期」排查方向被帶偏＝Day79「太久沒換發默默過期」的 federation 版＝解法主動監控 bundle 新鮮度與 refresh 成功率別等連線全斷。落地形態＝mesh federation（SPIRE Agent 透過 Envoy SDS 餵 SVID+federated bundle、授權收窄點移到 Istio AuthorizationPolicy 的 principals、三條主軸一條沒變）。誤區表（無共同上級 CA/federate≠信任所有服務/MemberOf 授權跨 domain 太寬/bundle 非手動 copy 一次/兩種 profile 別隨便選/endpoint URL 別吃動態輸入=SSRF/refresh 失敗非明顯錯誤/非整片打通用 federatesWith 收窄/別設 HTTPS 端點識別）＋Code Review/維運 checklist 三區（信任範圍 authZ 收窄/bundle endpoint 出站攻擊面/refresh 失敗沉默斷線）＋測試（跨 domain 授權收窄=有效簽章但 ID 不在清單斷言被拒=守門員存在證明/MemberOf 過寬迴歸/bundle refresh 斷線演練=握手大規模失敗前就告警/端點身分偽造=拒絕假 endpoint 的 bundle/SSRF 邊界/初始 bundle 過期斷言明確失敗非靜默吃舊的） | SPIRE Server upstream CA 與 CA 簽發金鑰 HSM/KMS 保管（Day83）、UpstreamAuthority plugin（Vault PKI/AWS PCA）、KeyManager plugin 走 KMS、CA rotation 不中斷簽發、SPIFFE Bundle Endpoint 自建與加固、mesh federation（Istio/Envoy SDS）深入、多層 federation 拓撲與傳遞信任風險、federated bundle 稽核與 Day16 告警整合 |
| 83 | `content/資訊安全/Day83_SPIRE_Server_Upstream_CA_HSM.md` | SPIRE Server 信任根保管：upstream CA、簽發金鑰與 HSM | 延伸（承 Day82 預告，交叉 Day80/Day82/Day79/Day78/Day15/Day19/Day16/Day07/Day32/Day48） | 不重講 SPIFFE/SPIRE 基礎（SPIFFE ID 格式、attestation 兩層、X509-SVID/mTLS、JWT-SVID、federation bundle 交換），聚焦「簽 SVID 的那把 CA 金鑰怎麼保管」。威脅定位＝短效 SVID（Day80）只解「單一 workload 憑證外洩」爆炸半徑一小時，但沒保護簽發鏈的根；CA 簽發金鑰被偷＝能簽任意 SPIFFE ID 的合法 SVID（admin/payment-service 全驗過因鏈真的通）且 federation 後連 partner 都信，短效在這層完全無效因被偷的是印章不是憑證；威脅模型＝SPIRE Server 主機被攻陷（RCE/容器逃逸/備份外洩/供應鏈）時 CA 私鑰能不能被搬走，預設能（躺在資料目錄）。三大安全主軸＝① upstream CA 模式：預設 self-signed＝SPIRE 自生根金鑰又線上又是根又落地檔案系統（最糟三性質疊一把）；upstream＝SPIRE 不當根改向上游 CA（Vault PKI/AWS PCA/企業 PKI）要短效中繼來簽 SVID，鏈變 Root(金鑰在 Vault/PCA/HSM，SPIRE 碰不到)→Intermediate(SPIRE 持有短效可撤)→SVID；性質轉移＝根隔離在 SPIRE 外/中繼可撤銷停用/爆炸半徑從整個信任體系收到一段可替換中繼（self-signed 被偷只能打掉整個 trust domain 重練，承 Day15/19）；HCL＝UpstreamAuthority "vault"(vault_addr/pki_mount_point/approle 或 k8s auth) 或 "aws_pca"(certificate_authority_arn/region/signing_algorithm)，Vault/PCA 才是根保管者 SPIRE 只拿短效中繼。② KeyManager 走 KMS/HSM：upstream 解了根但 SPIRE 仍握中繼私鑰，預設 KeyManager "disk" 明文寫資料目錄＝主機攻陷一起搬走；KeyManager plugin＝簽發私鑰不出檔案系統簽章動作送進 KMS/HSM 做（KeyManager "aws_kms" region/key_metadata_file，私鑰本體永遠在 KMS）；心智＝私鑰是能力不是資料（承 Day32/48），攻擊者拿下主機只能「在你撤權前透過這台機器叫 KMS 簽」＝有時間窗可稽核可即時斷（Day78 線上能撤 vs 離線無感搬到 CA 層）非離線隨便簽的災難；upstream+KMS 疊起來＝Server 上沒有任何可離線複製走的簽發金鑰；應用端(X509Source/DefaultX509Source)完全無感，CA 保管是平台層事。③ 短效中繼+不中斷 CA rotation：中繼也短效化把可用窗口壓短（Day80 邏輯上推一層），輪替沒做好＝Day79/82 同款靜默全面斷線；rotation 不硬切＝新舊 CA 同時有效重疊窗口靠 bundle 同放新舊兩把公鑰撐（新 CA 先進 bundle 靠 Day80 X509Source 自動推→切換簽發→舊 CA 最後移除，任何時刻在跑的 SVID 其簽發 CA 都還在 bundle，跟 Day79 換發/Day82 refresh 同「新的先進場舊的最後退場」道理）；最容易錯＝upstream 換中繼靜默失敗用舊的硬撐到過期才全崩，監控盯中繼剩餘效期比例(非固定天數)+換發成功率別等全斷，重疊窗口要長於 bundle 傳到最慢 workload 的時間。Day16 稽核＝CA「簽了什麼」要留兩來源＝SPIRE Server audit log(哪個 entry/SPIFFE ID/selector 通過)+KMS/upstream 的 Sign 稽核(CloudTrail/Vault audit device 獨立於 Server，Server 被攻陷 log 被竄改這份還在＝tamper resistance)；workload 端縱深＝Go source.GetX509BundleForTrustDomain 取 bundle 記錄根憑證 sha256 指紋、Java x509Source.getBundleForTrustDomain+getX509Authorities 對稱，非預期根變動告警。誤區表（短效 SVID＝CA 不用保護反了/self-signed 先跑起來＝根放桌上/upstream 設了就安全＝中繼仍 disk 落地/KMS＝100%拿不走實則攻陷後能透過機器叫 KMS 簽/中繼設長免得一直換＝被偷可用越久/rotation 硬切＝舊 SVID 突然驗不過/換發失敗會看到＝靜默/Server log 夠稽核＝可被竄改要獨立 KMS 稽核）＋Code Review/維運 checklist 四區（信任根架構＝非 self-signed·KeyManager 非 disk·權限最小化/金鑰生命週期＝中繼短效自動輪替·重疊窗口·剩餘效期比例+成功率告警/稽核＝SPIRE audit+獨立 KMS Sign 稽核+根指紋/範圍＝內部 CA 對外仍 CT/CAA Day77·79）＋測試（中繼輪替不中斷演練最重要=效期設幾分鐘打 mTLS 斷言全程不斷序號換新/upstream 斷線告警=中繼過期前告警/金鑰不落地=grep 資料目錄找不到明文私鑰/撤權即時性=模擬攻陷撤中繼或收回 KMS grant 斷言簽不出且根與其他 domain 不受影響/稽核完整性=SPIRE+KMS 稽核都記到且對得上·清空 Server log 後 KMS 稽核仍在/重疊窗口過短迴歸） | workload attestation selector 冒領邊界與收窄（K8s/Docker/裸機，Day84）、SPIRE HA 與 datastore（SQL/多 Server）高可用、UpstreamAuthority 各 plugin（Vault/AWS PCA/cert-manager/nested SPIRE）深入、KeyManager 各 KMS（GCP/Azure Key Vault）落地、SPIRE Server audit log 接 SIEM 的 detection rule、trust bundle 分發與 bootstrap 的完整性 |
| 84 | `content/資訊安全/Day84_SPIRE_Workload_Attestation_Selectors.md` | SPIRE workload attestation selector 冒領邊界與收窄 | 延伸（承 Day83 預告，交叉 Day80/Day07/Day49/Day18/Day22/Day11/Day12/Day16） | 不重講 Day80 attestation 兩層基本流程與 Day83 CA 金鑰保管，聚焦「registration entry 綁的那組 selector 有多難被同一台機器上另一個程序湊出來」。核心＝selector 是 AND，威脅模型窄＝同一 node 同一 Agent 底下另一個被攻陷/惡意程序連上同一 Workload API socket 能否湊齊同組 selector，綁越寬同機能湊齊越多冒領越易＝Day07 最小權限/Day49 範圍開太寬搬到身分發放層。三平台冒領邊界＝① K8s selector：只綁 k8s:ns＝開放整個 namespace 同 namespace 惡意 pod 全對上（namespace 是部署範圍非身分）、加 k8s:sa 收一截但 SA 共用或 RBAC 放任挑 SA 開 pod 就破（承 Day07 K8s RBAC 封頂 selector 強度）、k8s:pod-label 是自陳資料能改 manifest 就能改身分只能當輔助、k8s:container-image 要綁 @sha256:digest 不能綁 mutable tag（承 Day18）；② Unix selector：unix:uid 容器內外不對應且 root 到處是 uid:0≈任何 root 程序、unix:path 是字串非內容可被同名檔/bind mount 混淆（承 Day11/12）、unix:sha256 綁內容最硬但對 Java 幾乎無效＝綁到的是 /usr/bin/java 同機所有 JVM 服務共用同一雜湊零鑑別力（Go 靜態 binary 能靠 sha256 釘死本體、Java 只能靠 k8s:sa+image digest 或每 JVM 專屬 unix user＝編譯語言 vs 共用 runtime 的部署形態差異）、所有平台底層踩 PID 反查屬性＝PID 重用 TOCTOU 心智模型（承 Day22）；③ Docker selector：docker:label/env 自陳最弱設 compose 檔/run 參數的人就能改身分、docker:image_id 綁 digest 較硬。④ 為什麼要疊加＝單一 selector 幾乎都能繞要 AND 交集收到只剩它自己（K8s 疊 ns+sa+container-image@digest、裸機 Go 疊 unix:user+unix:sha256），把冒領成本拉到「攻擊者得能部署一個實質相同的工作負載」。程式面＝spire-server entry create -selector 從只綁 ns 收窄到 sa+container-image@digest、sha256sum 算 binary hash 填 unix:sha256、sha256sum $(command -v java) 示範全 JVM 相同；Day16 稽核兩層＝執行期 SPIRE Server audit log 記「通過哪些 selector」進防竄改日誌、設定期 CI 掃 spire-server entry show -output json 攔過寬 entry（Go 版解 {type,value} selector JSON 判斷有無強 binder：k8s sa:/container-image:@sha256:、unix sha256:、docker image_id:，缺則 CI 紅；Java 版 Jackson 對稱且標記 Java unix:sha256 需人工覆核）。誤區表（有做 attestation 就擋得住/綁 namespace 就有隔離/label 當身分方便/綁 image tag 就釘死/unix:path 綁執行檔安全/unix:sha256 對誰都最硬＝對 Java 綁 JVM/uid 綁了就好/單一 selector 夠/selector 設定一次即可/SPIRE 收窄了 K8s RBAC 不用管）＋Code Review/維運 checklist 四區（selector 收窄＝無只綁 ns/uid·image 綁 digest·自陳 label 只當輔助·Java 不靠 unix:sha256/平台權限邊界承 Day07·11/稽核偵測承 Day16＝CI 攔過寬 entry+audit log/範圍認知＝防同機冒領跨 node 由 node attestation 負責別混）＋測試（同機冒領測試最重要＝特徵對不上斷言 Agent 不發/selector 收窄迴歸＝放寬成只綁 ns 斷言 CI 紅/Java sha256 無效性＝兩 JVM 服務 unix:sha256 相同/image tag vs digest＝重推同 tag 惡意映像綁 tag 仍過綁 digest 擋下/label 偽造/audit 對得上） | node attestation 各 node attestor 冒領邊界（join token 重用/AWS IID 重放/K8s PSAT 信任假設，Day85）、registration entry 規模化管理與 selector drift 偵測自動化、Workload API socket 存取控制與同機多租戶隔離、SELinux/AppArmor label selector 深入、SPIRE HA 與 datastore 高可用、SVID 熱替換與 zero-downtime rollout、service mesh（Istio/Envoy SDS）整合 |
| 85 | `content/資訊安全/Day85_SPIRE_Node_Attestation_Attestor_Boundaries.md` | SPIRE node attestation 各 attestor 冒領邊界（join token / AWS IID / K8s PSAT） | 延伸（承 Day84 預告，交叉 Day80/Day84/Day15/Day26/Day10/Day07/Day05/Day16） | 不重講 Day80 attestation 兩層基本流程與 Day84 workload selector，回到第一層 node attestation，聚焦「你選的 attestor 拿來當節點身分根的憑據能不能被『不是那台機器的東西』拿到」。定位＝node 層被冒領比 workload 層更致命因 workload attestation 前提是 node 可信＝第一層破了 Day84 selector 收再窄都是蓋流沙上；agent SPIFFE ID 形如 spiffe://td/spire/agent/<attestor>/... 同時是稽核線索。三 attestor 三信任根＝① join_token：一段預先發的祕密（承 Day15/26）spire-server token generate -ttl 產一次性 token 用完即失效有 TTL、SPIFFE ID 含 token 明文故 agent list 輸出當敏感、冒領邊界＝祕密老病（外洩進 log/CI/IaC state/image、重用 pre-bake 進 image 違反一次性、塞進不該加入的機器誰持有誰能 bootstrap agent 領 workload 身分）、一次性+TTL 是緩解非免死、收窄＝短 TTL 產生即用/一機一張/走安全通道不進 log/規模化改用平台原生 attestor 定位僅 bootstrap·裸機·dev；② aws_iid：AWS 簽名 IID（承 Day10 SSRF）SPIFFE ID 含 account/region/instance、簽章偽造不了但官方 Security Considerations 明載 IID 對 node 上任何程序可讀＝非 agent 程序/被 SSRF 服務都能讀去 attest 領整台 node 身分＝Day10 打 169.254.169.254 同線、緩解 TOFU（每 node 只能 attest 一次搶先者得手則真 agent 啟動失敗雙方留 log＝可偵測資安事件非雜訊）+IMDSv2（session token/hop limit=1 收窄誰能讀）、重放到別帳號靠 assume_role/verify_organization（account_list_file fail-closed）/validate_eks_cluster_membership 收窄哪帳號哪 cluster 算數、skip_block_device 預設 false 保留 root volume 防竄改別亂關；③ k8s_psat：projected SA token（承 Day05/07）Server 用 TokenReview 驗、SPIFFE ID 含 cluster/node-uid、PSAT vs legacy SAT＝SAT 長效不綁 audience 可無限重放 PSAT 短效綁 audience 自動輪替＝信任假設升級、冒領邊界＝audience 留空 [] 退回 apiserver audience 讓別元件吃同張 token 冒充故 Server audience 專用(預設 ["spire-server"])+agent projected volume audience 兩端對齊、service_account_allow_list 太寬讓任何能用該 SA 起 pod 者冒充＝強度封頂 K8s RBAC(Day07)、clusters 空=fail-closed、node/pod label(allowed_node_label_keys/allowed_pod_label_keys)自陳只當輔助(承 Day84)。共同心智模型＝拿到非那台機器該獨佔憑據→Server 信你是可信 node→領該 node 授權 workload 身分；收窄壓縮三維＝能被誰拿到(IMDSv2/audience/org·SA allowlist)、用幾次(一次性/TOFU)、有效多久(TTL/短效輪替)。Day16 稽核＝agent SPIFFE ID 編碼 attestor+節點識別、spire-server agent list/show/ban(banned 不能再 attest)/evict(解除可重 attest)/count、告警 TOFU 衝突/生產冒出 join_token 型 agent/非預期 account·cluster·SA/join token 短時間大量產生、Go+Java 解析 agent list -output json 對非白名單 attestor(join_token 排除生產)判紅 CI gate。誤區表（node attestation 做了就安全/join token 生產也用/同 token 烤 image/IID 有簽章就安全忽略可讀+重放/TOFU 多餘/關 skip_block_device/PSAT≈SAT/audience 留空較安全/SA allowlist 放寬/node 冒領只影響一台）＋Code Review checklist（attestor 選型定位/join token/aws_iid IMDSv2+org·EKS+TOFU 告警/k8s_psat audience 對齊+allowlist+RBAC/稽核與範圍認知）＋測試（TOFU 搶先測試最重要真 agent 啟動失敗告警/IMDSv2 可讀性收窄/join token 一次性/PSAT audience 不符/SA allowlist 收窄迴歸/非預期 attestor 攔截/ban vs evict 處置演練） | Workload API socket 存取控制與同機多租戶隔離（SPIFFE CSI Driver/hostPath 掛載風險，Day86）、SPIRE Agent SVID 輪替與 reattestation/agent ban-evict 生命週期、其他 node attestor（GCP·Azure metadata/TPM/sshpop/nested SPIRE）冒領邊界、IMDSv2 與雲端 metadata 強化落地、SPIRE HA 與 datastore 高可用、service mesh（Istio/Envoy SDS）node identity 整合、SELinux/AppArmor 與 node 層防竄改 |
| 86 | `content/資訊安全/Day86_Workload_API_Socket_Access_Control.md` | SPIRE Workload API socket 存取控制與同機多租戶隔離（hostPath 直掛 vs SPIFFE CSI Driver） | 延伸（承 Day85 預告，交叉 Day80/Day84/Day11/Day07/Day16/Day22/Day72/Day18） | 不重講 Day80 Workload API/attestation 兩層與 Day84 workload selector，聚焦 attestation 之前那道門＝workload 得先連得上 Agent Unix domain socket 才輪到 workload attestation。兩段閘門模型：閘門一(能不能連上 socket＝本篇存取控制)→閘門二(連上後 attestation 讓不讓拿 SVID＝Day84 selector)；閘門一回答你在不在門口、閘門二回答你是誰、兩道 AND 非替代。掛載方式一 hostPath 直掛(反例 workload pod volumes hostPath path=/run/spire/agent-sockets)：hostPath 是被低估的高權限操作(改字串即可掛 //docker.sock)＝PSS baseline 起就限制 restricted 更把 volume 收白名單(csi 在 hostPath 不在)、存取邊界＝誰能在該 node 宣告此 hostPath 封頂於 Day07 RBAC、readOnly:true 必要但不充分(連 socket 是 connect inode 不需寫檔 read-only 只擋汙染目錄)、Agent socket 檔案權限 SPIRE 刻意寬鬆不靠 mode 擋人靠 attestation SO_PEERCRED 反查 PID 別靠 0700 隔離。掛載方式二 SPIFFE CSI Driver(driver=csi.spiffe.io spiffe/spiffe-csi)：workload 改宣告 ephemeral inline csi volume(readOnly:true)、底層唯讀 bind mount socket 目錄、CSI Driver DaemonSet 自己仍用 hostPath 摸 socket 目錄(不能 emptyDir 否則重啟目錄消失打斷掛載)＝hostPath 從每個 workload 收斂成單一受控 DaemonSet、workload 全可關進 PSS restricted、CSIDriver 只支援 ephemeral inline 無 controller 要求 pod info；別跟 cert-manager/csi-driver-spiffe(把 X.509 SVID 當檔案掛)混。連得上 socket≠拿得到 SVID＝閘門一砍機會非發證：①砍掉探測/施壓 Workload API 機會(Day72)②深度防禦 attestation 非零 bug/selector drift 時仍站著把不需身分 pod(前端站/工具容器/第三方 job)整個擋在 Workload API 外③降低同機冒領立足點(Day84 那個另一程序少一批)；閘門一不能取代閘門二該有身分 workload 本來就掛著 socket 冒充彼此 100% 是 selector 的事。同機多租戶隔離(一 node 一 Agent 一 Workload API)＝最多人想錯：破除給每 workload 自己 socket 的錯覺(SPIRE 主流一 Agent 一 Workload API CSI 只是把同一 socket 分掛)、四層由弱到強＝①attestation selector(Day84 預設也是同機共用 Agent 唯一實質隔離、強度＝selector 收窄強度、只綁 ns 又同 ns=零、ns+sa+image@digest AND=A 冒充 B 得先攻陷 B)②socket 存取控制收第三者非 A 對 B③根本不同機(互不信任租戶 node pool/taint/nodeSelector 分不同 node 各自 Agent＝比同 Agent 收 selector 到極限更根本 selector 再嚴也是同窗口前排隊分 node 連窗口都不同)④mesh sidecar(Envoy SDS 應用不碰 socket 邊界移到誰能注入 sidecar+SDS 通道保護 Day87)；租戶牆是 Day84 selector 非 socket 權限非 CSI socket 存取收第三者 node 分離收互不信任者。Day16 稽核哪些 pod 掛 agent socket 寫成 CI/admission(application pod 不該 hostPath 直掛 socket 目錄判紅/平台元件 spire ns 白名單放行/csi.spiffe.io 缺 readOnly 判黃)、Go 版(podList 解 kubectl get pods -A -o json、platformNamespaces 白名單、hostPath.Path contains /run/spire→FAIL、CSI driver==csi.spiffe.io 且 ReadOnly 非 true→WARN、os.Exit 1)＋Java 版(Jackson readTree path items/metadata/spec/volumes、isMissingNode、asBoolean(false)、System.exit)、實務更穩寫進 admission(OPA/Gatekeeper/Kyverno)把偵測升級預防承 Day07 default deny。誤區表(掛進來就安全靠 attestation/readOnly 就沒事/0700 隔離/CSI 消滅 hostPath/每 pod 自己 socket 隔租戶/selector 收緊就夠/socket 存取防 A 冒充 B/前端站掛 socket 沒差/csi.spiffe.io≈cert-manager csi-driver-spiffe/RBAC 有擋別假設)＋Code Review checklist(掛載方式/存取範圍閘門一/多租戶隔離/稽核)＋測試(hostPath 攔截測試最重要 CI/admission 判紅/不該有身分 pod 掛 socket 告警/連得上≠拿得到 SVID 特徵不符 Agent 不發/CSI readOnly 寫檔失敗/多租戶 selector 放寬成只綁 ns 會紅/節點隔離 taint 生效 A 排不到 B node/PSS restricted 帶 hostPath 被 API server 拒絕) | SPIRE×service mesh 整合：Envoy SDS 餵 SVID/Istio AuthorizationPolicy principals 綁 SPIFFE ID/sidecar 注入器(mutating webhook)信任邊界(Day87)、SPIRE Agent SVID 熱替換與 zero-downtime rollout、SPIRE HA 與 datastore 高可用、SPIFFE CSI Driver 部署加固與 admission policy(OPA/Gatekeeper/Kyverno)落地、per-node Agent 資源與 Workload API DoS 隔離、SELinux/AppArmor 與容器逃逸對 socket 存取的影響 |
| 87 | `content/資訊安全/Day87_SPIRE_Service_Mesh_Envoy_SDS.md` | SPIRE × service mesh：Envoy SDS 餵 SVID、Istio AuthorizationPolicy 用 SPIFFE ID 授權、sidecar 信任邊界 | 延伸（承 Day86 預告，交叉 Day80/Day84/Day81/Day82/Day07/Day49/Day38/Day75/Day79/Day16/Day72） | 不重講 Day80 SVID/attestation 兩層基礎、Day19/74/75 mTLS 握手與憑證驗證、Day86 socket 掛載方式，聚焦「決定應用不自己拿 SVID、交給 mesh」之後身分的驗發與授權從應用碼搬到哪。定位＝library 模式（Day80）應用碼用 go-spiffe/java-spiffe 連 Workload API、authorizer（AuthorizeID）在應用裡決定接不接受對方；mesh 模式應用只講純 HTTP 給 localhost:Envoy、拿憑證/驗對方/決定授權整包搬給 Envoy＝三道新門（門一 Envoy 怎麼拿 SVID、門二誰決定注入 sidecar、門三 Envoy 憑什麼接受對方）。門一 Envoy 拿 SVID＝SPIRE Agent 的 Workload API endpoint 同時實作 Envoy SDS 協定，Envoy 連同一 Agent socket 用 SDS 要①tls_certificate（自己的 X509-SVID 憑證+私鑰）②validation_context（trust bundle 驗對方）；SDS 是串流訂閱＝SVID 快到期 Agent 主動從 stream 推新憑證 Envoy 熱換不重啟（Day79 reload 不重啟+Day80 X509Source 背景更新的 mesh 版）；Envoy bootstrap 定義指向 agent socket 的 spire_agent SDS cluster、對外 listener transport_socket 引用它，對方身分比對 match_typed_subject_alt_names san_type:URI 比 SPIFFE ID（承 Day75/80 千萬別退回比 DNS name/主機名 SVID 無 DNS SAN）、憑證與 bundle 都走 SDS 不落靜態檔、Envoy 連 Agent socket 一樣要過 workload attestation（SO_PEERCRED 反查 Envoy 進程 Day84，Day84/86 兩道門一個沒少只是守門對象從應用換成 sidecar）、每個 mesh workload 要有對應 SPIRE registration entry（沒 entry 連上 SDS 也拿不到 SVID＝閘門二）；Istio 整合用 SPIFFE CSI Driver（Day86）把 agent socket 掛給 sidecar + label/annotation 讓 istiod 把 Envoy SDS 指到 socket，serviceAccountName 被 attestation selector 綁到（Day84）、image 綁 digest（Day18/84）。門二信任邊界搬去哪＝sidecar 靠 mutating admission webhook（sidecar injector）在 pod 建立時把 Envoy 容器與 SPIFFE CSI volume 改寫進 pod spec，injector 能改寫全叢集任何被攔到的 pod＝比 Day86 hostPath 更集中、爆炸半徑更大的高權限元件，「注入範圍＝身分發放範圍」（不需身分 pod 就別被注入＝Day86 別掛 socket 的 mesh 版），收窄＝MutatingWebhookConfiguration/injector ConfigMap 寫入權 RBAC 收平台團隊（改模板＝改每個 pod 多什麼容器 Day07）、注入範圍明確 label/annotation 選入非全叢集預設、注入的 sidecar 模板本身過 PodSecurity（runAsNonRoot/drop caps/readOnlyRootFilesystem 承 Day86）。門三 authN≠authZ＝mTLS 握手成功只證明對方是合法有 SVID 的 workload、沒回答它可不可以呼叫我（Day81 aud 驗過≠授權同一釘子）；Istio AuthorizationPolicy（security.istio.io/v1）用對方 SPIFFE ID 當 principals（Istio 慣例去掉 spiffe:// 前綴），三致命誤區＝①沒鋪預設拒絕 spec:{}（沒有任何 rule 的 ALLOW policy＝什麼都不允許）等於預設全通＝workload 沒任何 ALLOW policy 命中就放行所有請求＝mesh 最大假安全②principals:["*"] 匹配任何有 mTLS 身分來源＝驗身分沒授權＝Day81 AuthorizeMemberOf/Day82 跨 domain MemberOf 翻版③from.source.principals 方向填反（要填「允許誰來呼叫我」＝上游 ID，selector 才選被保護的自己 承 Day81 驗證端照抄上游 ID 同錯）；authZ 收到 methods+paths 層級（承 Day49）；XFCC＝Envoy 把對方 SPIFFE ID 塞進 X-Forwarded-Client-Cert header 給應用＝把密碼學身分降級成 HTTP header，應用信它做授權要確保外部絕對送不進（承 Day38 繞過前置元件偽造轉發 header）。兩面刃＝①應用對「跟誰通話」變無感（library 模式 AuthorizeID 寫在應用碼看得到、mesh 搬到 AuthorizationPolicy 另一 repo，應用常誤以為 mesh 會擋而完全不驗＝authZ 掉進應用與平台沒人負責的縫，心智矯正 mesh 平台化了 authN 但 authZ 業務規則永遠是應用+policy 共同責任 Day07/49）②localhost 明文那段（應用↔自己 sidecar 走 pod 內 loopback 純 HTTP 沒加密，pod 內任何容器共用 network namespace 隔離邊界是 pod 不是容器，多容器 pod 別混不受信任容器）③繞過 sidecar 直連＝mesh 最大隱形破口（Envoy 靠注入的 iptables 重導流量，前提破了攻擊者純 HTTP 直打 pod IP:appPort 就跳過 mTLS+policy+XFCC 全部，常見破口應用綁 0.0.0.0/PeerAuthentication 停 PERMISSIVE/excludeInboundPorts/headless service 直連/gateway 驗過後端不驗 Day38，接回 XFCC 可自捏，整題留 Day88）。Day16 稽核＝掃 AuthorizationPolicy JSON 抓「principals 含 *」「rule 沒 from＝任何來源」「namespace 有 workload 卻沒預設拒絕」，Go 版（exec.Command kubectl get authorizationpolicy -A -o json、apList struct 巢狀 anon struct + json tag、action=="" \|\| "ALLOW" 且 len(Rules)==0→記 nsHasDefaultDeny、len(From)==0→FAIL、principal=="*"\|\|""→FAIL、掃 workload namespace 無預設拒絕→FAIL、os.Exit(1)）+ Java 版（Jackson ObjectMapper readTree ProcessBuilder、path items/metadata/spec、asText("ALLOW") 預設、isMissingNode/size、Set.of+HashSet、System.exit(1)），再掃 PeerAuthentication PERMISSIVE 殘留+excludeInboundPorts 為繞過靜態訊號（留 Day88），執行期承 Day16 Envoy access log 進 SIEM 比對走 mTLS 帶對端 SPIFFE ID vs 明文直連並對後者告警（policy 掃再乾淨擋不住根本沒走 sidecar 的流量）。誤區表（裝了 mesh 跑 mTLS 就安全＝mTLS 只 authN/mesh 給 workload 加身分＝只把拿 SVID 從應用搬 Envoy/Envoy 走 SDS 繞過 attestation＝相反一樣過 attestation/SDS 拿憑證要自寫 reload＝串流熱換/Envoy 比對主機名＝比 URI SAN/sidecar 是維運細節＝injector 高權限注入範圍=身分範圍/principals * 好管＝authN 當 authZ/from 填自己 ID＝方向反/有 mesh 不用管授權＝authZ 共同責任/mesh 全程加密＝loopback 那段明文/反正走 sidecar＝可繞過/信 XFCC 授權＝header 可自捏）+ Code Review checklist 四區（Envoy/SDS 接線門一/sidecar injector 門二承 Day07/86/授權門三承 Day07/49/81/繞過與明文第五節承 Day38 細節 Day88）+ 測試（繞過 sidecar 直連測試最重要＝未注入 pod 純 HTTP 直打應用埠斷言被拒＝Day88 STRICT+NetworkPolicy 存在證明/預設拒絕存在＝無 ALLOW 規則服務發請求斷言被拒/萬用 principal 迴歸＝放寬成 ["*"] 斷言 CI 判紅/principal 方向＝不在清單 ID 帶合法 SVID 呼叫斷言被拒/SVID 輪替不中斷承 Day80 效期設短跑超過一效期斷言連線正常且換新序號/URI vs DNS 比對/注入範圍演練＝不該有身分 namespace 部署 pod 斷言不被注入/XFCC 偽造＝繞過 sidecar 送自捏 XFCC 斷言應用不因此授權） | mesh 全域強制 mTLS 與封堵繞過 sidecar：PeerAuthentication STRICT vs PERMISSIVE、NetworkPolicy 縱深、應用綁 0.0.0.0 vs 127.0.0.1（Day88）、Envoy ext_authz 外接 OPA 做 L7 細粒度授權、Istio RequestAuthentication 用 JWT-SVID 做 L7 authN（承 Day81）、sidecar injector mutating webhook 自身加固與 failurePolicy/reinvocationPolicy、Istio ambient mesh / sidecarless（ztunnel）身分模型與信任邊界差異、SPIRE Agent SVID 熱替換與 zero-downtime rollout、Envoy access log 與 mesh 可觀測性稽核（承 Day16）、mesh egress 出站身分與外部 mTLS |
| 88 | `content/資訊安全/Day88_Mesh_STRICT_mTLS_Sidecar_Bypass.md` | mesh 全域強制 mTLS 與封堵繞過 sidecar：PeerAuthentication STRICT、只綁 127.0.0.1、NetworkPolicy 縱深 | 延伸（承 Day87 預告，交叉 Day80/Day87/Day38/Day10/Day53/Day07/Day19/Day74/Day75/Day16/Day51） | 不重講 Day19/74/75 mTLS 握手與憑證驗證、Day87 SDS 接線與 AuthorizationPolicy，聚焦「怎麼逼打進來的流量沒有任何一條路能繞過 Envoy」。定位＝Day87 的 authN/authZ 全是「流量已進 Envoy」之後的事，只要一條路能不碰 Envoy 抵達應用埠前面全作廢；三種繞過三層封堵＝繞過A明文被 Envoy 收下(PERMISSIVE 殘留)→層一 STRICT、繞過B根本不進 Envoy(app 綁 0.0.0.0 直打 pod IP:appPort)→層二只綁 127.0.0.1、繞過C連線壓根不該存在(hostNetwork/excludeInboundPorts/直連)→層三 NetworkPolicy 預設拒絕；三層分屬 L7 mesh/應用進程/L3-L4 CNI 故各擋另兩層看不到的路。層一 PeerAuthentication(security.istio.io/v1) mtls.mode＝PERMISSIVE 同時收 mTLS 與明文(遷移期用最常見災難是上線忘了收「裝了 mTLS」變假話)vs STRICT 拒一切非 mTLS；三範圍疊加覆蓋(root namespace 通常 istio-system+無 selector=mesh-wide/ns 級/workload+portLevelMtls)；漸進收窄別一刀切=先用 Day16 mTLS 觀測找明文來源→逐一改走 mesh 或排除→先收 ns 再收 mesh-wide，遷移期用 portLevelMtls 把暫時 PERMISSIVE 收斂到單一埠；天花板＝STRICT 只管進到 Envoy 的流量要 mTLS 管不到沒進 Envoy 的封包(必要非充分)。層二只綁 127.0.0.1(唯一在應用碼裡的一層)＝Envoy 終結 mTLS 後用 loopback 轉明文給 app(127.0.0.1:appPort)故 app 只綁 loopback 就夠收 sidecar 流量、綁 0.0.0.0=appPort 在 pod 網路裸露 iptables 攔截有前提會破(排除埠/PERMISSIVE/同 pod 鄰居容器共用 netns/hostNetwork/規則誤設)一破就直達；只綁 loopback=把繞過從「靠 iptables 攔」升級成「物理不可達」不依賴單一機制；Go net.Listen(":8080") 綁全介面 vs "127.0.0.1:8080"、http.Server.Addr、gRPC net.Listen loopback；Java Spring Boot 預設綁 0.0.0.0 用 server.address+management.server.address 收(次要/actuator 埠別忘)、ServerSocket(port) wildcard vs ServerSocket(port,backlog,InetAddress.getLoopbackAddress())、InetSocketAddress(port) wildcard vs ("127.0.0.1",port)、gRPC-Java forPort wildcard 改 NettyServerBuilder.forAddress(loopback)(承 Day51)、1.8/21 通用；必踩維運坑=綁 loopback 後 kubelet HTTP 探針從 node 打 pod IP 會連不上→pod 反覆重啟，解法讓探針走 sidecar(sidecar.istio.io/rewriteAppHTTPProbers→pilot-agent 15020)非綁回 0.0.0.0，收前先確認 probe rewrite 生效否則滾動更新打掛服務。層三 NetworkPolicy(networking.k8s.io/v1)L3/L4 縱深＝STRICT 是 L7 Envoy 的事、NetworkPolicy 是 CNI 收「誰能對 pod IP 開連線」擋 STRICT 看不到的繞過(hostNetwork/排除埠/node 直連)；預設拒絕入站(podSelector:{}+policyTypes:["Ingress"]+無 ingress rule)再逐一 podSelector+port 開通(承 Day07 default deny)；三要點=NetworkPolicy 看不到 mTLS 身分只認 selector/IP/port 故與 STRICT 是 AND 非替代、policyTypes 要寫明否則預設拒絕沒生效、要 CNI(Calico/Cilium)支援並強制才算數否則廢紙必實測。合法但打洞旋鈕＝excludeInboundPorts(該埠完全不經 Envoy 無 mTLS/policy/XFCC 改用 includeInboundPorts 白名單)、headless service(直連 pod IP 搭綁 0.0.0.0 更危險)、hostNetwork:true(共用 node netns per-pod sidecar 攔截失效=最粗暴繞過應用 pod 出現幾乎紅旗)、sidecar.istio.io/inject:false；不該禁該稽核到有清單。Day16 稽核＝靜態掃 PeerAuthentication 非 STRICT(含 port-level)+pod excludeInboundPorts/hostNetwork，Go 版(paList/podList struct+kubectlJSON(any)+exec kubectl get peerauthentication/pods -A -o json+mtls 指標 nil 判斷+PortLevelMtls map range+annotations map+os.Exit(1))、Java 版(Jackson readTree+ProcessBuilder+path items/metadata/spec+asText("PERMISSIVE") 預設+portLevelMtls fieldNames Iterator+asBoolean(false)+System.exit)，CI 掃不到但要補=namespace 缺預設拒絕 NetworkPolicy(延用 Day87 nsHasDefaultDeny 邏輯)、應用綁哪介面 CI 看不到(執行期實測或 pod 內 ss -tlnp 啟動自檢斷言只 listen 127.0.0.1)；執行期承 Day16 切 STRICT 前把 Envoy mTLS 觀測進 SIEM 比對帶對端 SPIFFE ID vs 明文對後者告警。誤區表(裝了 mTLS 就強制=預設 PERMISSIVE/PERMISSIVE 還算安全=身分虛設/STRICT 滴水不漏=管不到沒進 Envoy/綁哪介面跟資安無關=0.0.0.0 裸露/反正 iptables 會導=攔截前提會破/探針掛就綁回 0.0.0.0=白做該走 sidecar/有 STRICT 不用 NetworkPolicy=不同層擋不同繞過/鋪了就生效=要 CNI 強制/excludeInboundPorts 是細節=STRICT 管不到的洞/hostNetwork 沒大不了=攔截失效/繞過是理論=綁 0.0.0.0+PERMISSIVE+無 NetworkPolicy 就成立承 Day38)+Code Review checklist 四區(層一 STRICT/層二綁 loopback/層三 NetworkPolicy 承 Day07/打洞與稽核承 Day16)+測試(繞過 sidecar 直連測試最重要=未注入 pod 純 HTTP 直打 pod IP:appPort 斷言被拒/明文被拒/ss 斷言只綁 127.0.0.1/健康檢查不中斷/NetworkPolicy 生效/打洞迴歸 CI 判紅/STRICT 漸進切換演練) | mesh egress 出站身分與對外 mTLS：ServiceEntry/DestinationRule originate TLS/mTLS、egress gateway、防繞過 egress 直接對外、NetworkPolicy egress 預設拒絕(Day89)、Envoy ext_authz 外接 OPA 做 L7 細粒度授權、Istio RequestAuthentication 用 JWT-SVID 做 L7 authN(承 Day81)、sidecar injector mutating webhook 自身加固 failurePolicy/reinvocationPolicy、Istio ambient mesh/ztunnel sidecarless 身分模型與繞過模型差異、NetworkPolicy 與 Istio 互動與 CNI 差異、Envoy access log/telemetry mTLS 可觀測性稽核(承 Day16) |
| 89 | `content/資訊安全/Day89_Mesh_Egress_ServiceEntry_DestinationRule.md` | mesh egress 出站身分與對外 mTLS：ServiceEntry 認知/REGISTRY_ONLY、DestinationRule originate TLS/mTLS、egress gateway + NetworkPolicy egress 防繞過 | 延伸（承 Day88 預告，交叉 Day10/Day53/Day07/Day87/Day88/Day19/Day75/Day79/Day16/Day72/Day82） | 不重講 Day19/74/75 mTLS 握手與憑證驗證、Day10/53 SSRF 入門、Day88 入站三層封堵，聚焦「mesh 出站從黑洞收成有身分/受控/防繞過」。定位＝Day88 收入站可達性 Day89 收出站三件(能連去哪/怎麼加密/有沒有繞過受控出口)，對外信任邊界比內部更需明確列舉(內部共享 SPIFFE trust domain 第三方在控制外)；與 SSRF 關係＝Day10/53 防「被騙去打內網」vs egress allowlist 防「未經列舉的外連」是 AND 非重複。手段一 ServiceEntry+outboundTrafficPolicy REGISTRY_ONLY＝預設 ALLOW_ANY 對未知外連放行且 passthrough(TLS 不終結/看不到/無 policy)＝出站黑洞，翻 REGISTRY_ONLY＝出站 default deny(承 Day07)，ServiceEntry 宣告外部目標(hosts 精確主機名別 wildcard/location MESH_EXTERNAL/ports protocol/resolution DNS 別 NONE)，漸進切換(先用 Day16 egress log 盤外連再逐一補 ServiceEntry 才翻)，ServiceEntry 是認知非授權(誰能連要 AuthorizationPolicy/Sidecar egress 收窄)。手段二 DestinationRule trafficPolicy.tls.mode originate＝DISABLE(應用端到端做)/SIMPLE(Envoy 單向 TLS 應用送 http)/MUTUAL(Envoy 對外 mTLS 用約定 client cert clientCertificate/privateKey/caCertificates/sni)/ISTIO_MUTUAL(內部 SVID 別誤用給第三方)，核心取捨對外 TLS 該 mesh 做還應用做判準同 Day79「TLS 在哪終結就在哪管」＝mesh 做集中憑證/pinning/cipher 應用無感但 app↔sidecar loopback 明文+認知落差，應用做端到端加密憑證自捏但散在各應用碼(Day19 老問題)，Go 兩寫法(http.Client+Transport+tls.Config Certificates/RootCAs/MinVersion TLS12/CheckRedirect ErrUseLastResponse vs 送 http:// 給 sidecar)、Java 兩寫法(OkHttp SSLContext+sslSocketFactory+callTimeout+followRedirects(false)/Spring RestClient requestFactory 綁 SSLContext vs 送 http:// 給 sidecar，1.8 無 HttpClient 走 HttpsURLConnection.setSSLSocketFactory/Apache，別自寫空 X509TrustManager 承 Day75)。手段三防繞過對稱 Day88 繞過 sidecar＝egress gateway 靠攔截導流前提會破，三種出站繞過(A 未知外連被放行→REGISTRY_ONLY/B 直連外部不經 egress→VirtualService 導去 egress gateway+只信任其出口/C 攔截不成立 hostNetwork/excludeOutboundPorts/node 直連→NetworkPolicy egress)，NetworkPolicy egress 預設拒絕(podSelector:{}+policyTypes:["Egress"]+無 egress rule＝出站全拒，再放行 DNS 53 UDP/TCP+egress gateway，承 Day07)，三要點＝NetworkPolicy 看不到 mesh/TLS 只認 selector/IP/port 與 REGISTRY_ONLY 是 AND(L7 vs L3/L4 擋不同繞過)/別忘放行 DNS 否則整 ns 解不了名症狀像全斷/要 CNI 支援 egress 方向並強制否則廢紙必實測。合法但打洞旋鈕＝ALLOW_ANY 殘留(allowlist 形同虛設)/ServiceEntry hosts:* + resolution:NONE(精確列舉打回全通)/excludeOutboundPorts/excludeOutboundIPRanges(該出站不經 Envoy 無 allowlist/TLS/稽核)/hostNetwork(出站攔截失效)/DestinationRule tls.mode:DISABLE 用錯地方(明文對外)。Day16 稽核＝靜態掃 ServiceEntry wildcard host/resolution NONE + pod excludeOutbound*/hostNetwork，Go 版(seList/podList struct+kubectlJSON(any)+exec kubectl get serviceentry/pods -A -o json+strings.Contains "*"+EqualFold "NONE"+annotations map 兩 key+os.Exit(1))、Java 版(Jackson readTree+ProcessBuilder+path items/spec/hosts+asText contains "*"+equalsIgnoreCase NONE+annotations asText+asBoolean(false)+System.exit)，CI 要補＝outboundTrafficPolicy 是否 ALLOW_ANY(讀 istio-system configmap istio meshConfig)是總開關/namespace 缺出站預設拒絕 NetworkPolicy(延用 Day88 nsHasDefaultDeny 換 Egress)/對外走沒走 egress gateway CI 看不到；執行期承 Day16 Envoy egress access log 進 SIEM 比對已知 ServiceEntry 目標 vs passthrough 未知外連對後者告警(allowlist 掃再乾淨擋不住沒走 sidecar/egress 的出站)。誤區表(入站收好出站沒啥好收/mesh 預設會擋未知外連=ALLOW_ANY/egress allowlist=SSRF 同一件事=AND/宣告 ServiceEntry=授權誰都能連/對外用 ISTIO_MUTUAL=內部 SVID 第三方不信/TLS 交 mesh 就一定加密=loopback 明文+沒生效裸奔/送 http:// 就沒加密=originate 有生效就 TLS 但要實測/鋪 egress gateway 就一定經過=攔截前提會破/有 REGISTRY_ONLY 不用 NetworkPolicy egress=L7 vs L3/L4/出站預設拒絕把 53 也擋=DNS 全斷/excludeOutboundPorts 是細節=管不到的洞/ServiceEntry hosts:* 好管=打回全通)+Code Review checklist 四區(手段一認知 allowlist/手段二對外 TLS mTLS/手段三防繞過承 Day07/打洞與稽核承 Day16)+測試(出站繞過測試最重要=直連未宣告外部位址斷言被拒/REGISTRY_ONLY 生效=未宣告拒已宣告通/對外 mTLS=要 client cert 端點握手成功拿掉 cert 被拒/egress 集中=對方看到來源 IP 都是 egress gateway 固定出口/DNS 沒被誤擋/打洞迴歸 CI 判紅/REGISTRY_ONLY 漸進切換演練) | mesh L7 細粒度授權 Envoy ext_authz 外接 OPA/Rego、AuthorizationPolicy 天花板、fail-open vs fail-closed 授權服務可用性(Day90)、Sidecar resource egress host 收窄與 per-workload 出站範圍、egress gateway 自身加固與多租戶出口隔離、Istio ambient mesh/ztunnel 出站攔截與繞過模型差異、對外 client cert 輪替與 SDS 供憑證給 egress originate、第三方 API 出站 rate limit 與重試風暴(承 Day17/72)、雲端 metadata egress 強化與 SSRF 交叉(承 Day10) |
| 90 | `content/資訊安全/Day90_Mesh_ExtAuthz_OPA_Authorization.md` | mesh L7 細粒度授權：Envoy ext_authz 外接授權服務（OPA/Rego 或自寫 gRPC/HTTP）、AuthorizationPolicy 天花板、fail-open vs fail-closed 與授權服務可用性 | 延伸（承 Day89 預告，交叉 Day87/Day07/Day49/Day81/Day38/Day16/Day72/Day80） | 不重講 Day07 存取控制入門/Day49 BFLA/Day87 AuthorizationPolicy 接線/Day81 aud，聚焦「授權從宣告式比對升級成外接決策」。天花板＝AuthorizationPolicy 是無狀態 L4/L7 屬性比對只看請求靜態屬性(principals/methods/paths)看不到被存取物件 owner/request body/DB 狀態/外部政策＝表達不了「使用者只能讀自己訂單」(Day07 IDOR)/business rule/依 body 決策；ext_authz 補這段＝分層非替代(AuthorizationPolicy 收粗粒度 method/path→ext_authz 對通過者做 per-object/business-rule 決策)，心智矯正別把授權全塞 policy 或散在各 handler(Day49 成因)而是集中成必經可測試決策點。契約＝Envoy ext_authz filter 轉發前暫停呼叫外部授權服務拿 allow/deny，gRPC(envoy.service.auth.v3 Check CheckRequest/CheckResponse OK/PermissionDenied，OPA-Envoy 走這條)vs HTTP(2xx 放行/4xx 拒自寫最省)；Istio 用 meshConfig.extensionProviders(envoyExtAuthzGrpc/envoyExtAuthzHttp 各有 service/port/timeout/failOpen/includeRequestHeadersInCheck/headersToUpstreamOnAllow)+AuthorizationPolicy action:CUSTOM+provider.name(承 Day87 同物件 action 換 CUSTOM)接，failOpen 底層＝Envoy failure_mode_allow(transport_api_version V3)；三注意＝CUSTOM 只在符合 rules 的請求觸發非萬能兜底(未命中回一般 ALLOW/DENY 評估沒兜底恐全通)/授權在關鍵路徑每請求多一趟/身分要不可偽造(驗 JWT 承 Day81 或信 RequestAuthentication metadata 別信 client 自帶 header 承 Day38)。政策 OPA/Rego＝policy-as-code(集中/opa test/解耦)input.attributes.request.http 取 method/path/headers、default allow:=false(承 Day07)、owner 判斷靠 data.orders(OPA bundle 或 http.send 即時查—後者多一趟外呼+SSRF 面 Day10)；Rego 天花板＝只在 input+已載入 data 決策複雜即時領域邏輯自寫服務更順。自寫服務＝HTTP 契約最省(原始 method/path→200/403)，Go(net/http 只綁 127.0.0.1 承 Day88+ReadTimeout 短+trustedUser 驗身分+ownerOf 查 owner+未涵蓋預設拒)/Java(Spring Boot3 Java17+ @RestController @GetMapping /orders/{id} 回 ResponseEntity 200/403+/** 兜底 403，1.8 走 Spring Boot2/Servlet，gRPC 兩版 AuthorizationImplBase)都做到 Day87 做不到的 per-object owner check(order.owner==user)＝把 BFLA/IDOR 面收斂成一決策點；gRPC 版用 go-control-plane auth/v3。fail 行為(最重要)＝failOpen/failure_mode_allow：true=授權服務一掛全部放行=Day07 default deny 反面+可被主動打垮開門，false=fail-closed 安全但授權服務成可用性單點；預設該 fail-closed 再用同機 sidecar(OPA-Envoy per-pod loopback 無跨 pod 單點一起生死)+決策快取(TTL 是安全 vs 效能取捨撤權後舊決策)+壓測定 timeout(太長拖垮 P99 承 Day72 太短高負載誤拒)+HA 把可用性補回，別用 fail-open 換安全；failure_mode_allow 只管故障預設明確 deny 永遠 deny。Day16 稽核＝掃 AuthorizationPolicy JSON 抓 action:CUSTOM 沒 provider.name/用 CUSTOM 的 ns 缺預設拒絕兜底(無 rules 的 ALLOW=deny-all 承 Day87 nsHasDefaultDeny)，Go(apList struct·action 預設 ALLOW·Rules RawMessage len·Provider.Name·nsHasDefaultDeny/nsUsesCustom map·os.Exit1)/Java(Jackson readTree path spec action asText ALLOW·rules isMissingNode/size·HashMap·System.exit)，CI 要補＝extensionProviders failOpen 在 istio configmap meshConfig YAML(kubectl -n istio-system get cm istio -o jsonpath .data.mesh)是總開關/授權服務有沒有真做 object-level CI 看不到靠測試/timeout 壓測；執行期承 Day16 決策+fail-open 啟動+延遲進 SIEM 對 deny 尖峰(探 IDOR/政策改壞)/fail-open 觸發(門正開 P1)/延遲逼近 timeout 告警。誤區表(授權全用 AuthorizationPolicy 夠/ext_authz 取代 policy/設 CUSTOM 整服務就守住/授權服務信 x-user header/failOpen:true 較不影響可用性/fail-closed 成單點所以 fail-open/failure_mode_allow 把 deny 也放行/Rego 能做所有授權/ext_authz 延遲不用管/授權外接應用就不用管)+Code Review checklist 四區(天花板分層/契約兜底承 Day87/fail 行為承 Day07/稽核承 Day16)+測試(object-level 授權最重要 A 讀 B 訂單斷言被拒 Day87 會放行/未涵蓋路徑兜底/fail-closed 演練停授權服務斷言被拒非放行+刻意 fail-open 斷言告警/failOpen 迴歸 CI 判紅/延遲預算注入延遲/opa test/身分不可偽造承 Day38) | Kubernetes admission webhook 信任邊界與 failurePolicy fail-open vs fail-closed(mutating/validating·sidecar injector·OPA Gatekeeper/Kyverno·namespaceSelector 排除自身·timeoutSeconds·reinvocationPolicy，Day91)、Istio RequestAuthentication 用 JWT-SVID 做 L7 end-user authN(承 Day81)、OPA bundle/external data 同步與 http.send SSRF 交叉(承 Day10)、決策快取 TTL 與撤權延遲、Envoy ext_authz include request body 與大 body DoS(承 Day72)、Sidecar resource per-workload 授權範圍收窄、Istio ambient mesh/ztunnel 的 L7 授權與 waypoint proxy 模型差異、授權延遲預算與 SLO 可觀測性(承 Day16) |
| 91 | `content/資訊安全/Day91_Admission_Webhook_FailurePolicy.md` | Kubernetes admission webhook 信任邊界與可用性：mutating/validating webhook 自身加固、failurePolicy fail-open vs fail-closed、webhook 成為叢集單點 | 延伸（承 Day90 預告，交叉 Day07/Day87/Day90/Day72/Day79/Day19/Day18/Day10/Day22/Day16） | 不重講 Day07 存取控制入門/Day87 sidecar injector 是什麼/Day90 ext_authz 資料面授權，聚焦「資源進不進叢集的決策外接給 webhook 後 webhook 成為控制面高權限單點」的信任邊界/fail 行為/可用性。定位＝Day90 收資料面(Envoy 對每請求問 ext_authz)fail 行為，Day91 收控制面(apiserver 對每次 API 寫入問 admission webhook)fail 行為同題換場；admission 鏈＝Authentication→Authorization(RBAC Day07)→Mutating admission(MutatingWebhookConfiguration 可改寫)→schema validation→Validating admission(ValidatingWebhookConfiguration 只准/拒)→etcd，mutating 在 validating 之前(Day92 mutation 繞過伏筆)；兩種 webhook＝Mutating(injector/Gatekeeper-Kyverno mutation 回 JSONPatch 改寫)vs Validating(policy 檢查回 allow/deny)；關鍵邊界＝admission 只在資源被寫入當下(CREATE/UPDATE/DELETE/CONNECT)觸發管不到既存資源(既存要 Gatekeeper audit/Kyverno background scan，Day92)。爆炸半徑＝①能力面 mutating 幾乎能對被攔資源做任何事(塞惡意 sidecar/init container 承 Day87 反用、換 image 承 Day18、加外送 secret env、拿掉 securityContext)validating 能拒=能拒全部部署/選擇性放後門②信任面 *WebhookConfiguration 是 cluster-scoped 高權限物件誰能 create/update 誰能全叢集攔截=RBAC 收平台團隊(Day07 控制面最高等級)③憑證面 caBundle 是 apiserver 信任 webhook 唯一根據，外洩/被替換=可假冒攔截竄改 AdmissionReview、過期沒輪替=webhook 呼叫全失敗接著觸發 failurePolicy(承 Day79/19)④端點面 clientConfig service(in-cluster)vs url(外部端點=多一可用性/信任/SSRF 面 Day10)。信任邊界加固＝①RBAC 收 validatingwebhookconfigurations/mutatingwebhookconfigurations 寫入權②namespaceSelector/objectSelector 精準攔且必排除 kube-system+其他控制面 ns+webhook 自身 ns(否則 fail-closed 攔控制面卡死/webhook Pod 重排時 apiserver 得先呼叫還沒起來的自己=啟動死結；用 1.21+ 自動標的 kubernetes.io/metadata.name label NotIn 排除)③sideEffects:None(dry-run/重試安全 webhook 別在 admission 寫外部系統)④in-cluster service+可輪替憑證(承 Day79)；加固版 ValidatingWebhookConfiguration YAML(admissionReviewVersions:[v1]/sideEffects:None/failurePolicy:Fail/matchPolicy:Equivalent/timeoutSeconds:5/namespaceSelector NotIn kube-system 等/objectSelector matchLabels/rules 精準列舉不用 星號/clientConfig.service+caBundle)。failurePolicy(核心，Day90 翻版)＝Ignore(fail-open)webhook 掛放行未檢查資源=policy 形同虛設+可被主動打垮/拖慢繞過(承 Day90/72)vs Fail(fail-closed，v1 預設)webhook 掛擋下被攔寫入=安全但成叢集寫入可用性單點；預設該 Fail(default deny 承 Day07)，很多團隊怕單點設 Ignore=把安全丟掉換可用性方向反；failurePolicy 只管故障預設(逾時/連不上/憑證錯)明確 deny 永遠 deny(同 Day90 failure_mode_allow)；血淚點 Fail+rules 全星號又 webhook 掛/慢=全叢集無法建任何資源(常見事故解法縮範圍+撐可用性非改回 Ignore)。兩者兼顧＝①縮攔截範圍(namespaceSelector/objectSelector/rules 精準)一石二鳥同降爆炸半徑與可用性依賴(fail-closed 只擋本來被攔的一小撮)②webhook 服務 HA(多副本/PDB/跨 node，前提自身 ns 已排除)③timeoutSeconds 短但別太短(預設 10 範圍 1-30，太長慢 webhook 拖垮每次 API 寫入 P99 承 Day72、太短高負載/冷啟動誤擋，壓測定)④reinvocationPolicy(僅 mutating)IfNeeded 允許後續 webhook 有改動時重呼叫先前 mutating(預設 Never)代價同 webhook 被多次呼叫故 mutation 必冪等(承 Day22 否則 sidecar 注兩次)⑤matchConditions(CEL 較新版才有)apiserver 端更細判斷要不要送 webhook。handler(最小可用)＝apiserver POST AdmissionReview(含 request)回帶 response 的 AdmissionReview，response.allowed 准/拒；Go(k8s.io/api/admission/v1+corev1+metav1，解 review.Request、resp:=AdmissionResponse{UID:req.UID}回填 UID、json.Unmarshal req.Object.Raw 成 Pod、validate 拒 privileged、Result:&metav1.Status{Message}deny 帶原因、回 AdmissionReview{TypeMeta,Response}、ListenAndServeTLS 憑證對 caBundle 承 Day79/19；mutating 版回 resp.Patch JSONPatch+PatchType=&PatchTypeJSONPatch 需冪等)/Java(Spring Boot3 Java17+ @PostMapping /validate 收 JsonNode review、path request/uid/object/spec/containers/securityContext/privileged asBoolean、回 ObjectNode uid/allowed/status.message、1.8 走 Spring Boot2)。Day16 稽核＝掃 validatingwebhookconfigurations,mutatingwebhookconfigurations -o json(cluster-scoped 不用 -A 逗號撈成一 List)抓①failurePolicy:Ignore②namespaceSelector 沒排除 kube-system+自身 ns(判紅)③timeoutSeconds 過長④rules 含星號/外部 url/sideEffects 非 None；Go(nsSelector 具名型別+excludes 方法檢 NotIn kubernetes.io/metadata.name、webhook/whList struct、TimeoutSeconds *int 預設 10、ClientConfig.Service.Namespace 當自身 ns、os.Exit1)/Java(Jackson readTree ProcessBuilder、excludes 掃 matchExpressions、asText(Fail)/asInt(10) 預設、System.exit)；CI 掃不到要補＝RBAC 誰能寫 *WebhookConfiguration(掃 ClusterRole/Binding)、webhook 有沒真做檢查/mutation 冪等靠測試、憑證有效期/HA 執行期；執行期承 Day16 apiserver_admission_webhook 指標+webhook 決策進 SIEM 對 webhook 呼叫失敗率上升(fail-closed 大量擋/fail-open 門開兩者 P1)/Ignore 實際 fail-open 觸發(policy 被繞)/延遲逼近 timeout 告警(靜態掃不到門開沒開)。誤區表(admission webhook 是小工具/Ignore 較不影響叢集/fail-closed 成單點所以 Ignore/failurePolicy 把 deny 放行/攔全叢集含 kube-system 較保險/不用排除自身 ns/rules 全星號省事/timeout 大一點較不誤擋/多個 mutating 不互相影響/憑證是維運細節/外部 url 沒差/admission 擋住=叢集乾淨)+Code Review checklist 三區(爆炸半徑與信任邊界承 Day07/87、fail 行為與可用性承 Day07/90/72、稽核承 Day16)+測試(fail-closed 演練最關鍵 停 webhook 斷言被攔非放行+kube-system/自身 ns 仍能寫、啟動死結演練 webhook 不可用時重排自身 Pod 斷言起得來、failurePolicy:Ignore 迴歸 CI 判紅、未排除 kube-system 迴歸判紅、object-level 送 privileged 斷言被拒帶原因、冪等 mutation 重複套用結果不變承 Day22、延遲預算注入延遲量 P99 承 Day72、憑證輪替不中斷+過期告警承 Day79) | OPA Gatekeeper/Kyverno 政策即程式碼盲點與繞過(ConstraintTemplate/Constraint·Kyverno ClusterPolicy·enforce vs dryrun/Audit·admission 只擋新寫入既存靠 audit/background scan·excludedNamespaces/exempt 濫用·mutation 改掉 validation 看到內容·UPDATE/subresource 繞過·opa test/kyverno test，Day92)、admission webhook 憑證輪替與 cert-manager 整合(承 Day79)、RBAC 提權路徑與 *WebhookConfiguration 寫入權稽核、Pod Security Admission(內建 admission plugin)vs webhook 取捨、Gatekeeper mutation 與 validation 順序風險、matchConditions(CEL)細粒度攔截、Istio ambient/ztunnel 無 sidecar injector 信任模型差異、admission webhook DoS 與延遲預算 SLO(承 Day72/16) |
| 92 | `content/資訊安全/Day92_Gatekeeper_Kyverno_Policy_Bypass.md` | OPA Gatekeeper / Kyverno 政策即程式碼的盲點與繞過：政策怎麼寫（ConstraintTemplate+Constraint 內嵌 Rego / Kyverno ClusterPolicy validate/mutate）＋五大繞過（dryrun/Audit 沒上線、excludedNamespaces/exempt 後門、mutation 改掉 validation 內容、只攔 CREATE 漏 UPDATE/subresource、既存資源要 audit/background scan）＋政策單元測試 | 延伸（承 Day91 預告，交叉 Day90/Day91/Day07/Day49/Day18/Day22/Day16） | 不重講 Day91 admission webhook 機制/failurePolicy、Day90 Rego 語法基礎/opa test 基礎、Day07 default deny 入門；聚焦「跑在 admission 管線上的政策內容」怎麼被寫壞/繞過（管線 vs 內容正交：failurePolicy:Fail 只保證掛了會擋，不保證活著時政策真的在擋）。定位＝Day91 收管線（webhook 本身），Day92 收內容（policy engine 的政策）；兩大 engine＝Gatekeeper(ConstraintTemplate 定義規則內嵌 Rego 必含 violation 區塊+Constraint 實例指定 match/enforcementAction，自管 webhook)vs Kyverno(ClusterPolicy/Policy YAML DSL 或 CEL 表達 validate/mutate/generate/verifyImages 不寫 Rego，自管 webhook)。政策怎麼寫（範例：禁 :latest/要求 image 帶 digest 承 Day18/要求 resource limits）＝Gatekeeper ConstraintTemplate(templates.gatekeeper.sh/v1，spec.crd.spec.names.kind 動態產生 constraints.gatekeeper.sh CRD，targets[].rego 內嵌 violation[{"msg":msg}] 用 input.review.object)+Constraint(spec.enforcementAction/spec.match.kinds/excludedNamespaces)；Kyverno ClusterPolicy(kyverno.io/v1，spec.background、spec.rules[].match.any[].resources.kinds/operations、validate.failureAction/message/pattern，image:"!*:latest")。①盲點-dryrun/Audit 等於沒上線＝Gatekeeper enforcementAction:deny(預設真擋)/dryrun(不擋只記 status.violations)/warn(不擋但回警告)、Kyverno failureAction(舊 spec.validationFailureAction):Enforce(擋)/Audit(不擋只寫 PolicyReport/ClusterPolicyReport)；跟 Day91 fail-open 形似但成因不同(fail-open=webhook 掛才放行、dryrun/Audit=活著也不擋更徹底)，觀察期後忘了切 enforce=常駐不設防可被利用；部署≠上線。②盲點-excludedNamespaces/exempt 後門＝Gatekeeper Constraint.spec.match.excludedNamespaces+全域 Config(config.gatekeeper.sh)spec.match.excludedNamespaces(影響所有 Constraint 更危險)、Kyverno spec.rules[].exclude(排 ns/clusterRoles/roles/subjects/SA)；三風險=exempt ns 成合規逃生艙(能往該 ns 部署即繞過)/exempt by user/role/SA 更隱蔽(承 Day07/49 豁免本身是存取控制決策寫寬=後門)/exempt 沒到期沒審核；防禦=只給控制面必要 ns 業務豁免要審核標原因到期被稽核，用精準 match 別先全攔再大量 exempt。③盲點-mutation 改掉 validation 看到內容(承 Day91 mutating-before-validating)＝validate 永遠看到「所有 mutation 改寫後」的資源不是原始輸入；坑一 mutate 補預設(補 runAsNonRoot:true)讓 validate 抓不到原始違規=違規被靜默修正而非擋下回報(把攔截降級成粉飾)、坑二 mutate 設出不安全預設而 validate 沒覆蓋該欄位;心法=安全關鍵欄位優先 validate(deny)非 mutate(silent fix)、validate 檢查的是 post-mutation 狀態、mutation 必冪等(承 Day91 reinvocationPolicy/Day22)。④盲點-只攔 CREATE 漏 UPDATE/subresource＝Gatekeeper webhook 預設攔 CREATE+UPDATE、Kyverno validate 預設涵蓋 CREATE/UPDATE；坑一主動把 operations 縮成[CREATE]=建合規再 patch 成違規繞過(安全政策 operations 必含 CREATE+UPDATE)、坑二 subresource 繞過(pods/ephemeralcontainers=kubectl debug 往跑著的 Pod 注入 privileged 臨時容器、pods/exec+pods/attach 進容器執行；要明確匹配 subresource 或 RBAC 收 exec 承 Day07)、坑三控制器代建(Kyverno autogen 自動幫 match Pod 政策產生 Pod controller 規則；autogen 關掉或 exclude 控制器 SA=Deployment 代建繞過)。⑤盲點-admission 只擋新寫入既存違規要 audit/background scan(承 Day91 第一節)＝Gatekeeper audit controller 週期掃既存列進 Constraint.status.violations、Kyverno spec.background:true 定期掃產出 PolicyReport/ClusterPolicyReport；上線 enforce 前先用 audit/background 盤存量再切(直接 enforce 不刪既存但會讓既存違規的下次 UPDATE 突然被擋造成看似無關的失敗)、background:false=存量盲區。Day16 稽核=Kyverno kubectl get clusterpolicies,policies -A -o json 掃①failureAction/validationFailureAction=Audit(判紅)②background:false③exclude 過寬(namespaces 含星號判紅/排 ns 判黃要審)④validate 規則 operations 只含 CREATE 漏 UPDATE(判紅)；Go(resources/matchBlock/rule/policy/polList struct、effectiveAction per-rule 優先否則 policy-level 預設 Audit、Background *bool、has helper、os.Exit1)/Java(Jackson readTree ProcessBuilder、effectiveAction path failureAction/validationFailureAction asText(Audit)、listHas、System.exit)；Gatekeeper Constraint 是動態 CRD 無單一 kubectl get constraints，shell 先 get constrainttemplates 拿 kind 再逐 kind get + jq 抓 enforcementAction!=deny；CI 掃不到要補=政策邏輯對不對(Rego/pattern 可能永遠通過/抓錯欄位靠單元測試)、exempt 該不該給(只能抓過寬抓不到該不該)、既存存量(status.violations/PolicyReport 執行期產物)；執行期承 Day16 audit/PolicyReport+deny 事件進 SIEM 對安全政策實際處於 dryrun/Audit(P1)/既存違規數突增(有人往 exempt 或走 subresource 塞)/deny 對特定 ns/user 尖峰(探政策沒生效)告警。誤區表(部署=在擋/Audit 較安全先觀察/failurePolicy:Fail 政策就穩/加 team 進 excludedNamespaces 省事/exempt role/SA 較精準/mutate 修再 validate 檢查/mutate 預設一定安全/只攔 CREATE 夠/攔 Pod=攔所有危險操作/match Pod 則 Deployment 自然被管/上線 enforce 叢集就乾淨/background:false 沒差/語法對=邏輯對)+Code Review checklist 四區(政策真在擋承 Day91/攔截面完整承 Day91 mutating 順序/豁免與存量承 Day07/49/稽核承 Day16)+測試(opa test/kyverno test 把 policy 當程式碼、enforce 迴歸改 Audit 斷言 CI 判紅、UPDATE 繞過演練建合規再 patch 違規斷言被擋、subresource 演練 kubectl debug 注 privileged 斷言被擋、mutation 順序測試送原始違規斷言預期結果別被 silent fix、exempt 後門演練往豁免 ns 部署違規斷言不被攔+有審核到期、既存存量盤點 audit/background scan 斷言列出既存違規 background:false 斷言掃不到) | Kubernetes 內建 Pod Security Admission(PSA)/Pod Security Standards：privileged/baseline/restricted 三等級·enforce/audit/warn 三模式·namespace label 驅動·built-in vs policy engine 取捨·誰能改 ns label 誰能降級(承 Day07)·exemptions 過寬(承本篇第四節)·只擋新 Pod 管不到既存(Day93)、Gatekeeper mutation(Assign) 與 validation 順序深入、Kyverno verifyImages/image 簽章驗證(承 Day18 供應鏈)、Kyverno generate 規則自動產生資源的風險、CEL validating admission policy(內建 ValidatingAdmissionPolicy 免 webhook)、policy 的 GitOps/版本控管與 drift、多 policy engine 併存衝突與順序 | 
| 93 | `content/資訊安全/Day93_Pod_Security_Admission_Standards.md` | Kubernetes 內建 Pod Security Admission(PSA) 與 Pod Security Standards：三等級(privileged/baseline/restricted)·三模式(enforce/audit/warn)·namespace label 驅動·built-in vs Gatekeeper/Kyverno 取捨·namespace label＝安全等級的信任邊界 | 新主題（PSA 首次介紹，接續 admission 系列；交叉 Day91/Day92/Day07/Day49/Day18/Day16） | 不重講 Day91 webhook 機制/failurePolicy、Day92 Gatekeeper/Kyverno 政策寫法；接續 admission 系列新主題(PSA 首次介紹)。定位＝Day91–92 外掛路線(要裝 engine/管 webhook 可用性/政策自己寫對)vs PSA 內建路線(apiserver 內建/預設開 v1.23 beta v1.25 GA/namespace label 驅動/三固定等級)，兩者正交且併用非二選一。PSA 做一件事＝Pod 建立更新當下比對 namespace 要求的 Pod Security Standard 等級違規依模式處理。三等級=privileged(不設限等於沒 PSA)/baseline(擋明顯逃逸：privileged 容器·host namespaces hostNetwork/hostPID/hostIPC·hostPath·hostPort受限·Unconfined seccomp·危險 sysctls·NET_BIND_SERVICE 外 caps)/restricted(baseline 之上再強制 runAsNonRoot:true·allowPrivilegeEscalation:false·capabilities drop ALL 只准 add NET_BIND_SERVICE·seccompProfile RuntimeDefault/Localhost·volume 類型限縮非 host)；baseline=不准逃出容器 restricted=容器內也最小權限。三模式(獨立可疊加)=enforce(違規直接拒但只套用到 Pod 不套用到 Deployment/工作負載資源)/audit(放行記 audit log annotation 承 Day16)/warn(放行回使用者可見警告；warn+audit 會套用到工作負載資源)；常見漸進配置 enforce baseline + warn restricted + audit restricted。namespace label 驅動=pod-security.kubernetes.io/enforce、audit、warn 三個 label + 對應 *-version(釘版本升級不改語意 vs latest 跟最新可能多擋)；沒貼 label 吃 cluster 端 AdmissionConfiguration(apiserver.config.k8s.io/v1) 內嵌 PodSecurityConfiguration(pod-security.admission.config.k8s.io/v1；1.23 v1beta1/1.25 v1) 的 defaults(enforce 未設預設 privileged＝沒防護)。實例=payments ns 貼 restricted 擋 hostNetwork/privileged/allowPrivilegeEscalation true/runAsNonRoot false/hostPath/add SYS_ADMIN，PSA 一次列出所有違規；合規版帶 seccomp RuntimeDefault/drop ALL/digest(承 Day18)；PSA 不管 resource limits/image latest tag/命名規範(回 Gatekeeper/Kyverno Day92)。built-in vs policy engine 取捨表(安裝維運/可用性風險/規則範圍/表達力/既存資源)=併用：PSA 當底盤(免裝·無 webhook 可維運·不會因 webhook 掛掉或被繞而失效·把 cluster default enforce 拉到至少 baseline)＋policy engine 補自訂(禁 latest tag/要 digest 承 Day18·resource limits·命名/label 規範)＝縱深。盲點①namespace label＝安全等級誰能改 label 誰能降級(信任邊界承 Day07 RBAC)=有 namespaces update/patch 權者可把 enforce restricted 改 privileged 或刪 label(退回 cluster 預設 privileged)、有 create 權者可直接生 privileged ns、同 Day91(誰能改 WebhookConfiguration)/Day92(誰在 exclude)；防禦=收 namespaces create/update/patch(尤其改 labels) 只給平台團隊+用 policy engine 攔 namespace UPDATE 禁把 enforce 改 privileged/刪除(PSA 守不了自己)+稽核 label 變更 P1。盲點②exemptions 後門(承 Day92 第四節)=PodSecurityConfiguration.exemptions.usernames/runtimeClassNames/namespaces 完全跳過 PSA 連 label 都不看；namespaces exempt=合規逃生艙(貼 restricted 也不看)、usernames=按身分最隱蔽(承 Day07/49 豁免本身是存取控制決策)、runtimeClassNames=最易漏；跟 Day92 exempt 不同點=藏在 apiserver admission config 裡 kubectl 看不到改不到(好處不易被業務 RBAC 動·壞處難稽核易被遺忘)。盲點③enforce 只套用到 Pod 不套用到 Deployment＋只擋新寫入(承 Day91/92 邊界)=違規 Deployment apply 建成功無錯但 ReplicaSet 建 Pod 才被擋→Deployment 存在但 READY 0/N Pod 靜默起不來錯藏 RS event(FailedCreate)→enforce 必搭 warn(至少同級) 讓工作負載層違規在 apply 時露出；relabel 既存違規 Pod 續跑不驅逐只在 relabel 當下回警告，真正擋的是之後新 Pod/既存 Pod 下次重建→先 warn/audit 盤乾淨再切 enforce。Day16 稽核=kubectl get namespaces -o json(cluster-scoped 一次撈 List) 掃①沒 enforce label(判紅=吃預設多半 privileged 門開著)②enforce privileged(判紅明確降級)③enforce 弱於最低要求 baseline(判黃)④有 enforce 沒 warn(判黃工作負載層違規靜默)；Go(ns/nsList struct、rank map privileged<baseline<restricted、controlPlane 排除 kube-system 等、os.Exit1)/Java(Jackson readTree ProcessBuilder、RANK Map.of、CONTROL_PLANE Set.of、System.exit；1.8 換傳統寫法)；CI 掃 namespace label 掃不到要補=exemptions 過寬(在 apiserver admission config/IaC 不在 namespace)、等級夠不夠(人審 baseline vs restricted)、既存違規存量(audit log/warn 輸出)；執行期承 Day16 audit annotation+namespace label 變更+exemptions 異動進 SIEM 對 namespace 被降級成 privileged/被移除 label(P1)、audit 違規 Pod 數突增、exemptions 被動告警。誤區表(裝 K8s 就有 PSA 保護/沒 label 中性沒差/PSA 取代 Gatekeeper-Kyverno/有 Gatekeeper 就不用 PSA/貼 restricted 就穩了忽略 label 寫入權/exemptions 只是系統小設定/enforce 會擋違規 Deployment/只設 enforce 而 warn-audit 可省/貼 restricted 既存違規 Pod 被清/enforce-version 用 latest 就好/restricted 有管 limits 與 tag)+Code Review checklist 四區(PSA 真在防：cluster default enforce≥baseline·業務 ns 有 enforce 非 privileged·搭 warn·釘 version/信任邊界與豁免承 Day07/49/Day92：收 namespaces 寫入權·policy engine 反守 label·exemptions 審核到期/與 policy engine 併用：PSA 底盤+engine 自訂·特權工作負載隔離單獨 ns/稽核承 Day16)+測試(restricted 擋得住、Deployment 靜默吞違規演練最易誤判、降級信任邊界演練業務身分改 enforce 應被擋、既存 Pod 不驅逐 relabel 只警告重建才擋、exemptions 後門演練貼 restricted 也放行、cluster default 演練沒 label=門開、version pinning 演練釘版本升級行為不變) | ValidatingAdmissionPolicy(VAP)+CEL 內建免 webhook 可寫自訂規則的第三條路(ValidatingAdmissionPolicy validations CEL + ValidatingAdmissionPolicyBinding matchResources/paramRef·validationActions Deny/Warn/Audit 留 Warn/Audit 等於沒擋·failurePolicy 仍在·CEL 表達力天花板不能呼叫外部/查跨資源·v1.30 GA，Day94)、用 Gatekeeper/Kyverno 反守 PSA label(攔 namespace UPDATE 禁把 enforce 降 privileged/刪除)、PSP(PodSecurityPolicy)→PSA 遷移路徑、多叢集 PSA 基線用 GitOps 統一與 drift、runtimeClass 強隔離(gVisor/Kata)搭 exemptions 的取捨、OpenShift SCC(SecurityContextConstraints) 與 PSA 對比、PSA warn/audit 漸進收斂到 restricted 的 rollout 策略 |
| 94 | `content/資訊安全/Day94_ValidatingAdmissionPolicy_CEL.md` | Kubernetes 內建 ValidatingAdmissionPolicy(VAP)＋CEL：內建×可自訂×免 webhook 的第三條 admission 路·Policy＋Binding 拆分·validationActions Deny/Warn/Audit·CEL 表達力天花板·failurePolicy 仍在 | 新主題（VAP 首次介紹，接續 admission 系列；交叉 Day91/Day92/Day93/Day90/Day18/Day07/Day16） | 不重講 Day91 webhook 機制/failurePolicy 成因、Day92 Gatekeeper ConstraintTemplate+Rego/Kyverno 政策寫法、Day93 PSA 三等級三模式；接續 admission 系列新主題(VAP 首次介紹)。定位＝admission 三條路二維表(規則可否自訂×要否養 webhook)：Day91–92 外掛 webhook 路線(表達力最強但要養 webhook：caBundle 憑證輪替承 Day79、可用性單點承 Day91、啟動死結、每次寫入多一趟網路承 Day72)、Day93 PSA 內建但固定(三等級不能自訂)、Day94 VAP 補第三象限「內建＋可自訂＋免 webhook」(apiserver in-process 跑 CEL，v1.29 beta/v1.30 GA，admissionregistration.k8s.io/v1)。邊界照舊：VAP 同所有 admission 只擋新寫入管不到既存，且無內建 audit controller 存量靠 validationActions:[Audit] 寫 audit log 撈(承 Day16)。VAP 怎麼寫＝規則拆兩物件：ValidatingAdmissionPolicy(規則模板，光有它不生效)＝matchConstraints.resourceRules(apiGroups/apiVersions/operations 安全政策必含 CREATE+UPDATE 承 Day92/resources)+validations[](expression CEL/message 固定字串/messageExpression 動態 CEL/reason 對應 HTTP Unauthorized/Forbidden/Invalid/RequestEntityTooLarge)+variables[](抽共用運算式 validations 引用 variables.xxx)+failurePolicy(Fail 預設/Ignore)+可選 paramKind；ValidatingAdmissionPolicyBinding(綁資源決定動作)＝policyName+matchResources(namespaceSelector/objectSelector/excludeResourceRules 在 policy 之上再收窄)+validationActions[](陣列可組合 Deny/Warn/Audit)+可選 paramRef(name/namespace/parameterNotFoundAction Allow=後門 Deny=fail-closed)。CEL 可用變數＝object(本次寫入 UPDATE 是新版)/oldObject(UPDATE 前 CREATE 為 null 可寫「只准往嚴改」)/request(AdmissionRequest 詮釋 operation/userInfo)/params(paramRef 綁進的參數)/namespaceObject(讀 ns label)/authorizer(CEL 內做 RBAC 檢查)。範例用 Day92 同款規則(禁 :latest 承 Day18/要 resource limits PSA 管不到承 Day93)＝object.spec.containers.all(c, !c.image.endsWith(':latest'))、要 digest c.image.contains('@sha256:')、要 limits has(c.resources.limits.cpu/memory)；一個 validations 只表達一件事別用 OR 短路自己。為什麼第三條路＝免 webhook 把 Day91 一整包負擔全消(無 caBundle 憑證/無可用性單點/無啟動死結/in-process 無網路往返延遲更低)；換回 CEL 天花板＝不能外呼(無 http.send 對比 Rego)、不能任意跨資源查(只看 object/oldObject/namespaceObject/authorizer/params，跨資源如 Ingress host 撞號回 Gatekeeper data.inventory/webhook)、有 cost budget(巢狀 all() 太貴被拒/中止)；故簡單~中等只看物件本身→VAP(免 webhook)、要外呼/跨資源/重邏輯→回 webhook/Gatekeeper/Day90 ext_authz，分層併用非二選一。四盲點全前幾天重演：①validationActions 留 Warn/Audit＝等於沒擋(Day92 dryrun/Audit、Day93 warn/audit 第三次翻版，觀察完忘切 Deny＝常駐不設防；與 Day91 failurePolicy:Ignore 差別＝Ignore 是出錯才放行、Warn/Audit 是沒出錯本來就不擋更徹底)；②failurePolicy 仍在(CEL 會編譯/執行期型別錯/超 cost，Fail 預設 fail-closed 承 Day07/Ignore=出錯 fail-open 一條寫錯政策靜默失效)；③matchConditions(CEL 端閘門寫窄漏擋寫寬多成本)/matchResources(只 match pods 有 Deployment 靜默 apply 成功 Pod 起不來邊界承 Day93)/paramRef(parameterNotFoundAction:Allow 後門，參數即政策誰能改參數誰能改政策承 Day07)；④CEL 邏輯寫錯永遠通過(語法對≠邏輯對，用 OR 短路，靜態掃不到要單元測試承 Day92 語法對≠邏輯對)。Day16 稽核＝kubectl get validatingadmissionpolicybindings/-policies -o json(皆 cluster-scoped)掃①binding validationActions 沒 Deny(判紅只 Warn/Audit 或沒設)②policy failurePolicy=Ignore(判紅，未設=預設 Fail OK)；Go(bindingList/policyList struct、hasDeny helper、FailurePolicy 空=Fail、os.Exit1)/Java(Jackson readTree ProcessBuilder、hasDeny 遍歷 array、failurePolicy asText("Fail") 預設、System.exit；1.8 換傳統寫法)；CI 掃不到要補=CEL 邏輯對不對(永遠通過語法對掃不出靠單元測試)、matchConditions/matchResources 範圍、paramRef 參數物件寫入權(承 Day07)、既存存量(VAP 無 audit controller 靠 Audit 寫 audit log 撈)；執行期承 Day16 Deny 事件/Audit annotation/政策變更進 SIEM 對政策實際只 Audit(P1)/某政策 deny 歸零(被改 Audit 或 binding 刪)/CEL 錯誤率上升(可能對某輸入 fail-open)告警。誤區表(GA 就取代 Gatekeeper/Kyverno=CEL 天花板/建 policy 就在擋=沒 binding 不生效/Audit 較安全先觀察=忘切 Deny/沒 webhook 就沒 failurePolicy=CEL 會出錯/CEL 語法對=規則對/match pods 管住所有部署=Deployment 靜默/paramRef 找不到會擋=看 parameterNotFoundAction/in-process 沒延遲=有 cost budget/VAP 清既存=只擋新寫入/參數 ConfigMap 是普通設定=誰改參數誰改政策/有 VAP 不用 PSA=併用)+Code Review checklist 四區(政策真在擋承 Day92/93：有 binding·含 Deny·failurePolicy Fail/攔截面完整承 Day92/93：operations 含 UPDATE·Deployment 邊界·matchConditions 範圍/CEL 與參數承 Day07/18：一 validations 一件事+has() 守空值·過 type checking·parameterNotFoundAction Deny+參數寫入權/與其他 admission 併用承 Day93：VAP 自訂+PSA 底盤+複雜回 engine·稽核承 Day16)+測試(政策真在擋演練最重要 Deny 送違規斷言被拒改 Audit 斷言放行有 log、CEL 邏輯單元測試 dry-run=server/cel-go 餵樣本抓永遠通過、UPDATE 繞過建合規再 patch 違規、Deployment 靜默 apply 違規 Deployment 斷言 READY 0/N、failurePolicy 演練故意 CEL 出錯 Fail 擋 Ignore 放行、參數後門 parameterNotFoundAction:Allow 刪參數放行、稽核迴歸改 Audit/Ignore 斷言 CI 判紅) | MutatingAdmissionPolicy(MAP)+CEL 內建免 webhook 做 in-process 改寫的第四條路(MutatingAdmissionPolicy+Binding·mutations[] patchType ApplyConfiguration(server-side apply merge)/JSONPatch·v1.32 alpha v1.34 beta admissionregistration.k8s.io/v1beta1·mutation-before-validation 讓 mutate 補預設粉飾違規承 Day91/92·reinvocation 隨機順序 mutation 必冪等承 Day91/22·failurePolicy/matchConditions 同 VAP，Day95)、用 Gatekeeper/Kyverno 或 VAP 反守 PSA/VAP 自身 label 與參數(承 Day93/本篇 paramRef)、VAP type checking 與 CEL cost budget 深入、authorizer/namespaceObject 進階 CEL 授權情境、VAP GitOps/版本控管與跨叢集 drift、CEL 運算式單元測試工具鏈(cel-go/kubectl dry-run)、VAP 與 Gatekeeper/Kyverno 遷移(Gatekeeper 產生 VAP)、多 admission plugin 併存的評估順序與衝突 |
| 95 | `content/資訊安全/Day95_MutatingAdmissionPolicy_CEL.md` | Kubernetes 內建 MutatingAdmissionPolicy(MAP)＋CEL：內建×可自訂×免 webhook 的第四條 admission 路(改寫側)·Policy＋Binding 拆分·mutations[] patchType ApplyConfiguration/JSONPatch·mutation-before-validation 粉飾違規·reinvocation 冪等·failurePolicy 仍在 | 新主題（MAP 首次介紹，接續 admission 系列，補齊改寫側；交叉 Day94/Day91/Day92/Day93/Day22/Day18/Day16/Day07/Day72/Day79） | 不重講 Day91 mutating webhook 機制/reinvocationPolicy、Day92 Gatekeeper Assign/Kyverno mutate 寫法、Day93 PSA、Day94 VAP validation 側與 CEL 基礎語法；接續 admission 系列新主題(MAP 首次介紹)。定位＝把 admission 四條路擺齊(規則可否自訂×要否養 webhook×能改寫或只驗證)：Day91 自寫 webhook(表達力最強要養 caBundle 承 Day79/可用性單點/啟動死結/多一趟網路承 Day72)、Day92 外掛 engine(Gatekeeper/Kyverno validate+mutate 要裝要管 webhook)、Day93 PSA(內建固定三等級只管 Pod 不能自訂只驗證)、Day94 VAP(內建+可自訂+免 webhook 但只驗證)、Day95 MAP 補改寫側(同一條 in-process 免 webhook 的路，MAP 之於 VAP＝mutating webhook 之於 validating webhook，v1.32 alpha/v1.34 beta，admissionregistration.k8s.io/v1beta1，beta 預設開 feature gate 但仍需 --runtime-config=admissionregistration.k8s.io/v1beta1=true)。MAP 怎麼寫＝拆兩物件：MutatingAdmissionPolicy(定義改什麼)＝matchConstraints.resourceRules(operations 安全政策含 CREATE+UPDATE 承 Day92)+failurePolicy(Fail 安全 mutation 該 Fail/Ignore=出錯不補 fail-open)+reinvocationPolicy(Never/IfNeeded)+mutations[](每條 patchType)；MutatingAdmissionPolicyBinding(套哪裡)＝policyName+matchResources(namespaceSelector/exempt 範圍最小化)+可選 paramRef，注意無 validationActions/無 Audit 檔位一 apply 就真改要 --dry-run=server 看 diff。兩種 patchType＝ApplyConfiguration(CEL 回 Object{} apply configuration，走 server-side apply merge，天生冪等，首選，範例補 spec.securityContext.seccompProfile.type=RuntimeDefault)/JSONPatch(CEL 回 JSONPatch{op,path,value} RFC6902，威力大但要自扛冪等：無條件 add /- 到陣列尾 reinvocation 會累加、path 的 / ~ 要跳脫 ~1 ~0 用 jsonpatch.escapeKey 依版本)。為什麼還是免 webhook 那條路＝同 VAP 甩掉 Day91 一整包(caBundle 承 Day79/可用性單點/啟動死結/網路往返延遲承 Day72)in-process 更低延遲；換回 CEL 天花板＝不能外呼/不能跨資源查/有 cost budget，mutation 專屬限制＝只能用 object/params 既有資料算補值(去 KMS 拿 key/查 registry 拿 digest 算不出→回 webhook)，MAP 與 webhook 分層併用非二選一。MAP 盲點(mutation 半邊老坑 CEL 版重演)＝①mutation 必須冪等(reinvocation 順序次數不保證可重跑，ApplyConfiguration 天生冪等 JSONPatch 無條件 add 非冪等，承 Day91 reinvocationPolicy/Day22 TOCTOU 別假設只跑一次)；②mutation 在 validation 之前=補預設把違規粉飾掉(後面 VAP/PSA 看到被補好的物件非原始違規輸入，攔截降級成粉飾，最危險，判準＝補上去會不會改變後面 validation 結論會就別用 mutation 改用 validation 擋，安全違規要 VAP/PSA 擋下回報 mutation 只補無關判定預設 label/imagePullPolicy，承 Day92 盲點③)；③承 VAP＝failurePolicy:Ignore 安全 mutation fail-open 該 Fail 承 Day91、operations 漏 UPDATE 先建後 patch 繞過承 Day92、matchConditions/selector 寫寬寫窄、無 dry-run 檔位上線前 --dry-run=server。Day16 稽核＝Go client-go(MutatingAdmissionPolicies().List v1beta1)/Java(fabric8 或 official client)掃(a)mutation 觸及安全關鍵欄位 securityContext/seccompProfile/privileged/capabilities 等=粉飾判紅(b)failurePolicy:Ignore 判紅(c)JSONPatch 無條件 add /- 非冪等嫌疑判警，跟 Day94 VAP/Day92 policy 稽核同一 pipeline，MAP 政策變更進 SIEM。誤區(有 MAP 補就不用 PSA/VAP 擋=粉飾/mutation 只跑一次=reinvocation/MAP 有 Audit 可觀察=無 validationActions 用 dry-run/JSONPatch 較靈活都用它=能 ApplyConfiguration 就別 JSONPatch/beta 預設開就能用=仍需 --runtime-config 且托管叢集要確認)+Code Review checklist(改的欄位與安全判定無關/優先 ApplyConfiguration 確認冪等/failurePolicy Fail/operations 含 UPDATE/reinvocationPolicy 與冪等一致/exempt 最小化/上線前 dry-run/CI 納入/與 VAP-PSA-webhook 分工清楚)+測試(冪等演練最重要跑兩次結果同、粉飾演練 privileged+缺 seccomp 只掛 MAP vs 加 VAP、failurePolicy 演練、UPDATE 繞過、dry-run diff、稽核迴歸) | admit 時映像簽章驗證 Sigstore cosign+Kyverno verifyImages/policy-controller(keyless Fulcio+Rekor 承 Day77 CT/keyful、驗過搭 mutation 釘 digest 承本篇/Day18、Day96)、MAP 與 mutating webhook/Gatekeeper Assign 遷移與並存評估順序、reinvocation 導致的 mutation 衝突與收斂分析、server-side apply field ownership 與 MAP 的交互、CEL cost budget 對複雜 mutation 的限制、attestation(SBOM/SLSA provenance 承 Day18)在 admission 一起驗、MAP GitOps/版本控管與跨叢集 drift、多 admission plugin(mutating webhook+MAP+Gatekeeper mutate)併存的 mutation 評估順序與冪等保證 |
| 96 | `content/資訊安全/Day96_Sigstore_Image_Signature_Admission.md` | Sigstore/cosign 映像簽章在 admit 那一刻驗證(Kyverno verifyImages / policy-controller ClusterImagePolicy)：把 Day18 供應鏈信任落到 admission·keyless(Fulcio 短命憑證＋Rekor 透明日誌)vs keyful(自管公鑰)·CI cosign sign 簽 digest＋cosign attest 帶 SBOM/SLSA·mutateDigest 把 tag 釘成 digest·四大盲點(failurePolicy fail-open／只驗有沒有簽不驗誰簽的 identity／TUF root 信任根沒固定／只攔 CREATE 漏 UPDATE 與既存) | 新主題（映像簽章驗證與 Sigstore 首次介紹，接續 admission 系列並接回 Day18 供應鏈；交叉 Day18/Day91/Day92/Day95/Day77/Day76/Day78/Day15/Day22/Day72/Day16） | 不重講 Day18 供應鏈攻擊分類/SBOM/Trivy/govulncheck 掃描入門、Day91–95 admission 機制本身(webhook 是什麼/mutating 為何在 validating 前/failurePolicy 成因/Kyverno 政策骨架)；聚焦「admit 那一刻該驗映像什麼、怎麼被寫穿」。定位＝把 admission(Day91–95 會攔會改)最有價值的用途落地：驗映像有沒有簽(存在性)、是誰簽的(identity，最易漏)、admit 的 digest 對不對(mutateDigest 釘死杜絕 TOCTOU)。keyless 心法：Fulcio 公開所以「有簽章≠可信」，必鎖 issuer＋subject | Day97 provenance/SLSA attestation 在 admit 驗來歷(cosign verify-attestation＋in-toto predicate＋Kyverno attestations conditions)、私有 Sigstore 自建 Fulcio/Rekor/TUF root、cosign policy(CUE/Rego)進階條件、admission 驗簽的效能與可用性硬化(本地鏡像 Rekor)、SBOM attestation 內容驗證與漏洞閾值 gate |
| 97 | `content/資訊安全/Day97_SLSA_Provenance_Attestation_Admission.md` | 在 admit 那一刻驗 provenance／SLSA attestation 的「內容」(Kyverno verifyImages attestations.conditions／policy-controller CUE-Rego policy)：把供應鏈信任從「誰簽的」推進到「怎麼 build 的」·in-toto Statement 三層(subject.digest／predicateType／predicate)·SLSA provenance v0.2 vs v1 欄位路徑差異·對 buildType/repository/ref/builder.id 逐條下斷言·四大盲點(只驗有 provenance 不驗 conditions 內容／builder.id-buildType 沒鎖 對的原始碼錯的 builder／predicate 欄位信任邊界 build 自填欄位不可信／SLSA 等級宣稱與實際 build 隔離脫節) | 延伸／接續（provenance-attestation 內容驗證首次介紹，承 Day96 預告接續 admission＋供應鏈系列；交叉 Day96/Day18/Day22/Day92/Day91/Day16/Day07） | 不重講 Day96 簽章驗證機制(有沒有簽/是誰簽/鎖 issuer＋subject/mutateDigest 釘 digest)與 Day18 供應鏈-SBOM-掃描入門；聚焦「admit 那一刻驗 attestation 內容(不是存在性)」。核心攻擊情境＝fork PR／被污染 workflow 誘導合法身分(正牌 release.yml)簽一個 identity 一字不差卻帶後門的映像＝簽章政策挑不出毛病只有讀 build 履歷內容才擋得住。定位＝Day96 是地基(驗 attestation 第一關仍是驗簽鎖 issuer＋subject)第二關才是新增的對 predicate 欄位下 conditions。分工心法：簽章證明「可信身分交付」provenance 證明「可信方式從可信原始碼做出」。predicate 可信前提＝產 provenance 的實體要在 build 能污染範圍之外(SLSA build level／hermetic-isolated build)否則 build script 自產＝自己幫自己開證明 | Day98 SBOM attestation 內容驗證與 CVE 漏洞閾值 admission gate(cosign attest spdxjson/CycloneDX predicate＋Kyverno-policy-controller 對元件清單與 severity threshold 下 gate)、build-time 掃描 vs admit-time gate 分工與 CVE 資料新鮮度、私有 Sigstore 自建 Fulcio/Rekor/TUF root、policy-controller CUE/Rego 進階 predicate 斷言、admission 驗證效能與可用性硬化(本地鏡像 Rekor)、多 predicate 型別(VSA/verification summary)在 admit 一起驗 |
| 98 | `content/資訊安全/Day98_SBOM_Vulnerability_Admission_Gate.md` | 在 admit 那一刻用 SBOM attestation 與 vuln-scan attestation 對「成品內容」下 CVE 漏洞閾值 gate(Kyverno verifyImages attestations.conditions／policy-controller)：驗完 Day97「怎麼 build 的」再驗「裡面裝了什麼、現在安不安全」·in-toto Statement predicateType 分 SPDX SBOM(packages[] 禁用套件／授權) vs CycloneDX-cosign vuln(vulnerabilities[] severity threshold)·核心心法 build 掃過≠admit 該放(CVE 資料庫會變 digest 沒變但漏洞狀態變→驗掃描報告新鮮度／掃描結果會被繞過造假→admit 再驗並鎖 attestor／admit 是唯一涵蓋所有部署路徑的必經之路)·四大盲點(只驗有報告不驗 conditions 內容／SBOM-映像不符 綁 digest+mutateDigest／threshold 一刀切 CVSS vs 可利用性脫節 搭 VEX+受管白名單／白名單變永久後門 需到期-負責人-稽核) | 延伸／接續（SBOM/漏洞 gate admit-time 內容驗證首次介紹，承 Day97 預告接續 admission＋供應鏈系列；交叉 Day97/Day96/Day18/Day22/Day92/Day91/Day16/Day07） | 不重講 Day18 SBOM／Trivy／govulncheck 掃描入門，不重講 Day96/97 簽章／provenance 機制；聚焦 admit 消費 SBOM／vuln-scan attestation 對元件清單與 severity threshold 下 gate、build-time 掃描 vs admit-time gate 分工與 CVE 資料新鮮度(SLA 化強制重掃)、驗 SBOM 第一關仍是 Day96 驗簽鎖 identity 第二關才對 predicate 下 conditions。稽核(承 Day16)Go／Java 掃「有驗簽卻沒驗 SBOM/vuln 內容(缺 conditions)／validationFailureAction=Audit／沒鎖 attestors identity」跟 Day92/94/95/96/97 同一 pipeline，政策與白名單變更進 SIEM | Day99 VSA(Verification Summary Attestation)把多份 attestation(簽章＋provenance＋SBOM＋漏洞)驗證預先做完 admit 只驗一份 summary 的 verificationResult＝PASSED、信任邊界搬到產 VSA 的 verifier(identity／policy.uri-digest／VSA 新鮮度變新必驗欄位)、admit 效能與可用性硬化(本地鏡像 Rekor／referrers 快取／驗證結果快取 TTL 與撤權延遲取捨 承 Day72/16)、多 predicate 型別在 admit 一起驗、私有 Sigstore 自建 Fulcio/Rekor/TUF root |
| 99 | `content/資訊安全/Day99_VSA_Verification_Summary_Attestation.md` | VSA(Verification Summary Attestation)把「做驗證」與「消費驗證結果」拆開(SLSA predicateType https://slsa.dev/verification_summary/v1)：Day96~98 admit 要驗一大堆 attestation(簽章＋provenance＋SBOM＋漏洞)拖垮延遲與可用性(承 Day72/91)，改由可信 policy verifier 在 admit 之外預先全驗完產一份 VSA(記錄 digest 用哪套 policy 驗過／verificationResult=PASSED／verifiedLevels)，admit 只驗這一份 VSA 的簽章＋PASSED·VSA predicate 欄位(verifier.id／timeVerified／policy.uri-digest／inputAttestations／verificationResult／verifiedLevels，v0.2 vs v1 路徑差異)·admit 只宣告 VSA 一種型別否則收斂白費·四大盲點(只驗有 VSA 不驗 verificationResult=PASSED／不鎖 verifier identity 與 policy.digest 用舊弱 policy 洗 VSA／VSA 過期 stale PASSED 是當時不是現在／白費收斂或退回 CI 繞過 kubectl apply) | 延伸／接續（VSA 首次介紹，承 Day98 預告接續 admission＋供應鏈系列，聚焦驗證結果收斂消費與 admit 效能可用性；交叉 Day96/Day97/Day98/Day91/Day92/Day94/Day72/Day78/Day22/Day16） | 不重講 Day96/97/98 各自的驗證機制(驗簽／provenance 內容／SBOM-vuln gate)；聚焦①VSA 是什麼解決什麼(拆開做驗證 vs 消費驗證結果，verifier 預先驗完產 summary，admit 只驗一份 PASSED，驗證沒消失只是搬到 admit 之外)②信任邊界搬家代價(信任集中到產 VSA 的 verifier，verifier identity／policy 版本 policy.digest／VSA 新鮮度 timeVerified 三欄從參考升格必驗，承 Day97 信任根要釘死)③效能可用性硬化(本地鏡像 Rekor／OCI referrers 快取／驗證結果快取 TTL≤撤權空窗 承 Day78 soft-fail，新鮮度閘 vs 快取 TTL 是不同旋鈕)。Kyverno verifyImages attestations 只列 VSA type＋conditions(verificationResult==PASSED／鎖 policy.digest／time_since 新鮮度)＋attestors 鎖 verifier keyless issuer＋subject。稽核(承 Day16)Go 掃執行中 Pod status.containerStatuses[].imageID by-digest 有無新鮮且 PASSED VSA(cosign verify-attestation)、Java 掃 VSA 政策 conditions 是否覆蓋 PASSED／policy／新鮮度＋attestors，跟 Day92~98 同一 pipeline，verifier policy 變更進 SIEM | Day100 透明日誌(transparency log)信任與監控：inclusion proof(Merkle 路徑證明 entry 真被收錄)＋consistency proof(日誌只 append 不改寫歷史)、split-view／分裂視圖攻擊(對受害者與稽核者給不同視圖，光靠 inclusion proof 擋不住要靠 witness／gossip 多方見證 log root)、monitor 持續拉 log 驗 consistency 對未授權 entry 告警(承 Day35 主動偵測／Day16 SIEM)、自建鏡像 Rekor 會不會說謊、Merkle inclusion 驗證 Go／Java 範例；其他：TUF root 信任根與金鑰輪替(承 Day96/97 信任根釘死)、VSA verifier 本身的供應鏈與 policy 版控治理、VEX 在 admit 消費(承 Day98)、跨叢集／GitOps VSA drift |
| 100 | `content/資訊安全/Day100_Transparency_Log_Trust_Monitoring.md` | 透明日誌(transparency log)信任模型與監控：Day96 keyless 信任最後一節掛在 Rekor(SET 收據)、Day99 又為可用性自建鏡像 Rekor——付帳「憑什麼相信那面鏡子沒說謊」·①inclusion proof(RFC6962 Merkle audit path，0x00 葉/0x01 節點 domain separation，重算 root 比對「經 Rekor 公鑰驗簽的 checkpoint」而非只驗 SET 承諾)＋consistency proof(日誌只 append 不改寫歷史，抓 stale/rollback，treeSize 單調遞增)·②split-view/分裂視圖(作惡日誌對受害者與稽核者各給自洽假視圖，inclusion/consistency 只能自證單一觀察者對單一日誌，要靠 witness/gossip 多方副署同一 root 達門檻)·③監控 vs 阻擋分工(透明日誌是事後可稽核非事前阻擋，admit fail-closed 擋沒進日誌的、monitor 持續驗 consistency+比對 witness+掃未授權 entry 只告警不擋)·四大盲點(只驗 SET 不驗 inclusion/拿裸 root 當基準/自建鏡像不接 witness split-view 門檻歸零/有 admit 就不用 monitor) | 延伸篇（承 Day99 §5 自建鏡像 Rekor 付帳，聚焦透明日誌本身的信任模型；明確不重講 Day96 keyless 驗簽與 Day77 CT/Merkle/monitor 入門，接續 Day77 刻意略過的 Auditor 角色——自建鏡像後賴不掉；交叉 Day77/Day96/Day99/Day78/Day72/Day35/Day16/Day91/Day22/Day85） | Rekor SET 是承諾非兌現(同 Day77 SCT)·inclusion proof 只證「這筆在這個 root」/consistency proof 證「歷史 append-only」二表對照·RFC6962 0x00/0x01 domain separation 防第二原像·自建鏡像三種作惡 stale/rollback/fork·split-view 為何 inclusion 擋不住(每視圖各自自洽)·witness 副署+門檻 threshold+gossip·monitor 四檢查(驗 checkpoint 簽章鎖 Rekor 公鑰/consistency+treeSize 遞增/witness 門檻/掃未授權 identity entry 承 Day35)·admit 與 monitor 職責分離(monitor 只告警避免變可用性單點 承 Day72)·Go 重算 Merkle root 驗 inclusion(hashLeaf/hashNode/recomputeRoot，示意非手刻、生產用 sigstore 庫)·Java monitor(consistency+witness threshold+未授權 entry 告警)·偵測型控制通病「monitor 掛掉跟沒壞事長一樣」→成功也記指標+N 小時未執行告警+陽性測試存在證明(承 Day77)·CI 掃 admit 驗到哪層+排程 monitor·SIEM(consistency 失敗/treeSize 倒退/witness 不足/未授權 entry/monitor 未執行) | Day101 TUF(The Update Framework)與 Sigstore 信任根散布輪替金鑰折損復原(root.json threshold 簽章/root rotation N+1 被 N 簽/TOFU 開機/root-targets-snapshot-timestamp 角色分離關爆炸半徑，承 Day96 TUF root 沒固定盲點)·Rekor v2 tiled log(tlog-tiles/C2SP checkpoint 格式)·witness 網路獨立性盡職調查·gossip 協定實作·自建鏡像同步延遲容忍度(同 Day99 TTL≤空窗取捨)·CT v2 RFC9162 對照 |
| 101 | `content/資訊安全/Day101_TUF_Trust_Root_Distribution_Rotation.md` | TUF(The Update Framework)信任根散布／輪替／金鑰折損復原：把 Day96~100 一路往上收斂的信任(Fulcio／Rekor／CT／witness 公鑰)用一份可驗可輪替可復原的 root.json 發下來·①散布(別寫死＝金鑰輪替要重編譯全世界+無門檻無復原／別只 HTTPS 抓＝TLS 保傳輸不保來源+擋不了 freeze-rollback／TUF 疊 TLS 上補來源可信與新鮮度，Sigstore 用 trusted_root.json 當 target 發 Fulcio／Rekor／CT 公鑰)·②threshold 多簽(root 每角色 keyids+threshold，distinct 有效簽章≥threshold，去重+只認授權 keyid+同 keyid 計一次)·③root rotation(N+1 必須被舊 root 門檻 AND 新 root 門檻都簽過才收，版本嚴格遞增防 rollback，MAX_UPDATES=1024，中間版過期算數效期只驗最終版讓離線久 client 補鏈)·④TOFU 開機(第一份 root 無更舊版可驗只能自驗，靠帶外內嵌+pin 指紋，Sigstore 內嵌進 cosign)·⑤角色分離 root／targets／snapshot／timestamp(離線門檻 vs 線上頻繁威力關小，爆炸半徑切四段)·⑥金鑰折損復原(非 root 角色被偷＝root 簽新版換 keyids+clearMetaDueToKeyRotation／root 被偷＝門檻內其他金鑰簽新版踢掉，復原從發版事件降級成一次簽署) | 新主題（TUF 信任根首次介紹，承 Day96／97／99／100 反覆點名的「TUF root 沒固定」盲點，把供應鏈信任收斂的最後一顆釘子釘上；明確不重述 Day100 inclusion／consistency proof 與 Day96 keyless 驗簽；交叉 Day96／Day99／Day100／Day78／Day10／Day15／Day16／Day80／Day81／Day77） | 寫死公鑰 vs HTTPS 抓 vs TUF 三者缺口對照·四角色職責／金鑰上線程度／被偷後果表·threshold 驗證心法(sigstore-java verifyDelegate 去重+只認授權 keyid+門檻)·root rotation loop(舊簽新 AND 新簽新／版本遞增 RollbackVersionException／中間版過期合法效期只驗最終版＝go-tuf UpdateRoot 註解+sigstore-java throwIfExpired 在迴圈後／hasNewKeys→clearMetaDueToKeyRotation)·TOFU 第一份 root 帶外內嵌+pin sha256·金鑰折損兩情境復原·Go go-tuf v2(config.New+updater.New+Refresh 高階／trustedmetadata.New+UpdateRoot+UpdateTimestamp 低階)·Java sigstore-java(dev.sigstore.tuf.Updater builder+update()，需 JDK11+，1.8 走 sidecar 承 Day80／81)·拒收過期擋 freeze(timestamp 短效，別拉長 TTL／吞過期錯 承 Day78／99)·repo URL＝SSRF 新攻擊面(釘死不吃輸入／egress allowlist／size-timeout 承 Day10)·Day16 稽核(root 版本下限／效期剩餘比例／refresh 成功也記指標+N 小時未成功告警／SIEM：版本跳變-threshold 變小-效期到期-refresh 失敗-指紋不符)·誤區表·Code Review checklist·測試(門檻不足／未授權金鑰／rotation 斷鏈／rollback／freeze 過期最終版／中間版過期但最終有效仍能補到最新／第一份 root 指紋／egress 邊界) | 自建／私有 Sigstore 信任根運維與 TUF root-signing 金鑰儀式(離線門檻 root 金鑰保管／線上 vs 離線角色自動化邊界／自建信任治理與退場，Day102)·TUF delegations 與 targets 委派／hash bin·SPIFFE／SPIRE trust bundle bootstrap 與 TUF 對照·root key HSM／KMS 保管(承 Day83)·Sigstore trusted_root.json 內容(Fulcio／Rekor／CTlog 端點)驗證·cosign initialize／自建 TUF mirror·多 client 相容性與 root 版本升級管理 |
| 102 | `content/資訊安全/Day102_Private_Sigstore_Root_Signing_Ceremony.md` | 自建／私有 Sigstore／TUF 信任根運維：root-signing 金鑰儀式、離線門檻 root 金鑰保管與輪替治理 | 延伸／新主題（承 Day101 預告，發行端儀式與治理首次介紹，明確不重述 Day101 threshold 驗證／rotation loop／TOFU 補鏈機制；交叉 Day101／Day83／Day77／Day15／Day16／Day10／Day80／Day81／Day35） | 該不該自建與退場決策(多數團隊不該自建；自建 mirror≠自建 root)·公信實例外包了什麼(root 3-of-5 門檻／五 keyholder／YubiKey PIV 離線／一年2-3次 signing event／tuf-on-ci 走 sign 分支 PR 留證)·金鑰儀式(離線硬體產生+門檻多人各持+受控全程留證；與 Day83 SPIRE CA HSM 異同表：管 root-targets-snapshot-timestamp 四角色 vs 單一常態上線 CA 金鑰)·線上vs離線自動化邊界(威力與 effort 成反比：root-targets 離線人工，snapshot-timestamp 線上 KMS 自動重簽；線上金鑰只能是威力被關小的角色且私鑰不落地；go-tuf v2 repo 端 resignTimestamp 骨架 MetaFile-ClearSignatures-Sign-ToFile；tuf-on-ci-delegate 設簽署人門檻效期線上金鑰)·root.json 角色金鑰與 threshold(offline 硬體 keyid vs online KMS keyid 分層 JSON；roles threshold；AddKey 只收公鑰讓門檻私鑰永不集中)·離線門檻保管治理(硬體不可匯出／不同人不同地點分持／門檻<總數容忍偷幾把丟幾把／保管清單明文納稽核／緊急輪替發起授權寫進 runbook)·自建=失去 CT 外部制衡(承 Day77 內部 CA 盲區)用儀式留證+門檻分持+稽核上線補回·client bootstrap(cosign initialize --mirror --root --root-checksum=TOFU pin 承 Day101；mirror URL=SSRF 攻擊面承 Day10)·Day16 稽核(組態掃 threshold 下限-高權角色無線上 keyid-第一份 root 指紋；執行期掃效期剩餘比例-線上重簽成功也記指標+N 小時未成功告警；SIEM：root 版本跳變-門檻調低-效期到期-重簽失敗-指紋不符-儀式被繞過)·誤區·Code Review/治理 checklist·測試演練(門檻不足／未授權金鑰／高權角色混線上金鑰 CI 紅／門檻調低告警／第一份 root 指紋／線上重簽失效告警／緊急輪替桌面演練／退場演練) | TUF targets 委派與 hash bin delegation 縮小 targets 金鑰爆炸半徑(Day103)·root 金鑰 HSM PKCS#11 vs YubiKey PIV 選型·線上角色 KMS 簽章 pkg/signature/kms 實作·多 client fleet root 版本升級相容性·tuf-on-ci CI 儀式 pipeline 深入·私有 Fulcio/Rekor 自建與 trusted_root.json 內容·氣隙 mirror 同步與 targets 發佈流程 |
| 103 | `content/資訊安全/Day103_TUF_Targets_Delegation_Hash_Bin.md` | TUF targets 委派與 hash bin delegation：把 targets 簽署權往下拆、收斂爆炸半徑 | 新主題（targets 委派首次介紹，承 Day102 預告與 Day101「爆炸半徑切四段」往下鑽一層；明確不重述 Day101 頂層四角色分離與 Day102 root 儀式／離線保管；交叉 Day101／Day102／Day18／Day07／Day15／Day16／Day80／Day81） | 單一 targets 金鑰=root 之下全 repo 新單點(能為任一 target 背書，效期短不能像 root 關離線)·委派兩形態(命名委派按路徑語意 Delegations.Roles/Paths vs hash bin 按雜湊均勻分片 SuccinctRoles；心法命名切「誰負責」hash bin 切「攤平規模」可疊)·命名委派 go-tuf/v2(Delegations.Keys 只收下層公鑰+DelegatedRole Name/KeyIDs/Threshold/Terminating/Paths；把 maven/*.jar 委派給 jar-team；上層委派者 less volatile 效期拉長)·Terminating 截斷語意(true=路徑命名空間獨佔擋影子委派/false=fallthrough 接力)·路徑約束陷阱(go-tuf glob 逐段 filepath.Match 段數要相等 * 不跨 / → maven/*.jar 不吃 maven/com/x.jar 無 ** 跨樹要多寫或改 bin；委派只能收窄不能放大父角色權限，GetRolesForTarget 有序 GHSA-4f8r-qqr9-fq8j)·hash bin 心法(按 sha256 均勻灑進 2^bit_length 個 bin；binFor=sha256 前 4 bytes big-endian uint32 >>(32-bitLength) → 前綴-補零 hex 編號 對照 SuccinctRoles.GetRolesForTarget，真值 order-service.jar→bin-2ce；bit_length 8~12 取捨半徑 vs metadata 成本)·SuccinctRoles(BitLength/NamePrefix 免列舉上萬角色 TAP 15；AddKey role 參數可忽略；VerifyDelegate 自動走 SuccinctRoles 分支；consistent snapshot 消費端只抓命中 bin 不全下載)·blast radius 對照表(單一 targets=全 repo/命名委派=授權路徑內/hash bin=~1/N；委派金鑰折損由上層 targets RevokeKey+AddKey 換不動 root=Day101 折損復原下沉；安全換 metadata 成本蹺蹺板)·上線程度分層(高權命名委派/上層 targets 不上線比照 Day102，低權 bin 威力關小才上線 KMS 私鑰不落地)·發行端(go-tuf/tuf-on-ci 設拓撲)vs消費端(sigstore-java/updater 透明解析別手刻)·誤區·Code Review checklist·測試(越權路徑被擋/glob 段數/Terminating 影子/bin 分片一致 bin-2ce/門檻不足/折損半徑演練只炸一格/上線金鑰稽核) | TUF 委派 client 端解析與路徑逃逸防禦(pre-order DFS 遍歷/Terminating 消費端語意/下層不能簽父角色沒授權路徑/GHSA-4f8r-qqr9-fq8j 委派順序 advisory/cycle-visited 保護，Day104)·多層巢狀與 diamond 委派解析·python-tuf/tuf-on-ci hash bin 產生器與線上委派·委派 metadata 大小與 snapshot 膨脹效能取捨·per-team 委派金鑰保管與 CI 簽署動線·委派與 in-toto/SLSA provenance 綁定 |
| 104 | `content/資訊安全/Day104_TUF_Delegation_Traversal_Path_Escape.md` | TUF 委派 client 端解析與路徑逃逸防禦：pre-order DFS 遍歷／Terminating 消費端語意／順序即信任與 CVE-2024-47534 | 延伸（承 Day103 預告，鏡頭從發行端翻到消費端，委派消費端解析首次介紹；明確不重述 Day103 發行端授權／hash bin 分片與 Day101 頂層 threshold 機制；交叉 Day103／Day101／Day18／Day07／Day31／Day71／Day72／Day80） | 消費端職責=反向問題(手上一個 target 路徑→哪個角色簽名能背書它)·入口 GetTargetInfo→DownloadTarget(內部 preOrderDepthFirstWalk)·pre-order DFS(delegationsToVisit 當 stack 從尾 pop／roleParentTuple 帶 parent 供驗簽／前序檢查=先看當前角色自己有無 target、命中即回傳／自己沒有才標 visited 再下鑽／slices.Reverse 維持「pop 順序==宣告順序」／每跳先 loadTargets 下載+驗簽才讀內容)·順序即信任(in order of appearance implicitly order trustworthiness／回傳最先命中=最受信任角色的 target／payments-team 排前 vs catch-all 排後：順序對則假貨蓋不過，順序亂則下載錯成品且無金鑰被偷)·CVE-2024-47534 GHSA-4f8r-qqr9-fq8j(High CVSS8.7；GetRolesForTarget 舊回 map[string]bool→Go map 迭代隨機→委派追錯順序→下載錯成品；test_graph_traversal two-level-delegations targets→A/B、B→C 應 A→B→C 卻 B→C→A；修法 map 換有序 []RoleResult 連原始碼註解釘 advisory；受影響 <=v2.0.0 修於 >v2.0.0 實務 v2.3.1；後端防線=版本衛生 govulncheck/SBOM 當紅線)·Terminating 消費端語意(命中截斷角色→清空 delegationsToVisit+break→不 fallthrough 不回頭／發行端 Day103 設定、消費端執行／忘設截斷則穿透到 catch-all／SuccinctRoles 一律 terminating 只回唯一算出的 bin TAP15)·路徑逃逸防禦(結構性非額外檢查：GetRolesForTarget→IsDelegatedPath→isTargetInPathPattern 以 / 切段、段數相等、逐段 filepath.Match、* 不跨 /；被攻陷下層簽超出授權 Paths 的 target→父角色路由不到→永遠不被訪問=死資料；真路徑逃逸擋得下 vs 授權範圍內濫用要靠 Day103 收窄 Paths／拉高 Threshold)·遍歷護欄(visitedRoleNames 擋環 A→B→A／前序檢查後才標 visited／cfg.MaxDelegations 封頂擋委派炸彈 DoS 承 Day31/71/72)·每跳 VerifyDelegate(驗簽在讀 target 之前、任一跳門檻不足即整體中止；門檻以不同公鑰 PKIX SHA-256 指紋計、擋同鑰多 keyid 登記冒充多票)·發行端 go-tuf／tuf-on-ci 設拓撲 vs 消費端 sigstore-java dev.sigstore.tuf.Updater 透明解析別手刻／JDK11+、Java1.8 用 sidecar／版本衛生為主防線·誤區·Code Review checklist·測試(graph traversal 順序穩定／順序即信任前者勝／Terminating 不 fallthrough／路徑逃逸路由不到／glob 段數／bin 唯一且截斷／門檻不足／同鑰多登記不加分／環與深度護欄) | TUF 消費端 rollback／freeze／mix-and-match 版本回滾防禦 trustedmetadata UpdateSnapshot／UpdateTimestamp 版本單調遞增+過期+meta 綁定(Day105)·多層巢狀與 diamond 委派解析順序·consistent snapshot 一致性與 hash-prefixed target 下載·TUF conformance 測試套件導入 CI 當回歸·委派 metadata 快取與 UnsafeLocalMode 離線風險·MaxDelegations／MaxRootRotations 調參與 DoS 取捨 |
| 105 | `content/資訊安全/Day105_TUF_Rollback_Freeze_Mix_And_Match_Defense.md` | TUF 消費端 rollback／freeze／mix-and-match 防禦：trusted metadata set 的版本單調遞增＋過期檢查＋上層 meta 綁定 | 延伸（承 Day104 預告，鏡頭從委派橫向解析翻到 metadata 縱向版本／時間可信度；rollback／freeze／mix-and-match 攻擊面首次介紹。明確不重述 Day104 委派遍歷與 Day101 root 輪替 threshold；交叉 Day104／Day101／Day18／Day16／Day19） | 三攻擊只玩版本與時間、不需偷金鑰(rollback 回放合法但過時舊版讓你錯過安全更新／freeze 一直吐同一份舊的凍在漏洞版本／mix-and-match 拼裝不同 repo 狀態各自合法的 metadata)·trusted metadata set=client 手上可信 repo 狀態(TrustedMetadata struct: Root/Snapshot/Timestamp/Targets map/RefTime)·固定更新鏈 root(N)→timestamp→snapshot→targets→委派 targets 且狀態機硬擋亂序(cannot update timestamp after snapshot／cannot update snapshot before timestamp or after targets)·rollback 防禦=版本單調遞增(UpdateRoot 嚴格+1 一版不跳／UpdateTimestamp 舊版 ErrBadVersionNumber、同版 ErrEqualVersionNumber 保留舊、連 timestamp 指向的 snapshot meta 版本也不准回滾／UpdateSnapshot 逐角色不回滾+不准刪 Meta 記錄)·intermediate metadata=過期舊版仍載入當防回滾下限、只在當最終版用時才查過期(checkFinalTimestamp／checkFinalSnapshot)、丟掉下限就歸零 rollback 復活·freeze 防禦=過期檢查(IsExpired(RefTime)／ErrExpiredMetadata／timestamp 短效期=freeze 窗口上限=安全參數／5.3.10 最終 root 不過期)·freeze 隱性前提=時鐘可信(RefTime 被 NTP 中間人往回撥則 IsExpired 恆 false 繞過、authenticated NTP/NTS+監控時鐘偏移)·mix-and-match 防禦=綁定鏈(timestamp 用版本+雜湊 VerifyLengthHashes 釘 snapshot、checkFinalSnapshot 要版本相符／snapshot 用版本+雜湊釘每個 targets、UpdateDelegatedTargets 版本相符；雜湊擋換內容不換版本、版本擋換版本不換內容、只留一個合法組合)·先縱向確認版本後橫向找角色(Day104 委派遍歷在第4步內部、須先確認那份 targets 新鮮對版)·後端維運三責任(持久化 trusted metadata 當 rollback 下限且權限受控別清掉別被寫壞／機器時鐘可信/版本衛生 govulncheck+SBOM 承 Day18/104)·JVM sigstore-java dev.sigstore.tuf 透明維護持久 store 別手刻/JDK11+ Java1.8 sidecar·錯誤型別 ErrBadVersionNumber/ErrEqualVersionNumber/ErrExpiredMetadata/ErrRepository/ErrRuntime·誤區·Code Review checklist·測試(timestamp/snapshot rollback 拒、同版留舊、指向下層版本防回滾、不准刪、freeze 過期拒、intermediate 過期仍當下限、時鐘操縱模擬、mix-and-match 雜湊/版本拒、持久化下限重啟) | consistent snapshot 一致性下載與 hash-prefixed 檔名(`<version>.snapshot.json`／`<hash>.target`)、ConsistentSnapshot 開關與發佈原子性/並行不撕裂(Day106)·非一致快照 repo 發佈半成品競態·TUF conformance 測試導入 CI 當回歸·委派 metadata 快取與 UnsafeLocalMode 離線風險·MaxRootRotations 調參與 fast-forward attack recovery·repo 端 metadata expiry 策略與簽署自動化排程 |
